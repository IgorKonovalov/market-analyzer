"""Vendored slice of github.com/atilaahmettaner/tradingview-mcp.

Pinned SHA recorded in `vendored.lock` at the repo root. Per ADR-0003, files
in this tree are copied verbatim from upstream (only import-path rewrites
permitted). All structural changes live in `market_analyser.data.adapters.*`.

The MIT LICENSE alongside this file is the upstream license, unchanged.
"""
