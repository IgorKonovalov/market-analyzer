# Plans

Implementation plans for `market-analyser`. One file per plan (`NNNN-<slug>.md`), authored by `architect` and implemented by the sibling skill(s) named on each phase. Closed plans live in [`done/`](done) — the architect moves them there at the close ceremony, never the implementer.

This index is the one-minute view of what's in flight. The plan file (and `git log`) is the source of truth; if a status here disagrees with a plan's own `Status:` line, trust the plan file and fix this index.

## Active roster

| #    | File | Status | Summary |
|------|------|--------|---------|
| 0026 | [0026-live-signal-evaluator](0026-live-signal-evaluator.md) | approved | First step of the advisor + forecasting track ([ADR-0029](../adrs/0029-advisory-recommendation-boundary.md)/[ADR-0030](../adrs/0030-forecasting-subsystem.md)). Evaluate a strategy against the **current** bar (vs `run_backtest`'s historical path): pure `backtest/live_signal.py` + an `evaluate_signals` tool + `signal.evaluated v1` SSE + a reactive viewer panel. Stays a condition-reporter (no recommendations). 3 phases: `backtester` → `dev` → `ui-builder`. |
| 0034 | [0034-defi-deep-lp-detail](0034-defi-deep-lp-detail.md) | approved | DeFi **deep adapters — LP detail** (depth half of the ADR-0034 hybrid). Enrich Zerion's discovered Uni-v3 / Aerodrome LPs with precise on-chain state — tick range, in-range, uncollected fees (the `DefiPosition` fields Plan 0032 left `None`). **Source resolved by the 2026-06-05 [Zerion-API survey](../references/zerion-api-capabilities.md): RPC + The Graph keyed on `pool_address` — ADR-0034 holds, no new ADR.** 5 `dev` phases: F1 fix (`farming`/`staked` LPs → `lp`) → Protocol + model fields → Aerodrome adapter (one-hop) → Uni-v3 adapter (NFT-`tokenId` two-hop) → enrichment + live smoke. **Approved to build both paths up front**; Uni-v3 ships fixture-tested (live Uni-v3 verification deferred — F3). Aave health a later plan; no P&L/risk/UI. |
| 0039 | [0039-advisor-ui-surface](0039-advisor-ui-surface.md) | approved | **Pillar 2 (Advisor) UI.** Recommendations view rendering Plan 0038's call (direction, entry/stop/target, conviction, rationale, basis) reacting to a `recommendation.completed v1` SSE event. Hard constraint: **unmistakably advisory, no submit/trade control anywhere** (the ADR-0025 boundary, asserted as a spec). `dev` (event) → `ui-builder` (view). |
| 0038 | [0038-advisor-layer](0038-advisor-layer.md) | approved | **Pillar 2 (Advisor).** `advisor/` package fusing conditions (0018) + live signals (0026) + backtested edge (0020/0024) + forecasts (0036) into a **labeled trade recommendation** (dir/entry/stop/target/conviction/rationale/basis) via a `recommend` tool. Implements [ADR-0029](../adrs/0029-advisory-recommendation-boundary.md) (accepts at close). Basis-free recommendation = validation error (the rule enforced in code); advisory-only, **no keys/orders**. 2 `dev` phases + companion `advisor`-skill creation & `defi-analyst` charter reconciliation. |
| 0037 | [0037-forecast-ui-surface](0037-forecast-ui-surface.md) | approved | **Pillar 1 (Forecasting) UI.** Reactive Forecast panel rendering Plan 0036's up/down/flat probability + walk-forward skill-vs-baseline + provenance, reacting to a new `forecast.completed v1` SSE event; honest-uncertainty presentation with an explicit **no-edge** state. `dev` (event) → `ui-builder` (panel). |
| 0036 | [0036-forecasting-subsystem-foundation](0036-forecasting-subsystem-foundation.md) | approved | **Pillar 1 (Forecasting)** of the trade/predict/portfolio program — `forecast/` package predicting **next-bar direction as a calibrated up/down/flat probability** from cached OHLCV. Causal feature pipeline over the ADR-0023 `analysis/` surface → deterministic `HistGradientBoostingClassifier` (sklearn, seeded + single-thread) → **walk-forward validation that rejects any model not beating a naive baseline** (reuses Plan 0020) → `forecast` MCP tool carrying validation basis + model provenance. Implements [ADR-0030](../adrs/0030-forecasting-subsystem.md); paired [ADR-0040](../adrs/0040-forecasting-model-artifacts.md) (library/determinism/versioning). 4 phases: `dev` (features) → `dev` (label+training) → `backtester` (validation gate) → `dev` (registry+tool). No UI (Plan 0037), no recommendation (advisor/Plan 0038). Adds `scikit-learn`+`statsmodels` (exact-pinned). |
| 0035 | [0035-defi-pnl-reconstruction](0035-defi-pnl-reconstruction.md) | approved | DeFi **P&L reconstruction** — implements [ADR-0036](../adrs/0036-defi-pnl-reconstruction.md)'s transaction-replay method end to end. Ingest Zerion's decoded `/transactions/` history (normalized `DecodedTx`, **immutable SQLite cache**) + a DefiLlama `HistoricalPriceSource` with a **price-snapshot cache** (the determinism mechanism), then replay per position with **average-cost lots** at **block-time** prices → realized/unrealized P&L + LP-vs-HODL, surfaced via `compute_wallet_pnl` tool + `POST /defi/pnl`. Zerion `/pnl` retained only as an advisory cross-check (no trust in aggregator P&L). 8 phases (7 `dev` → `human` live smoke). **No new ADR** (implements ADR-0036/0035/0034); the smoke **closes ADR-0036** (`proposed → accepted`). Adds **2 migrations** + touches `data/sources.py`/`defi/` → **serialize with 0034**, not worktree-parallel. No UI/risk (later plans). |

## Recently closed

Full close-review notes live in each plan file under `done/`. Most-recent first.

| #    | Closed | Summary |
|------|--------|---------|
| 0033 | 2026-06-04 | UI theme control + visual polish — in-app **Light/Dark/System** (Settings *Appearance* segmented control + header `ThemeToggle`, one source of truth in `lib/theme.ts`), persisted in `localStorage['ma.theme']`, applied **pre-paint** via a `data-theme` attribute set by a tiny inline `<head>` bootstrap so there's no flash. The prod main-process CSP is `script-src 'self'` (no `'unsafe-inline'`), so the bootstrap is admitted by a **SHA-256 hash** — `window.csp.test.ts` recomputes it from `index.html` and asserts the hash is present and `unsafe-inline` is not. Plus a token/chrome visual-polish pass (palette/elevation/type-scale, both themes) and a **theme-aware candlestick chart** (candle/overlay/marker colors → CSS tokens, recolored in place via `applyOptions` on an `effectiveTheme` change — `CandlestickChart.theme.test.tsx` proves `createChart` fires once across the flip, i.e. no remount). [ADR-0039](../adrs/0039-renderer-theming-localstorage.md) accepted (renderer-owned theming, no sidecar route/IPC). Four `ui-builder` commits on `main` (`e87dea3`→`143f334`→`c0a2d9c`→`b8d48c6`). Clean Mode 4 — no blockers; one commit-hygiene nit (phase-1 commit carried an R100 rename of Plan 0032 into `done/`, left as-is per no-history-rewrite) + the visual-only AA-contrast/reduced-motion done-whens accepted on the implementer's verification. 31 renderer + 3 main specs green at close. No branch (committed on `main`). |
| 0032 | 2026-06-03 | DeFi wallet discovery — first plan of the DeFi program. Paste an EVM address → decoded positions across Ethereum/Base/Arbitrum/Optimism via Zerion. Four `dev` phases on `main`: `SecretsStore` (`0600` `secrets.json`, env-override-first, write-only `GET/POST /settings/secret(s)`, value never logged/echoed); `WalletPositionsSource` Protocol + `ZerionAdapter` (HTTP Basic, lazy key, typed `ZerionAuthError`/`ZerionError` taxonomy) + boundary-validated `DefiPosition` model; a source-agnostic `defi/` discovery service + async scan job streaming `defi.scan_*` SSE (masked wallet, "never silently zero"); `scan_wallet` MCP tool + renderer-gated `POST /defi/scan`. Correct layering (ADR-0032: `defi→events/data`, never `api`). **Paired ADR-0035 + ADR-0038 accepted; ADR-0034 held `proposed` pending the live smoke** (Zerion decoding quality unverified offline). Clean Mode 4 — one Major carried to the live smoke (LP `usd_value` sums per-token `value` against a *synthetic* fixture; the smoke must confirm it's not 2×) + two doc nits. 964-pass offline pytest + `mypy --strict` + ruff green (pre-push). No branch (committed on `main`). **Live smoke ran 2026-06-05 → [ADR-0034](../adrs/0034-defi-portfolio-aggregator.md) accepted:** scanned a real wallet, 5 Base positions, DeFi total reconciled to Zerion's net worth (no 2× — Major cleared); the smoke caught + fixed a `filter[positions]` bug (`4955b2e`) that had excluded all DeFi positions. Two non-blocking followups for the deep-adapter plan: F1 staked-LP classification (`kind="staking"`/`pool=null` → fold to `lp` + pool + de-dupe tokens) and F3 live-confirm Aave/Uni-v3. |
| 0023 | 2026-06-03 | News view in the app — first renderer surface for Plan 0010's news/sentiment. A renderer-bearer-gated `GET /news` (`news.py` + frozen `NewsResponse` envelope; window via the shared `SentimentWindow` literal; blank/whitespace symbol = browse-all, no aggregate) feeding a standalone **News** view (`NewsView` + typed `api.getNews` + nav tab). Untrusted feed content is rendered as text (no `dangerouslySetInnerHTML`), hrefs sanitized to `http(s)` and opened via `shell.openExternal`; server-side ±0.05 sentiment bucketing is not re-implemented (per-headline badge = compound-score sign only). No new ADR (a renderer read route like `/ohlcv`/`/search`). Clean Mode 4 (no blockers; two nits logged — stale `App.tsx` "Four views" docstring, optional `GET /news` smoke step). 8 route specs + 7 `NewsView` specs + full suites (renderer 229, API 328) + `mypy --strict` + `gen-types --check` no drift green. Branch `plan-0023-news-view` merged to `main` at close. |
| 0020 | 2026-06-03 | Extended backtest metrics + walk-forward + comparison tools — appended six metrics to `BacktestMetrics` per [ADR-0024](../adrs/0024-extended-backtest-metrics.md) (Calmar/Sortino/profit-factor/expectancy/best-worst; genuinely-undefined ratios are `null`, never `0.0`/NaN), bumped `ENGINE_VERSION` 0.1.0→0.2.0 and regenerated the 0008 golden fixture; added `walk_forward()` rolling out-of-sample evaluation (contiguous array-split anti-lookahead folds, per-fold metrics + aggregate + full-run baseline; `WalkForwardConfigError` on bad `n_splits`); and the `compare_strategies` (ranked leaderboard, deterministic None-last/`strategy_id` tie-break) + `walk_forward_backtest` MCP tools (both constrain `timeframe` to `1d/1h/1m`). Paired ADR-0024 accepted. Clean Mode 4 (no blockers); the four implementer-flagged decisions all confirmed sound — field defaults = degenerate value (engine sets them explicitly); `BacktestRunSummary` unchanged (Calmar column is a `dev` persistence migration); no pinned "material degradation" flag (baseline+aggregate exposed instead); UI rendering deferred to a `ui-builder` follow-up (logged below). 88 backtest specs (incl. golden/determinism) + 13 tool specs + mypy `--strict` green. Branch `plan-0020-backtest-metrics` merged to `main` at close. |
| 0022 | 2026-06-03 | Macro context — `bitcoin_market_pulse` (CoinGecko: BTC price/24h, dominance, total mcap/24h + a neutral structural `regime`) via a new `get_macro_context` Protocol method on a `CoinGeckoAdapter` (**two** keyless calls — `/global` for dominance + total-cap trend, `/simple/price` for BTC price + its own 24h move, which `/global` lacks); plus `market_snapshot` fanning `get_quote` over a fixed 8-symbol basket (`^GSPC`/`^IXIC`/`^VIX`/`BTC-USD`/`ETH-USD`/`EURUSD=X`/`SPY`/`GLD`) with per-symbol graceful degradation (one bad symbol → typed-null entry, rest return; "no other data dependency" enforced structurally in the test). Paired [ADR-0027](../adrs/0027-crypto-macro-regime-classification.md) accepted — `regime` is a closed neutral 4-value vocabulary (`btc_led`/`alt_structure`/`risk_off_structure`/`neutral`); thresholds pinned (−5% risk-off floor, 1pp dominance-trend deadband) and reconciled at close, with BTC-vs-whole-market 24h relative performance as the (direction-exact) dominance-trend proxy. Clean Mode 4 (no blockers; two pre-existing tracked nits — inline `datetime.now` seam, partial tool-registration test). 28 specs + full 307-pass API suite + mypy `--strict` green. Branch `plan-0022-macro-context` merged to `main` at close. |
| 0031 | 2026-06-03 | Yahoo absolute-range fetch — switched the OHLCV fetcher from now-relative `range=` to absolute `period1`/`period2`, so a past-ending window is fetched verbatim (`_yahoo_fetch.py`/`yahoo.py`); lifted the artificial 732-day span cap for uncapped timeframes; added the 2026-05-31 error-envelope guard. Phase 2 (finding M1) mapped the typed `UpstreamDataError` family to `/ohlcv` HTTP status (404/429/422/502) over the real provider+adapter chain — the route's 502 branch was dead for the real path — and recency-gated the empty-response branch ([ADR-0033](../adrs/0033-empty-ohlcv-response-by-recency.md) accepted): a strictly-historical empty window returns `[]`, a leading-edge empty window raises `UnknownSymbolError`→404. **Unblocked 0030.** Clean Mode 4 (no blockers; one redundant-catch nit left as-is). Live smoke: 249 AAPL 1d bars over a past year (pre-fix ~11). |
| 0030 | 2026-06-03 | Lazy historical loading (scroll-left to fetch older bars) — `useOhlcvHistory` accumulating buffer hook + `useLazyHistoryTrigger` visible-range trigger + scroll-anchored prepend (shift-right-by-N) + left-edge affordance/error chip. Rides the existing synchronous `GET /ohlcv` (no new route, no `dev` work); reacts to 422 (clamp) / empty (`reachedStart`) instead of mirroring data-layer constants. The deterministic seeded-cache Playwright e2e (`0cd7f7a`, real — asserts barCount growth + second fetch) is the close gate; the live path was dead until [Plan 0031](done/0031-yahoo-absolute-range-fetch.md), closed in the same ceremony. No new ADR (applies ADR-0007/0008). Clean Mode 4 (no blockers). |
| 0028 | 2026-06-02 | Data-layer boundary & extensibility hardening (2026-05-31 audit): per-capability source Protocols (`data/sources.py`, 7 `@runtime_checkable`) + selector-registry dispatch so adding a sentiment/market-sentiment source is one dict entry, not a 4-5-file edit; extracted the inlined VADER aggregation into `RssVaderSentimentAdapter`; relocated `EventBus`+schema from `api/events/` to a neutral top-level `events/` core, killing the lone `data→api` import (`data.backfill` now pulls zero `api` modules). [ADR-0031](../adrs/0031-data-source-adapter-contract.md) + [ADR-0032](../adrs/0032-data-layer-no-api-dependency.md) accepted at close. Behavior-preserving — 802 offline specs + renderer parity guard green. Clean Mode 4 (no blockers; one docstring nit left as-is). |
| 0029 | 2026-06-02 | Decompose the 618-line `CandlestickChart.tsx` god component (audit): extracted `useChartGestures` (the 8 prop-mirror refs are gone), moved marker/overlay math to `lib/markers.ts` + `lib/overlays.ts`, collapsed the 4-spot overlay seam into one `OVERLAY_REGISTRY` table (adding a kind is now one row). Behavior-preserving — component 618 → 364 lines; 200 renderer specs + the `__test_chart_render__` gate green. No new ADR (applies ADR-0008). Clean Mode 4 (no blockers); two nits left as opportunistic cleanup (pre-existing `act()` warning in `gestures.test.tsx`; a fix-forward two-commit phase-2 split). **Unblocks 0030.** |
| 0021 | 2026-06-02 | Multi-timeframe alignment (`multi_timeframe_analysis`, W→D→4H→1H→15m) + three volume-scanner tools (`volume_breakout`/`volume_confirmation`/`smart_volume`) over a supplied symbol list. Consumes the 0018 snapshot + 0027 volume primitives; default ladder unblocked by 0025. No new ADR (within ADR-0023/0007). Clean Mode 4 (no blockers); one Minor + 3 nits fixed pre-close in `fd35da3`. |
| 0027 | 2026-05-31 | Volume bars + volume-aware analysis — `analysis/volume.py` (volume MA, relative volume + percentile, OBV + slope, trailing VWAP, `volume_summary`); folds a `volume_stance` + volume measures into `condition_snapshot`; renders volume/VWAP/OBV chart bands client-side from `bars`. No new ADR (within ADR-0023/0008). **Unblocks 0021's phase 2.** |
| 0019 | 2026-05-31 | Live quote — `get_quote` (`YahooQuoteAdapter` on the chart endpoint) + `quote_for` tool. Discharged the last `MarketDataProvider` stub. **Unblocks 0022.** |
| 0025 | 2026-05-30 | Timeframe expansion — `15m`/`1w` (native) + `4h` (in-house resample); canonical `data/timeframes.py` registry; `history_exceeded` reason. ADR-0028 accepted. **Unblocks 0021's timeframe half.** |
| 0018 | 2026-05-30 | Technical-analysis surface — `analysis/` (9 indicators + 14 patterns + `condition_snapshot`) + `analyze_symbol` tool. ADR-0023 accepted. **Unblocks `market-analyst` + 0021.** |
| 0017 | 2026-05-30 | MCP tool-registration refactor — migrated 8 inline tools to the `register_*` pattern; `mcp_app.py` 576 → 131 lines. Behavior-preserving. |
| 0014 | 2026-05-30 | Interactive chart + agent-mode — UI-event vocabulary + `POST /ui_events` + `get_pending_ui_events` + agent-mode toggle + single-instance Electron. ADR-0021 accepted. |
| 0024 | 2026-05-29 | Symbol search + autocomplete — `search_symbols` over Yahoo `/v1/finance/search` + `GET /search` + debounced `SymbolPicker`. ADR-0026 accepted. |
| 0013 | 2026-05-26 | Auto-backfill on cache miss — honest `get_ohlcv` shape + `backfill_ohlcv` tool + `ohlcv.backfill_*` SSE + typed `UpstreamDataError` taxonomy + `BackfillCoordinator`. |
| 0012 | 2026-05-25 | StockTwits sentiment — second per-symbol source (explicit labels, no NLP); `source` param on `get_sentiment`; first `ResilientHttpClient` subclass. |

Earlier closed plans (0001–0011, 0015, 0016) are in [`done/`](done) and `git log`.

## Open follow-ups (no plan needed)

Small items carried from closed plans — pick up opportunistically; remove the row when it lands. If one grows past ~half a day or starts coupling other changes, promote it to a real plan.

| From | Item | Owner | Note |
|------|------|-------|------|
| 0008 | Trade-log P&L $ formula compounds inconsistently with the engine for multi-trade runs | `architect` → `ui-builder` / `backtester` | Plan 0008 pinned UI P&L as `(exit-entry)*(initial/entry)`, but `_build_equity_curve` compounds (`units = cash/entry` where `cash` is running equity). Row N>1 disagrees with the equity-curve delta (~10% on the 2-trade example). Either add a `Trade.pnl_usd` field (ADR-0018 amendment — becomes its own plan) or derive in the renderer from `equity_curve`. |
| 0008/0009/0013 | Refresh `diagrams/claude-cli-driven-architecture.md` with backtest-results, screener, and backfill lanes | `architect` | The map predates `run_backtest`/`BacktestView` (0008), `screener_query`/TradingView (0009), and `backfill_ohlcv`/`BackfillCoordinator`/`ohlcv.backfill_*` (0013). One pass adds all three. |
| 0012 | `news_for.py:31` still declares the window vocabulary inline | `dev` | The window hoist (`data/_windows.py`, commit `bccfc61`) migrated `sentiment_for_news` + `stocktwits_sentiment` but missed `news_for`, which carries the same `Literal["1h","4h","24h","7d"]`. Import `SentimentWindow` there too, then `gen-types --check` (stays clean — pydantic inlines the alias). |
| 0014 | Confirm whether Claude Code surfaces `notifications/resources/updated` to the model | `human`/`architect` | The phase-4 smoke confirmed the polling tool but not the best-effort push path. Polling is the contract regardless; re-measure before any plan leans on push (ADR-0021 open question). |
| 0020 | The `float \| None` extended metrics and the comparison/walk-forward reports have no UI surface | `ui-builder` | Plan 0020 added Calmar/Sortino/profit-factor/expectancy/best-worst (some `null`) to `BacktestMetrics` and the `compare_strategies` (leaderboard) + `walk_forward_backtest` (per-fold + aggregate) tools — all agent-only today. A renderer follow-up would render `null` as "—" and add a leaderboard / fold-report view. Optional; only if the user wants these on-screen. Also optional: extend `metrics._TIMEFRAME_BARS_PER_YEAR` to Plan 0025's `15m/4h/1w` so the two tools can compare on those timeframes (they currently constrain to `1d/1h/1m`). |
| 0032 (F1) | Zerion adapter classifies gauge-staked LPs as `kind="staking"` (`pool=null`) + lists per-leg duplicate token symbols | `dev` (likely folds into the deep-adapter plan) | Live-smoke finding 2026-06-05. Zerion returns staked Aerodrome/QuickSwap LPs with `position_type: staked`; `_classify_kind` (`data/adapters/zerion.py`) should fold them to `kind="lp"`, carry the pool name, and de-dupe tokens by symbol (sum amounts per symbol). **Values are correct** (reconcile to net worth) — this is classification/cosmetics, not a value bug. |
| 0032 (F3) | Live-confirm Aave & Uniswap-v3 decoding | `human` / `architect` | Live-smoke finding 2026-06-05: the smoke wallet was Aerodrome-heavy, so Aave (lending supply/borrow) and Uni-v3 LP classification remain fixture-only. Re-run `scan_wallet` on a wallet holding them before the deep-adapter / P&L plans lean on those shapes. |

_The Plan 0013 `data→api` smell was promoted to [Plan 0028](done/0028-data-layer-boundary-hardening.md) phase 3 ([ADR-0032](../adrs/0032-data-layer-no-api-dependency.md)) after the 2026-05-31 audit re-flagged it — **resolved at 0028 close (2026-06-02)**: the event bus now lives in the neutral top-level `events/` core._

### From the 2026-05-31 architecture audit

Minors surfaced by the codebase-health audit. The three majors became Plans [0028](done/0028-data-layer-boundary-hardening.md)/[0029](done/0029-candlestick-chart-decomposition.md) (both closed); these are the small items. Same rule — pick up opportunistically; promote if one grows past ~half a day or couples other changes.

| Area | Item | Owner | Note |
|------|------|-------|------|
| data | `Bar` permits zero prices → latent `ZeroDivisionError` | `dev` | `data/types.py` validator rejects `< 0` but allows `0.0`; `backtest/metrics.py:102,238` divides by entry/price. Tighten OHLC to `> 0` or guard the division. |
| api | MCP tools read wall-clock inline (snapshot-testability) | `dev` | ~8 tools call `datetime.now(tz=UTC)` for `queried_at`/`analyzed_at` with no seam (`analyze_symbol.py:97,100`, `quote_for.py:61`, `news_for.py:74`, …), blocking output snapshot-equality tests. Route through a module-level `_now()` seam like `default_provider._now`. |
| api | `ohlcv` route imports a data-internal underscore symbol | `dev` | `api/routes/ohlcv.py:15` imports `ResilientHttpError` from `data._http`; re-export it via the public `data/errors.py` and import from there. |
| api | No safety-net test that every MCP tool is registered | `dev` | `tests/api/test_mcp_tools.py:441` only checks a 3-tool subset; a forgotten `register_*` call yields a silently-missing tool. Assert the full expected toolset is registered. |
| desktop | SSE payloads cast unvalidated (IPC/SSE asymmetry) | `ui-builder` | `useEventStream.ts:216-235` validates only the envelope wrapper, then casts `payload as ChartShowPayloadV1` and hands it to the reducer raw — IPC is Zod-validated, SSE is not. Add Zod `safeParse` per event payload in `dispatchEnvelope`. **Consider promoting** if more event types land. |
| desktop | `sidecar:status` not parsed + raw channel literal | `ui-builder` | `main.ts:80` sends `'sidecar:status'` as a string literal (every other channel uses `IPC_CHANNELS`); `client.ts:62-73` consumes the payload without `SidecarStatusSchema.parse()`, forcing two `!` assertions. Parse on the renderer side and reference the constant. |
| desktop | Test gaps: `format.ts` + IPC handlers | `ui-builder` | `renderer/lib/format.ts` (NaN→"—", rounding, sign) has no direct test; `appHandlers.ts`/`dialogHandlers.ts`/`shellHandlers.ts` have no unit test (only `sidecarHandlers` does — and `shellHandlers` is a security boundary). Add `format.test.ts` + handler registration/validation specs. |
| desktop | `client.test.ts` shares module state across cases | `ui-builder` | `client.test.ts:69-171` reads "whatever port a prior test left" instead of pinning — order-coupled. Wrap the config-refresh block in `jest.isolateModules` or reset the module cache in `beforeEach`. |

_The `data` "Yahoo error-envelope not guarded" item was absorbed into [Plan 0031](done/0031-yahoo-absolute-range-fetch.md) (same file `_yahoo_fetch.py`, same parse path) and **shipped with it (closed 2026-06-03)** — the `_parse_chart_payload` guard now raises the typed taxonomy instead of a raw `KeyError`/500._

## Recommended execution order

The committed roster (Tier 2 0009–0012, then 0013–0014) is fully closed, as are the capability-expansion gates 0018, 0019, 0025, and 0027, the analysis batch's 0021, the macro-context plan 0022, the metrics/walk-forward plan 0020, the lazy-loading pair 0030/0031, the News view 0023, the DeFi wallet-discovery plan 0032 (first of the DeFi program; **[ADR-0034](../adrs/0034-defi-portfolio-aggregator.md) accepted 2026-06-05 after its live smoke** — Zerion decoding validated against a real wallet; the next DeFi plan, deep adapters, is now **drafted as [Plan 0034](0034-defi-deep-lp-detail.md)** — LP detail first, folding in the staked-LP followup F1), and (2026-06-04) the UI theme + visual-polish plan 0033 ([ADR-0039](../adrs/0039-renderer-theming-localstorage.md) accepted — renderer-owned theming via `data-theme` + localStorage). **Pickup-ready now: 0026, 0034, 0035, and 0036** (all `approved`; 0036 opens the trade/predict/portfolio program — see its reservation block below). **Plans 0037–0039 are also `approved`** but sequence behind their dependencies: 0037 (forecast UI) after 0036's `forecast` tool exists; 0038 (advisor) after both 0036 and 0026 (live signals); 0039 (advisor UI) after 0038. **Plan 0034 (DeFi deep LP detail) was approved 2026-06-05** after the Zerion survey resolved its source — build both the Aerodrome and Uni-v3 deep paths up front; a 2026-06-05 architect review added an explicit `pool_address` field to phase 1 (the discovery→deep join key, absent today) — see below. **Plan 0035 (DeFi P&L reconstruction) was approved 2026-06-05** — the P&L vertical implementing ADR-0036; it **serializes after 0034** (shared `data/sources.py` + `defi/` + the migration-chain rule), and **0034 should run first** so 0035 inherits the `pool_address` field its event→position join needs.

- **0026** (approved 2026-06-02, pickup-ready) opens the advisor + forecasting track ([ADR-0029](../adrs/0029-advisory-recommendation-boundary.md)/[ADR-0030](../adrs/0030-forecasting-subsystem.md), both proposed); it depends on nothing not already shipped and runs largely parallel to the analysis batch. Scoped at approval to the full `data/timeframes.py` set (15m/1h/4h/1d/1w) now that Plan 0025 has landed. Mixed-owner: `backtester` (phase 1) → `dev` (phase 2) → `ui-builder` (phase 3); phase 2 touches the event bus — now relocated to the neutral top-level `events/` core (Plan 0028, landed), so import from `market_analyser.events`, not `api.events`. Its `ui-builder` phase 3 adds a viewer panel on the chart that now coexists with 0030's (landed) scroll-left lazy-loading on the same `CandlestickChart`/`OhlcvView` surface — keep the visible-range subscription separate from any new panel wiring.
- **0034** (DeFi deep LP detail, `approved` 2026-06-05) is the depth half of the ADR-0034 hybrid: enrich Zerion's discovered Uni-v3/Aerodrome LPs with on-chain tick range / in-range / uncollected fees. The 2026-06-05 [Zerion-API survey](../references/zerion-api-capabilities.md) resolved the source — `pool_address` on 28/28 complex positions → **our RPC + The Graph keyed on `pool_address`** (ADR-0034 holds; no new ADR) — and pinned the F1 discriminator (`protocol_module: "farming"`). **Approved to build both paths up front:** 5 `dev` phases (F1 → Protocol/model → Aerodrome adapter → Uni-v3 NFT-`tokenId` adapter → enrichment + live smoke); Uni-v3 ships fixture-tested, live-confirmed later (F3). Migration-free; file-disjoint from 0026 (data-layer + `defi/` vs 0026's `backtest/`/event/UI surface), so the two could run in parallel.
- **0035** (DeFi P&L reconstruction, `approved` 2026-06-05) implements [ADR-0036](../adrs/0036-defi-pnl-reconstruction.md) end to end: Zerion `/transactions/` ingestion (normalized `DecodedTx` + an immutable SQLite cache) → a DefiLlama `HistoricalPriceSource` + `(token, timestamp)` price-snapshot cache (the determinism mechanism) → an average-cost replay engine valuing each leg at its **block timestamp** (the no-lookahead corollary) → realized/unrealized + LP-vs-HODL, surfaced via `compute_wallet_pnl` + `POST /defi/pnl`. 8 phases (7 `dev` → a `human` live smoke that gates **accepting ADR-0036** at close). **No new ADR.** It adds **two SQLite migrations** and touches `data/sources.py` + `defi/`, so it is **file-adjacent to 0034 — serialize, do not run in parallel worktrees** (the migration-chain rule alone forces it), and **0034 should run first**: 0035's phase-5 event→position join keys on the `pool_address` field 0034 phase 1 adds to `DefiPosition` (absent today). The P&L cost basis it produces is the later risk engine's input ([ADR-0037](../adrs/0037-defi-position-risk-forecast.md)); no UI/risk here.
### Trade / predict / portfolio program (provisional, approved-in-shape 2026-06-05)

A five-pillar program taking the app from read-only analyst toward forecasting, advice, cross-venue portfolio management, and **testnet-only** assisted execution. The shape was approved by the user 2026-06-05 after a gap analysis + a verified deep-research pass; the per-plan breakdown below is **provisional** (only Plan 0036 + ADR-0040 are written so far). Key user-set constraints: **forecasting + advisor first**; execution is **paper/testnet only** (no real-funds gate designed yet); Polymarket is a **read-only odds signal now, trading venue later**; Binance leg is **USDⓈ-M Futures testnet** (`testnet.binancefuture.com` — *not* the spot `testnet.binance.vision`, a correction the research caught).

| Pillar | New ADRs | Plans (provisional) | Accepts existing ADR | Notes |
|--------|----------|---------------------|----------------------|-------|
| **1 · Forecasting** | **0040** (written — model library/determinism/versioning) | **0036** foundation (written, approved) → **0037** UI (written, approved) | 0030 | sklearn `HistGradientBoostingClassifier` (no extra native dep); deep-learning + LightGBM/XGBoost deferred. |
| **2 · Advisor** | — | **0038** advisor layer (written, approved) → **0039** advisor UI (written, approved); + companion `advisor`-skill creation | 0029 | Consumes forecasts + Plan 0026 live-signal eval. Reconciles the `defi-analyst` "rebalance suggestion" charter tension (ADR-0037 note). |
| **3 · Polymarket data** | **0041** (read-only odds source) | **0040** odds adapter | — | Auth-free reads (Gamma + CLOB; price = implied probability). Disjoint + key-free → can run parallel with Pillar 1. **Target the new `Polymarket/py-sdk`; `py-clob-client` is archived.** |
| **4 · Portfolio** | **0042** (cross-venue aggregation + cost-basis) | **0041** aggregation → **0042** DeFi risk/forecast → **0043** UI | 0037 | Waits on DeFi Plans 0034+0035 and a Binance *read* key. Risk engine implements ADR-0037 (scenarios + conditional liquidation/IL prob). |
| **5 · Execution (testnet)** | **0043** (ExecutionVenue Protocol + order state machine), **0044** (segregated trade-secret store) | **0044** `execution/` skeleton + new `trader` skill → **0045** Binance USDⓈ-M testnet adapter → **0046** pending-queue + confirm UX | 0025 | Heaviest, last. All six ADR-0025 invariants are hard gates. Polymarket *trading* is a deferred second venue adapter behind its own later ADR. |

Sequencing: **1 → 2** is the critical path (user priority). **Pillar 3 (Polymarket data)** is file-disjoint + key-free and may run in parallel (own worktree). **Pillar 4** is gated on the in-flight DeFi plans. **Pillar 5** is last. The two research corrections above (Binance futures-testnet host; archived `py-clob-client` → `py-sdk`) are pinned into Pillars 5 and 3 respectively so the implementers don't re-derive them from stale memory.

**Skill ecosystem (decided 2026-06-05):** the program adds **exactly two new skills**, both at trust-boundary crossings — **`advisor`** (Pillar 2, mandated by [ADR-0029](../adrs/0029-advisory-recommendation-boundary.md); crosses the recommend line) and **`trader`/`execution`** (Pillar 5, mandated by [ADR-0025](../adrs/0025-trade-execution-feasibility.md) invariant 3; holds the only write path + trade-secret). Both are created via `skill-creator` as companion steps, not plan phases. **No new implementer skills** — all build work routes to existing `dev`/`backtester`/`ui-builder`. **Forecasting (Pillar 1)** and **Polymarket odds (Pillar 3)** stay read-only and **extend existing analyst surfaces** (a forecast is a calibrated *condition*, not a call; odds are a *signal*) rather than adding a skill. **Pillar 4 (portfolio) is tools-only — no operator skill:** the cross-venue aggregation ships as agent-callable tools, with `market-analyst` and `defi-analyst` each reporting their own slice and the agent assembling the unified view, preserving the TradFi/DeFi hard split rather than relaxing it.

Note for any future `ui-builder` plan: Plan 0033 rewrote the shared `styles.css` token system and established `lib/theme.ts` as the single source of truth for theme state. A plan that touches the chart, `App.tsx`, or the token file is inherently serial with other in-flight `ui-builder` work regardless of worktrees, and should read its colors from the theme tokens rather than reintroducing hardcoded hex.

The architect close ceremony serializes plan completion regardless: data-adapter plans (`data/adapters/`, `mcp_tools/`) and renderer plans (`App.tsx`/`client.ts`) shouldn't run in parallel against the same files. Plan history (the per-plan Gantt of who shipped what when) lives in `git log` and each plan's `done/` file.

## Parallel execution

Multiple `approved` plans can be implemented at once by giving **each implementer its own git worktree** (one branch off `main` per plan), so no two agents share a working tree. The default is still one session per plan; this is the opt-in pattern when you want throughput across plans the roster already marks disjoint. Worktrees isolate *files* — but three collisions survive file isolation in this repo and must be handled explicitly:

1. **Shared runtime (port + DB + lockfile + agent-mode file).** All resolve to one per-user app-data dir, and the sidecar refuses to start if a lockfile is already present (ADR-0016). Any done-when check that launches the **real sidecar process** (Playwright/Electron e2e, manual smoke, `pnpm dev`) collides across worktrees. Fix — set both per worktree before running such checks: `MARKET_ANALYSER_DATA_DIR=<unique temp dir>` (isolates DB + lockfile + agent-mode file in one move; taken verbatim per `config.py`) and run the sidecar with `--port=0` (OS-picked port, prints `PORT=<n>`). In-process `TestClient` unit tests bind no port and use temp DBs, so they parallelize without this.
2. **Alembic migrations are one linear chain.** `migrations/versions/` is a single sequence; two plans that each add a migration both branch off the same head → two heads at merge. There is no runtime fix — **do not parallelize two plans that touch `persistence/migrations/`**, or serialize just the migration-adding phase. Confirm the chosen set is migration-free before fanning out.
3. **Index + numbering races.** This README, `adrs/README.md`, and the next-free `NNNN` are shared append-points. **Assign all plan/ADR numbers up front** (architect, before fan-out) and expect trivial merge conflicts on the index files — resolve them at the close ceremony, or let one session own index writes.

**Workflow:**
1. **Architect, up front:** pick a disjoint set from the roster (the "largely disjoint files" sets the execution-order section already curates), confirm no two touch `persistence/migrations/`, assign any new numbers now.
2. **Per plan:** one worktree + branch; export a unique `MARKET_ANALYSER_DATA_DIR`; run sidecar/e2e on `--port=0`. The agent runs the plan's contiguous phases, committing per phase on its branch. **Never push.**
3. **Merge back through the close ceremony** — which is already serialized (architect reviews one plan at a time, flips status, moves to `done/`). That is the natural merge gate; do the branch merges one at a time there.

A **mixed-owner plan** (e.g. 0026 `backtester → dev → ui-builder`) is a *sequential* skill chain *within one worktree* — that is the cross-skill handoff protocol, not parallelism. Parallelism is **across plans** (worktree per plan), never across phases of one plan.

## Status vocabulary

| Status | Meaning |
|--------|---------|
| `draft` | Author wrote it; no user "go" yet. Implementers ignore. |
| `approved` | User signed off. Implementers may pick up. |
| `in-progress` | An implementing skill flipped it at Step 2 of its session. |
| `implementation complete — pending …` | All phases shipped; close blocked on a named followup or review delta. |
| `done` | Close ceremony fired; file lives in `done/`. |
| `abandoned` | Killed before completion. Stays here for the record. |
| `superseded by NNNN` | A later plan replaced this one. |

Only `architect` and the implementing skill (at Step 2) may mutate `Status:`. Implementers flip `draft → in-progress`; architect handles every other transition.

## Owner-skill vocabulary (per phase)

Each phase carries `**Owner skill:**` with exactly one value, backticked:

- `` `dev` `` — Python sidecar, persistence, CI, tooling, non-UI Electron.
- `` `ui-builder` `` — anything under `desktop/`.
- `` `strategy-author` `` — `src/market_analyser/strategies/`.
- `` `backtester` `` — `src/market_analyser/backtest/` and `runs/`.
- `` `human` `` — user-only task (rare).

Mixed-owner plans hand off at every boundary per the [cross-skill handoff protocol](../../../.claude/skills/architect/references/templates/cross-skill-handoff.md). Missing or ambiguous tags fail Mode 4 review as blockers.

## Conventions

- **Numbering** is sequential, zero-padded to four digits, independent for plans and ADRs. **Next free plan: 0040. Next free ADR: 0041** (see [`../adrs/`](../adrs/)). Architect runs `Glob docs/architecture/plans/*.md` and `…/adrs/*.md` before drafting — never trusting memory. **Note:** plans 0040–0046 and ADRs 0041–0044 are *provisionally reserved* by the trade/predict/portfolio program (see the program reservation below); they are intentions, not yet-written files — the pointer above is the real next-free for any unrelated work, and a dropped program plan simply leaves an unused number (never reused).
- **One plan per file.** No `0004a`/`0004b` splits — if work grows, write a new numbered plan and reference the parent.
- **Plans aren't ADRs.** A plan says *what we're building and how* (it expires); an ADR says *why we chose this over the alternatives* (it doesn't). If a plan's decision warrants permanent capture, write an ADR too.
- **Plans don't move until the close ceremony.** Implementers commit per phase but never `git mv` to `done/`. The architect reviews the whole plan, then flips status + moves the file in one commit.
- **In-progress plans are append-only on substance.** The only mid-flight edits are the `Status:` line and minor honesty fixes. Structural amendments happen via a new followup plan.
- **Cross-references stay link-shaped** so they survive renumbering and the move to `done/`.
