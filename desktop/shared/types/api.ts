/**
 * Type stub for the preload-exposed `window.api`. The concrete type is built
 * from the per-domain preload modules and re-exported as `ElectronAPI` from
 * `desktop/electron/preload/index.ts`. The renderer's global declaration
 * imports from there.
 */
export type Unsubscribe = () => void;
