# Plans

Implementation plans for `market-analyser`. One file per plan (`NNNN-<slug>.md`), authored by `architect` and implemented by the sibling skill(s) named on each phase. Closed plans live in [`done/`](done) — the architect moves them there at the close ceremony, never the implementer.

This index is the one-minute view of what's in flight. The plan file (and `git log`) is the source of truth; if a status here disagrees with a plan's own `Status:` line, trust the plan file and fix this index.

## Active roster

| #    | File | Status | Summary |
|------|------|--------|---------|
| 0108 | [0108-social-sentiment-source](0108-social-sentiment-source.md) | approved | **X/social sentiment source — `dev` ×2 + `human`, paired [ADR-0103](../adrs/0103-social-x-sentiment-source.md) (accepts at close).** From the 2026-07-15 AERO analysis (user asked for Twitter sentiment). A 5th `SentimentSource` beside news+VADER / StockTwits / Fear&Greed / Reddit, built **source-agnostically behind the seam** with the concrete provider (recommended **LunarCrush**, covers X) behind a `SecretsStore` key (ADR-0038). **Absent the key → inert / honest-empty** (ADR-0019), so the seam ships before any spend. Condition, not advice (ADR-0029); wall-clock-sensitive, no `as_of`. `dev` ×2 (1 adapter, key-gated + no-key honest-empty; 2 `social_sentiment` tool + `EXPECTED_FULL_TOOLSET` +1 + apiref) → `human` ×1 (3 live smoke, **deferred until a key is funded — does not gate close**). **Independent — runnable any time.** ADR-0103 = "design now, decide spend later"; breaks keyless-first deliberately. Migration-free, no vendor SDK (plain HTTP). NOT: raw X API v2 (documented fallback), scraping/Nitter, sentiment history store, a UI panel. |
| 0107 | [0107-defi-fundamentals](0107-defi-fundamentals.md) | approved | **DeFi token & protocol fundamentals — `dev` ×5 + `human`, paired [ADR-0102](../adrs/0102-defi-token-fundamentals-source.md) (accepts at close).** From the 2026-07-15 AERO analysis: our condition surface (price/structure + sentiment) is blind to DeFi-native fundamentals — TVL/volume/APR trend, the emissions schedule (sets LP reward APR), veAERO vote/bribe dynamics, the unlock/dilution calendar (`news_for` returns zero AERO items). A `DefiFundamentalsSource` (ADR-0031) surfaced as a `defi_fundamentals` tool, layered by cost: **keyless DefiLlama primary** (ph1–3: protocol TVL+history, DEX volume, fee/reward APR, mcap/FDV, unlocks — reuses `DefiLlamaAdapter`, no new key/dep) then a **best-effort Aerodrome-native deep tier** (ph4–5: exact emission decay + veAERO/Voter weights over the existing Base RPC). Conditions only (ADR-0029); honest-degrade (ADR-0019); wall-clock-sensitive, no `as_of`. `dev` ×5 (1 source+DefiLlama adapter; 2 registry+tool +1+apiref; 3 skill-doc consumption note; 4 Aerodrome-native reader; 5 wire deep tier) → `human` ×1 (6 live smoke on AERO). Consumed by `defi-analyst`. **Independent — runnable any time.** Migration-free. NOT: paid aggregator, non-Aerodrome deep reads, `as_of` replay, a fundamentals score/call, a UI panel. |
| 0103 | [0103-reddit-crowd-sentiment](0103-reddit-crowd-sentiment.md) | approved | **Reddit keyless crowd sentiment — `dev` ×2 + `human`, paired [ADR-0098](../adrs/0098-reddit-keyless-crowd-sentiment.md) (accepts at close).** From the 2026-07-13 upstream gap analysis: a 4th sentiment surface beside news+VADER / StockTwits / Fear&Greed, capturing the retail-crowd venue (WSB, r/CryptoCurrency) they miss. A keyless `SentimentSource` adapter over the public subreddit JSON mirroring `stocktwits.py` — keyword-lexicon scored (bullish/bearish → −1..+1 + label), upvote-weighted, on the ADR-0019 resilient path with a descriptive UA, degrading to an **honest empty** on rate-limit (never fabricates) — plus a `reddit_sentiment` MCP tool. Condition, not advice (ADR-0029). `dev` ×2 (1 adapter + provider wiring; 2 tool + `EXPECTED_FULL_TOOLSET` +1 + apiref) → `human` ×1 (3 live smoke). **Independent of the rest of the batch — runnable any time.** Migration-free, no new dependency, no chart contention. NOT: authenticated Reddit API/OAuth, VADER/ML scoring (v1 lexicon), a Reddit UI panel. |
| 0102 | [0102-crypto-sector-rotation](0102-crypto-sector-rotation.md) | approved | **Crypto sector rotation — `dev` ×3 + `human`, paired [ADR-0097](../adrs/0097-crypto-sector-taxonomy-and-baskets.md) (accepts at close).** From the 2026-07-13 upstream gap analysis (user chose crypto sectors over US ETFs): rank a self-defined set of crypto sectors (L1/L2/DeFi/memes/AI/…) by equal-weighted constituent momentum over cached bars, surfacing hot/cold sectors + per-sector leaders/laggards. Crypto has no canonical sector index, so ADR-0097 defines the taxonomy as **versioned config** (sector → liquid constituents priced through USD-native sources) with an equal-weight basket momentum + a ≥N-priced floor. `dev` ×3 (1 taxonomy config + loader; 2 `analysis/sectors.py` momentum engine; 3 `sector_rotation` tool + toolset +1 + apiref) → `human` ×1 (4 live smoke). **Builds on Plan 0100's `_scan_symbols` harness → sequence after 0100.** Migration-free, no new source (reuses fetched OHLCV), conditions-only (no action field). NOT: cap-weighted baskets, an external taxonomy, US sector ETFs, a rotation heatmap UI. |
| 0099 | [0099-defi-position-out-of-range-monitor](0099-defi-position-out-of-range-monitor.md) | approved | **DeFi LP out-of-range monitor + notification + advisory rebalance hook — cross-skill (`dev` ×3 + `ui-builder` ×1 + `human`), paired [ADR-0093](../adrs/0093-defi-position-monitor-dwell-triggered.md) + [ADR-0094](../adrs/0094-os-native-notification-transport.md) (both accept at close).** From a 2026-07-13 user report: a concentrated-liquidity LP on Base (wallet `0xae5b…9790`) drifted out of its tick range and stopped earning fees, with no way to be told. Detection is already read-only-possible today (`enrich_lp_positions` → `DefiPosition.in_range` over Base RPC via `RpcLpDetailAdapter`; surfaced by `scan_wallet`) — what's missing is a clock, dwell logic, and any passive notification. **ADR-0093:** a **dedicated in-sidecar DeFi position monitor** (a 4th lifespan asyncio loop, sibling to `defi/scan_job.py`/`defi/pnl_job.py` and to ADR-0055's `WatchScheduler` — the shipped market-alert path is **untouched**), firing a new **`defi.position_alert v1`** on a **dwell-qualified edge** (once, after *sustained* out-of-range ≥ a per-watch threshold; reset on re-entry — instantaneous-edge chatters on the boundary), with config-pinned **and** agent-created watches + MCP CRUD tools (`create/list/delete_position_watch`, `list_position_alerts`). **ADR-0094:** a new renderer→main **`notification:show` IPC channel** raising a native Electron `Notification` when the window is unfocused (the open-backlog "OS-native notification transport" item; ADR-0017 scoped it future), so a 03:00 alert reaches the user; notify-while-fully-closed stays deferred (ADR-0016 tray/supervisor). Delivery is three-way: **agent-visible** (pending-events poll + tools), **in-app** (toast + Alerts surface), **OS notification**. An **advisor rebalance-recommendation hook** (ADR-0029 — recenter/widen/exit, advice only) turns an alert into a labeled call. `dev` ×3 (1 persistence + pure dwell reducer + migration; 2 lifespan monitor + event + MCP tools; 3 advisor rebalance hook) → `ui-builder` ×1 (4 OS-notification IPC + in-app surface) → `human` ×1 (5 live smoke on the real wallet). Adds **one additive migration** (verify chain head — serialize vs other migration-bearing plans, do not worktree-parallel). Explicitly NOT: **any on-chain rebalancing or order preparation** (multi-leg/inventory-carrying → barred from the ADR-0072 BA-1 autonomous path, crosses the ADR-0025 read-only line — future ADR arc), notify-while-Electron-fully-closed, proximity/pre-warning triggers, any change to the shipped ADR-0055 path, any new runtime dependency. |
| 0046 | [0046-pending-order-confirm-ux](0046-pending-order-confirm-ux.md) | approved | **Pillar 5 (Execution) — assisted-confirm UX.** Pending-order queue (agent prepares, order sits `intended`) + explicit human Confirm/Cancel + prominent kill switch. **No order submits without confirmation** (asserted). Implements [ADR-0025](../adrs/0025-trade-execution-feasibility.md) invariant 1; its close (assisted testnet loop green) **accepts ADR-0025**. `dev` → `ui-builder` → `human` gate. |
| 0045 | [0045-binance-futures-testnet-adapter](0045-binance-futures-testnet-adapter.md) | approved | **Pillar 5 (Execution) — Binance adapter.** First `ExecutionVenue` adapter: Binance USDⓈ-M Futures **testnet** (`testnet.binancefuture.com`, not the spot host) via the **official modular SDK**; `newClientOrderId` idempotency + user-data WS (`listenKey` keep-alive) reconciliation. **Testnet only.** 3 `dev` phases (REST → WS → live smoke). |
| 0044 | [0044-execution-skeleton](0044-execution-skeleton.md) | approved | **Pillar 5 (Execution) core.** Venue-independent machinery in isolated `execution/`: `ExecutionVenue` Protocol + persisted order/position **state machine** + idempotency + reconciliation + **risk guard + kill switch** + append-only audit, proven against a stub venue. Implements [ADR-0043](../adrs/0043-execution-venue-protocol.md) + [ADR-0044](../adrs/0044-trade-secret-store.md). **Adds a migration → serialize, don't worktree-parallel.** 3 `dev` phases + `human` (create `trader` skill). |
| 0043 | [0043-portfolio-ui-surface](0043-portfolio-ui-surface.md) | approved | **Pillar 4 (Portfolio) UI.** Portfolio view (holdings + cost-basis + P&L + exposure, **per-leg as-of times**) + a DeFi risk panel (scenario-shock control → IL/HF/liquidation distance; conditional probabilities **with vol assumptions shown**). No rebalance/exit control (asserted spec). `dev` (surface) → `ui-builder` (view). |
| 0042 | [0042-defi-position-risk-forecast](0042-defi-position-risk-forecast.md) | approved | **Pillar 4 (Portfolio) risk.** DeFi risk/forecast engine implementing [ADR-0037](../adrs/0037-defi-position-risk-forecast.md) (accepts at close): deterministic **scenario sensitivity** (supplied shock → IL/HF/liquidation distance) + **seeded conditional probability** (trailing-vol MC → P(liquidation in Nd), IL distribution, each stating its assumption). Conditional facts, **no market view, no exit/rebalance**. 3 `dev` phases. Prereqs: Plans 0034 + 0035. |

## Recently closed

Full close-review notes live in each plan file under `done/`. Most-recent first. The human live smokes these plans deferred at close were run in one pass and recorded in [`../consolidated-smoke.md`](../consolidated-smoke.md) (2026-07-12, all green).

| #    | Closed | Summary |
|------|--------|---------|
| 0110 | 2026-07-15 | **Watch management UX — no new ADR (extends [ADR-0055](../adrs/0055-watchlist-alerting.md)'s "agent creates, viewer manages" grain).** From live use of the Alerts view (managing 10 ETH + 4 AERO watches): rows didn't show *which* condition, `created_at`, offered no delete, and had nowhere to note *why* a watch exists. Three phases in the **`plan-0110` worktree** (first branch-implemented plan in a while): `5d8f3b3` ph1 (`dev` — additive `note` migration `0010_watch_note`, `Watch.note`/`NOTE_MAX_LENGTH=500`, repo `create(note=)`+`set_note`, MCP `create_watch` optional `note`, `WatchOut.note`, `POST /watches/{id}` widened to partial `WatchUpdateRequest{enabled?, note?}` — `model_fields_set` distinguishes `note:null` clear from absent-untouched so an enable/disable never wipes a note, null-enabled/empty-body 422 — and a new `DELETE /watches/{id}` reusing the alert-history cascade) → `5df002a` ph2 (`dev` — apiref + renderer-type regen) → `615dee8` ph3 (`ui-builder` — pure `formatWatchCondition(kind, params)` helper (`close ≤ 1831.62` / localized pattern / strategy id, slug fallback), condition + `created_at` render, muted note distinct from the condition fact with inline pencil-edit, delete-with-confirm cascading local history/live-row state, history note echo via render-time `watch_id` join, en+ru). **Clean Mode 4 — no blockers/majors/minors.** ADR-0029 fact/context line held (note never interpolated into the condition string, never in the `alert.triggered v1` payload — render-time join costs zero schema). Verified at assertion level (73 watch Python + 25 renderer tests read, all genuine — partial-update-never-wipes-note, delete-cascade-by-row-count 3→0, over-length-rejected-before-write, condition/timestamp on rendered text, note-clear-to-null, declined-confirm no-op, deleted-watch fallback, ru spot-check). **Merged to `main` via `--no-ff` (`e776049`).** The branch was cut before Plan 0109 closed, so its ph2 apiref snapshot described the pre-fold tool surface — the two generated-doc conflicts (`docs/reference/README.md`, `mcp-tools.md`) were resolved by **regenerating** `docs/reference/` from the merged source (main's folded volume tools + the watch note), `apiref --check` green; main's volume-fold + 0.16.1 bump preserved through the merge (branch never touched those files). Post-merge tree green: 2647 Python + 1117 renderer tests pass (one pre-existing ts-jest ambient-augmentation flake in Plan-0098 chart code, passes in isolation, orthogonal). i18n en+ru (ADR-0063), apiref regen (ADR-0064). Worktree + branch pruned at close. **Phase 4 (`human` live smoke on the real ETH/AERO watches) outstanding — deferrable, does not gate close.** |
| 0109 | 2026-07-15 | **MCP tool-surface consolidation — [ADR-0104](../adrs/0104-mcp-tool-surface-granularity.md) accepted at close.** Executes ADR-0104's "one tool per verb; modes are parameters" granularity rule: folds five same-verb clusters (18 retired tools) into five discriminated tools, MCP surface **62 → 50**. Implemented directly in `main` (no branch): `9ac96c4` ph1 `scan_watchlist(rank_by)` (squeeze/gainers/losers/momentum/quality/volume_breakout/smart_volume — gainers & losers split into two mirror modes) → `c55d3d9` architect mid-implementation amendment (FastMCP wraps union *return* annotations in `{result}`, so a purpose-built `{kind, result}` envelope for genuinely-disjoint clusters) → `9339093` ph2 `forecast(kind)` (`ForecastResponse{kind, result}`, direction/volatility/regime) → `9f1eb0d` ph3 `sentiment(source)` (dict return + injectable source registry, no envelope) → `37b1f6b` ph4 `price_structure(kind)` → `f4d44ee` ph5 `volume_read(kind)` → `5340d47` ph6 ledger + apiref + skill-doc sync. **Surface refactor only — no compute/behavior change; internal consumers (`recommend`, advisor) call the pure `analysis/`+`forecast/` cores, never the retired tools.** **Clean Mode 4 — no blockers/majors.** Verified at assertion level (788 api/apiref tests green): `forecast(kind="direction")` `result` byte-identical (`MultiHorizonForecastResult` round-trip) and each kind publishes its *own* SSE event with payload unchanged (`forecast.completed`/`volatility_forecast.completed`/`regime_forecast.completed` — the envelope is agent-facing only, events carry the raw domain model); price_structure/volume_read anti-lookahead held under truncation (`as_of` trailing) and counter-trend anchored to the snapshot trend; `EXPECTED_FULL_TOOLSET` = 50 enforced against the live-wired server; `apiref --check` clean; no retired tool name in `.claude/skills`. **One ratified refinement, reconciled into the plan + ADR at close:** ph4/ph5 shipped the *flattened* `{kind, result, partial_reason, scanned_at}` shape (the mode-union in the `result` field, `kind` added) rather than the amendment's literal `{kind, result:<whole old Response>}` double-nest — because those retired reads *already* shared a `{result, partial_reason, scanned_at}` wrapper, making them the same shared-wrapper case as ph1 `scan_watchlist`, not disjoint clusters. Accepted as strictly better (`result`/`partial_reason`/`scanned_at` stay byte-identical, no pointless `result.result`); `forecast` is the sole cluster that actually needed the envelope. Reroutes queued **0103/0108** (sentiment→`source` mode) + **0042** (collapse plural risk tools→one `defi_risk(kind)`). Migration-free. Implemented in this working tree — no branch/worktree to merge or prune. Version bumped 0.16.0→0.16.1 (PATCH — the phase commits are all `refactor`, no `feat`; a pure surface refactor, no computation change). **Phase 7 (`human` live MCP smoke) outstanding — deferrable, does not gate close.** |
| 0101 | 2026-07-15 | **Composite quality/momentum screening rank — [ADR-0096](../adrs/0096-screening-quality-rank-conditions-side.md) accepted at close.** The first analytic built on Plan 0100's `_scan_symbols` harness, implemented directly in `main` (no branch): `e6816e4` ph1 (`dev` — `analysis/quality.py`, a pure/trailing/deterministic composite scorer mapping the `condition_snapshot` primitives to a normalized 0..100 score decomposed into four named factor contributions (trend/momentum/volume/volatility) that **sum to the score**, plus a per-asset-class liquidity gate that flags + caps thin names below a crypto/equity notional floor; each factor normalized against the symbol's own trailing distribution so crypto and equity rank comparably) → `0f96e47` ph2 (`dev` — `quality_rank` MCP tool ranking a caller watchlist by score descending over the harness, `EXPECTED_FULL_TOOLSET` +1, apiref regenerated). **Clean Mode 4 — no blockers/majors.** Verified at assertion level (27 tests green): the contribution-sum invariant holds liquid **and** through the liquidity cap (contributions rescaled, still summing to score); monotonicity (a better trend/momentum sub-score raises that contribution and the composite); the gate flags + caps a thin fixture and an uptrend outranks a downtrend; no-lookahead via a genuine field-by-field `as_of=t`-vs-pre-truncated comparison with an `at_end != as_of_k` guard so the equality isn't vacuous; and **no call-shaped field** — asserted absent on the model, rejected by `extra="forbid"`, and grep-guarded on serialized JSON (`buy`/`sell`/`grade`/`should`/…) at both scorer and tool level. Reuses `condition_snapshot` (ADR-0023 — no drift from `analyze_symbol`) + the ADR-0095 harness; conditions-only (ADR-0029), no advisor integration in scope. **Two non-blocking nits:** partial-factor neutral-0.5 fallback (short history → volume/volatility pinned neutral) is undisclosed; the liquidity floors are daily-bar-calibrated so finer timeframes flag more names (documented in-code as honest conservatism). Approved **Plan 0109** will later fold `quality_rank` into `scan_watchlist(rank_by=…)` — a downstream consolidation, not a reason to hold this close. Implemented in this working tree — no worktree/branch to merge or prune. Version bumped 0.15.0→0.16.0. **Phase 3 (`human` live smoke) outstanding — deferrable like the sibling read-only scanner smokes; does not gate close.** |
| 0100 | 2026-07-15 | **Watchlist condition scanners — [ADR-0095](../adrs/0095-watchlist-scan-fanout-harness.md) accepted at close.** The upstream-parity batch foundation, implemented directly in `main` (no branch): `3a5c784` ph1 (`dev` — the shared **`_scan_symbols` fan-out harness** in `analysis/scanners.py` owning cap + per-symbol cached read + `as_of` anti-lookahead + skip-into-`skipped` + deterministic sort, parameterised by a pure scorer, plus `squeeze_scan` as the walking skeleton over the ADR-0083 trio) → `042ddb6` ph2 (`gainers_losers`, trailing close-to-close % change split by direction) → `4ed554f` ph3 (`momentum_scan`, RSI-band + optional trend, no volume gate — the un-gated complement to `smart_volume`) → `c3c7050` ph4 (`volume_breakout` + `smart_volume` refactored onto the harness, behaviour-preserving). **Clean Mode 4 — no blockers/majors.** Verified at assertion level: the three new `_..._scan_response` suites pin real ranking orders (squeeze tightest-first by `bb_width_pct90`, gainers biggest-move-first, momentum RSI-desc), the skip-vs-dropped split (short-history/missing → `skipped`; filtered-out → dropped), sign + divide-by-zero + boundary-inclusivity, and per-scanner truncation-invariance / no-lookahead as genuine field-by-field `as_of=t`-vs-pre-truncated comparisons — not stubs. Phase-4 refactor touched only the two tool modules (zero test edits); both shipped volume suites pass unchanged and both tools now delegate to `_scan_symbols` with `MAX_SCAN_SYMBOLS` re-exported. ADR-0083 trio + ADR-0023 snapshot reused via `condition_snapshot` (no drift from `analyze_symbol`); conditions-only (ADR-0029) — no call-shaped field on any match model. 41 scanner + tool-count tests green; `EXPECTED_FULL_TOOLSET` +3; apiref regenerated per phase. **Two documented non-blocking nits:** `gainers_losers` measures a single-period (last-two-bar) move, not the whole fetched window; flat 0.0% reads `direction:"up"` — both internally consistent across code/tests/description. **Unblocks Plans 0101 + 0102**, which build on the harness. Implemented in this working tree — no worktree/branch to merge or prune. Version bumped 0.14.0→0.15.0. **Phase 5 (`human` live smoke) outstanding — deferrable like the other read-only smokes; does not gate close.** |
| 0098 | 2026-07-15 | **CandlestickChart god-component refactor — [ADR-0092](../adrs/0092-chart-controller-imperative-core.md) accepted at close.** The last chart-file plan, run over the settled surface exactly as designed (0092/0096/0097/0104/0105/0106 all closed first). Renderer-only, no branch, no wire/event/schema/CSP change: `df47417` ph1 (`ui-builder` — `ChartController` scaffold + lifecycle: chart creation, main + always-on series, `PaneRegistry` + OBV pane, 5 primitive attaches, `setBars`/`setCandleType`/`dispose`, `__test_chart_render__` mirror; the 158-line creation effect + ~17 refs collapse to one `controllerRef`) → `cfa4d3c` ph2 (overlay + oscillator-pane reconcile folded in; the divergence→oscillator `requiredKinds` coupling made structural via a pure `requiredOscillatorKindsFor` in `divergences.ts`) → `39281f5` ph3 (primitives/restyle/axis/forming-bar folded in) → `732d996` ph4 (thin the surface + `legendRouting.ts` extraction; component drops all lightweight-charts imports) → `a18ec1d` ph5 (headless `controller.test.ts` suite) → `f06b95d` thin-A (fold the 4 residual reconcilers — OBV/structure-levels/anchored-VWAP/market-structure — into the controller) → `abb4f6a` thin-B (extract `useLayersControl`) → `af59d9c` thin-C (extract `useChartSync`). **Clean Mode 4 — no blockers/majors.** Verified at assertion level: `controller.test.ts` pins series/primitive counts (5 primitives on the main series), the lazy-prepend viewport-anchor math (`{from:8,to:18}`), StrictMode-safe mount→dispose→mount (second `createChart` + first `remove`), overlay reconcile add/reuse/remove without remount, oscillator pane create/reuse/teardown, restyle-in-place (chart identity stable), and dispose nulling every ref + clearing the reconciler maps — genuine, not stubs. `CandlestickChart.tsx` imports **no** `IChartApi`/`ISeriesApi`/`lightweight-charts` type; holds one `controllerRef` + `useLayersControl` + `useChartSync`; `ChartController` is a pure-delegating facade. 15 orchestration hooks deleted with no dangling refs (typecheck across 5 tsconfigs + 1100 renderer jest / 106 suites + lint all green). **Two non-blocking minors:** (1) `CandlestickChart.tsx` at 457 lines and (2) `controller.ts` at 413 lines both exceed the plan's ~250-line target/smell threshold, but both are inflated by docstrings + prop-interface comments + JSX + read-getters, not accumulated logic — the coordination-surface goal (one `controllerRef`, no LWC imports, facade delegates) is met. **Scope expanded beyond the 6 named phases (ratified — improves on plan):** thin-A/B/C folded 4 additional residual reconcilers into the controller and extracted two orchestration hooks, completing the plan's spirit ("all imperative wiring in the controller; component is a thin adapter") more fully than the literal phase list. Renderer-internal (the `divergences.ts` change is a pure helper, not a wire change). Implemented directly in this working tree — no branch/worktree to merge or prune. Version bumped 0.13.0→0.14.0. **Phase 6 (`human` visual smoke) PASS 2026-07-15** (user-attested — the refactor is pixel-equivalent on a live chart; plan fully complete). **The chart-file plan chain is now fully closed** — no chart-file plan remains in flight. |
| 0104 | 2026-07-15 | **Drawing read-back + position & range tools — [ADR-0099](../adrs/0099-user-drawing-readback-and-advisory-positions.md) accepted at close.** Closes the two gaps Plan 0097 left open (2026-07-14 user ask: draw a resistance the agent reads over MCP; agent draws long/short setups back). Cross-skill on `main`, no branch, migration-free: `7bad8ef` ph1 (`dev` — five `DrawingSpec` kinds `long_position`/`short_position`/`date_range`/`price_range`/`date_price_range`, per-kind position ordering invariant (long `stop<entry<target`, short `target<entry<stop`) validated, `rationale`/`basis` fields, `annotate_chart` structurally rejects an agent-placed position without non-empty rationale+basis — ADR-0029 to geometry, TS regenerated) → `c2419cd` ph2 (`dev` — in-memory per-symbol `UserDrawingsMirror`, renderer-bearer `PUT /user_drawings/{symbol}` with `provenance=="user"` 422 guard, `get_chart_drawings` MCP tool with honest `synced_at` (`null` = never-synced ≠ empty), `ui.drawing_changed v1` **ungated** on the ADR-0021 buffer per ADR-0101, `EXPECTED_FULL_TOOLSET` +1, apiref regenerated) → `79adbe0` ph3 (`ui-builder` — five dock tools with placement/render/full-edit reusing the 0097 tool-mode + hit-test/edit engine, `lib/positions.ts` single-home ordering clamp + derived R:R, range readouts, en/ru) → `ca8588b` ph4 (`ui-builder` — `lib/drawingsSync.ts` PUT-on-mutation+load with one retry + one `ui.drawing_changed` POST per mutation through the typed client, agent advisory position render hide-only with advisory label + rationale-on-hover, en/ru). Clean **Mode 4 — no blockers/majors/minors**; all owner tags in-vocabulary. Assertion-level audit confirmed genuine tests (22 Python re-run green at close): PUT-then-tool round-trips all eleven kinds, a second PUT replaces (not appends), `provenance:"agent"` 422 on the mirror, the advisory guard both branches, `synced_at` null-before / stamped-after, `ui.drawing_changed` buffers with no mode precondition, and the **ADR-0014 dual-bearer split both directions** (MCP bearer can't PUT, renderer bearer can't call the tool). **Two deliberate phase-3 deviations from human smoke, ratified — both improve on the plan:** (1) position/range anchors place at the raw cursor price (`isFreePriceKind`), not OHLC-snapped as ph3 worded (`959983e`) — a trade idea's levels are arbitrary, not bar prices; (2) anchors extrapolate past the last loaded bar rather than clamping (`2bc9e71`), so a position/range projects forward. Time still snaps to a known bar over the loaded range in both cases. Additive wire only (one route, one tool, one ui-event type, five kinds), mirror ephemeral, no CSP/determinism change. Implemented directly in this working tree — no branch/worktree to merge or prune. **Phase 5 (`human` live smoke) PASS 2026-07-15** (user-attested): five kinds draw/edit/persist; drawn-line read-back grounded an agent answer (visibly called `get_chart_drawings`); draw fired an event with no setup step; an advisor call rendered a hide-only advisory position box with rationale on hover; a rationale-less agent position was rejected typed; sidecar restart honestly reported never-synced until reload; en/ru render; full gates green. Version bumped 0.12.0→0.13.0. **Followup flagged for architect (roadmap, not urgent):** select-range (0106, ephemeral+push via `ui.range_selected`) now overlaps the new `date_range` tool (persistent+pull via `get_chart_drawings`) — decide retire / relabel / keep-as-is; touches the ADR-0021 wire contract, so an ADR call. |
| 0097 | 2026-07-14 | **Chart drawing dock — [ADR-0091](../adrs/0091-chart-annotation-layer.md) accepted at close.** The left-edge drawing tools deferred out of Plan 0096, cross-skill (`dev` ×1 + `ui-builder` ×3 + `human`) on `main`: `c83e0bf` ph1 (one sidecar `DrawingSpec` + `chart.annotations v1` event + `annotate_chart` MCP tool, TS-generated, `EXPECTED_FULL_TOOLSET` +1, apiref regenerated) → `8b47f20` ph2 (left tool-rail + trendline/ray + per-symbol `(time,price)`-anchored `ma.userDrawings` persistence + full edit engine: select via hit-test, drag-endpoint, delete, snap-to-OHLC — walking skeleton) → `56dc92a` ph3 (hline/vline/rect-zone/fib) → `81fcd0d` ph4 (agent-source render off the SSE stream + provenance merge — user drawings editable, agent drawings hide-only, identical agent+user pairs collapse, agent set never persisted). **Two-source annotation layer, one `DrawingSpec`, one render/merge path** (ADR-0091): user drawings renderer-owned/local like ADR-0077; agent drawings on the wire because agent annotation is analysis (ADR-0015). Reuses the 0049/0061 primitive on the 0088 v5 substrate + the 0059 logical-coordinate handling. **Clean Mode 4 — no blockers/majors.** Two deviations that *improve* on the plan's file list: (1) a dedicated `lib/drawings.ts` `DrawingPrimitive` reusing the ADR-0059 pure helpers from `trendlines.ts` rather than coupling into it — cleaner layer separation; (2) a single segment-distance hit-test path — every kind decomposes to line segments (rect→4 edges, fib→6 lines), so no per-kind distance functions were needed, shrinking the regression surface the ADR flagged as the main risk. Verified at assertion level: phase-1 tests defend one valid + one malformed spec **per kind** plus provenance/dup-id rejection, empty-set clear, and a live MCP round-trip; edit-gating is provenance-scoped; i18n parity holds (10 `chart.draw.*` keys in both en + ru). Additive wire only, no CSP/determinism change. Implemented directly on `main` (no branch/worktree). Version bumped 0.11.0→0.12.0. **Phase 5 (`human` live smoke) PASS 2026-07-15** (user-attested) — six kinds end-to-end, agent-hide-only vs user-editable in the running app, en/ru render, and the green renderer-jest + `test:main` + `gen-types:check` + `apiref --check` + no-CSP-diff all confirmed. **Plan fully complete.** |
| 0106 | 2026-07-14 | **Remove agent mode (always-on gesture forwarding) — [ADR-0101](../adrs/0101-remove-agent-mode-gate.md) accepted at close** (amends [ADR-0021](../adrs/0021-renderer-to-agent-feedback.md)'s gate — the buffer/`get_pending_ui_events`/resource transport stands — and [ADR-0099](../adrs/0099-user-drawing-readback-and-advisory-positions.md)'s push-gate clause; both files carry the amendment marker). Two impl phases on `main`, no branch, migration-free: `63638ef` ph1 (`dev` — `AgentModeStore` + `GET`/`PUT /agent_mode` deleted, the 403 gate off `POST /ui_events` with renderer-bearer auth intact and asserted, `ui.agent_mode_toggled v1` out of the vocabulary (422 + nothing buffered, asserted), best-effort startup delete of a leftover `agent_mode.json` asserted incl. the undeletable case, `get_pending_ui_events` framing updated, CORS preflight test retargeted to the POST route, apiref regenerated — two routes gone) → `7d6f8bb` ph2 (`ui-builder` — `AgentModeToggle` + `useAgentMode` deleted with tests, `useChartGestures` loses the `agentMode` param (symbol/timeframe the only remaining guard, asserted via mocked uiEvents), prop un-threaded from `CandlestickChart`/`ChartToolbar`/`OhlcvView` + `DefiPnlView` dead branch dropped, typed client loses `getAgentMode`/`setAgentMode`, en + ru shed the same seven keys parity-preserved, e2e adapted — range-select needs no setup step, the toggle testid asserted gone, pan-drag buffers nothing). Clean **Mode 4 — no blockers/majors**; two sanctioned-residue minors: stale gate references in the `ui_events/buffer.py` docstring + an `alerts/scheduler.py` comment (both files pinned untouched by the plan; a one-line comment sweep for the next `dev` touch of either file). Grep-verified at close: zero live `agent_mode` in `src/` beyond the sanctioned startup cleanup (`api/__main__.py`), zero `agentMode`/`agent_mode` identifiers in `desktop/`. Select-range mode + single-instance intact as ADR-0101 decided; `EXPECTED_FULL_TOOLSET` unchanged; no CSP change. Gates: pytest 2559 + `mypy --strict` + ruff + `apiref --check`; typecheck (5 tsconfigs) + lint + jest 997 + test:main 58 + `gen-types:check`. **Phase 3 human smoke PASS 2026-07-14** (no toggle anywhere in the UI; bar click + range drag surface their envelopes via `get_pending_ui_events` with no setup; alert path intact; `GET /agent_mode` 404s; en/ru clean). Implemented directly in this working tree — no branch/worktree to merge or prune. Version bumped 0.10.0→0.11.0. **Unblocks Plan 0097** — the drawing dock lands on the simplified post-removal gesture machine; the re-gating decision owed by any future remote-surface topology (ADR-0073 tunnel arc) is recorded in ADR-0101's consequences. (Removing the roster row also de-glued the 0106/0104 line-glue left by the 2026-07-14 shared-index collision.) |
| 0105 | 2026-07-14 | **Chart legibility — [ADR-0100](../adrs/0100-on-chart-legibility-labels-and-primitive-hover.md) accepted at close.** Renderer-only legibility follow-up to Plan 0096 (its phase-6 AAPL-1d smoke surfaced seven legibility gaps distinct from clutter). All impl phases on `main`, no branch, migration-free, no wire/schema/CSP change: `b925a44` ph1 (`market-analyst` — glossary prose for `ichimoku`/`obv`/`rsi`/`structure` + the 9 classical chart-pattern names under a new `chart_pattern` category) → `b73d026` ph2 (wire the inert legend rows to the glossary + a completeness gate that fails if any legend-emitted `glossaryKey` has no entry) → `f2a798b` ph3 (OBV pane reconciled like the oscillator panes — lazy create/remove, kept first sub-pane, divergence primitive re-attached, pane ensured when an OBV divergence needs it; no empty ~30px pane when off / Clean chart born with no OBV pane) → `253dbac` ph4 (a drawn label naming every managed sub-pane via a pane-attached primitive, reusing the indicator's legend name) → `71e02af` ph5 (Fibonacci per-level colours + drawn 0/1 swing anchors + anchoring-leg disclosure; `FibGrid.anchors` exposed, level-price math untouched) → `350a2cd` ph6 (pivot per-level colours + nearest-level-by-Y hover reusing the crosshair path) → `d27ac56` ph7 (HH/HL/LH/LL + BOS/CHoCH marker tooltips via the same time-keyed lookup candlestick markers use). Clean **Mode 4 — no blockers/majors**; two non-blocking minors: (1) ph5's done-when names a `fibonacci.test.ts` that does not exist — the fib/anchor coverage lives in `structureOverlays.test.ts` (a doc-accuracy nit; coverage is real); (2) ph3's promised jest spec pins the `PaneRegistry` ensure/remove/slot-0 mechanism (`panes.test.ts`) but the `useObvPane` reconcile *decision* (`wanted = visible ‖ divergenceNeedsPane`, the divergence-only branch) has no direct unit test — mitigated by following the established untested-hook pattern (`useOscillatorPanes` is likewise hook-untested), correct-by-inspection, and a live-confirmed reclaim. Assertion-level audit confirmed genuine tests: fib anchors pinned by price+ts + endpoint titles; the JS completeness gate builds the FULL renderable vocabulary and asserts every emitted key resolves (with a lower-bound sanity guard so it can't pass on an empty set); Python `test_glossary_accuracy.py` bidirectionally pins `chart_pattern` keys == `CHART_PATTERNS` and the four summary keys; `nearestLevelAtY` pins nearest-within-threshold / none-beyond / off-scale-skip; `structureTooltipText` pins glossary-backed content + ru fallback + degrade-to-bare-label; `paneLabelFor` pins every oscillator kind's short name + the primitive draws its text. Gates re-verified at close: 7 Python glossary + 96 renderer jest across the touched suites green. Implemented directly in this working tree — no branch/worktree to merge or prune. **Phase 8 (`human` live-repro of the finding-6 zoom-clip bleed) run 2026-07-14 — the bleed did NOT reproduce** (`a91c13d`), so phase 9 (the clip fix) has no target and stays dormant; the OBV pane reclaim was confirmed working live. The pass surfaced three *behavioral* findings outside this plan's legibility scope, left as routed-to-architect followups: candlestick-scan vs trendline-recompute asymmetry, Yahoo-sourced history-paging dead-end (the Plan 0030 range-only gap), and no re-scan on zoom-in. Version bumped 0.9.0→0.10.0. |
| 0096 | 2026-07-14 | **Chart & app declutter — [ADR-0089](../adrs/0089-chart-display-presets.md) accepted at close.** Renderer-only TradingView-style declutter from a 2026-07-13 UI-clutter review. All five `ui-builder` phases on `main`, no branch, migration-free: `3a77a30` ph1 (segmented timeframe — roving-tabindex group, `aria-pressed`, `<select>` retired) → `8b243b2` ph2 (inline `ChartLegend` retiring the LAYERS checklist; ADR-0077 provenance split preserved — remove only on user rows) → `e9019d1` ph3 (chart presets + persisted `ma.layerVisibility`; the load-bearing bit — layer visibility survives a remount, default Clean, `candleType` untouched by presets) → `5ec2aa0` ph4 (collapsible `ChartSidePanel`, `ma.rightPanelCollapsed` defaults collapsed, left-rail gutter reserved) → `6420c4b`+`d2a13d6` ph5 (grouped `NavMenu`, backtest SSE auto-switch preserved). Phase-6 human smoke run live (AAPL 1d) → three fix-forwards: `b001ff8` (market structure a toggleable layer, off by default), `d156251` (pane-crash fix — `addPane(true)`/`preserveEmptyPane` stops the v5 `_cleanupIfPaneIsEmpty` "Invalid pane index" double-free), `90aab84` (collapsible legend panel + accent Save-confirm). Clean **Mode 4 — no blockers/majors**; two non-blocking minors ticketed: (1) `LayersPanel.tsx` is orphaned dead code (survives only as the `ChartLayer` type + shared-CSS home; its resize apparatus + `ma.layersPanelWidth` key are dead) → relocate `ChartLayer` + delete the component; (2) the pane-crash regression guard (`panes.test.ts`) is registry-level only (the jest lightweight-charts mock doesn't model `_cleanupIfPaneIsEmpty`) → faithful mock. Assertion-level audit confirmed genuine acceptance tests (real unmount/remount visibility persistence in `CandlestickChart.presets.test.tsx`, provenance-scoped remove, `addPane(true)` pin); renderer-only confirmed by diff across `3a77a30^..90aab84` (no `desktop/main/` or Python `src/` touch); i18n en+ru parity holds. Version bumped 0.8.0→0.9.0 (`v0.9.0`; the baseline `v0.8.0` tag was recreated first — the earlier history-rewrite had dropped every release tag). Six deferred legibility items → [Plan 0105](0105-chart-legibility.md). Implemented directly in this working tree — no branch/worktree to merge or prune. **Close-ceremony note:** the doc-close (`f1a2a5e`) + bump (`654f2c6`) + uv.lock sync (`aed5936`) landed during a concurrent architect session (Plan 0106/ADR-0101); the shared-index sweeps folded that session's in-progress index/README/diagram edits into the bump commits (committed, not lost — attribution mixed), and this row + the 0105/0104 roster de-glue are the forward-fix. |
| 0092 | 2026-07-13 | **Price-structure & levels: Fibonacci + market-structure trend + pivot points + anchored VWAP — [ADR-0084](../adrs/0084-market-structure-as-distinct-trend-read.md) accepted at close** (market structure is a distinct second trend read beside the ADR-0067 `trend`, never merged in); the geometry pieces ride [ADR-0023](../adrs/0023-technical-analysis-surface.md), overlays 0077/0049/0061, glossary 0060. Second of two plans from the 2026-07-12 `market-analyst` capability audit (companion to Plan 0091). Closed the price-structure gaps verified absent in `src/`: **Fibonacci retracement/extension** (`analysis/fibonacci.py`, auto-anchored to the dominant `swing_pivots` leg + explicit override), **market-structure trend** (`analysis/structure.py` — HH/HL/LH/LL labeling → `structural_trend` + BOS/CHoCH events), **classic pivot points** (floor/Camarilla/Woodie in `levels.py`), and **anchored VWAP** (`volume.py`, distinct from the existing rolling `vwap`). Six code phases on `main`, no branch, migration-free, no new dep: `dev` ph1–4 (`2c98707`→`3ac91e8`: the four pure/trailing primitives, then snapshot `market_structure` + `nearest_fib_level` beside the **UNTOUCHED** `trend`, four MCP tools — `fibonacci_levels`/`market_structure`/`pivot_points`/`anchored_vwap` — `EXPECTED_FULL_TOOLSET` +4, apiref regenerated) → `ui-builder` ph5–6 (`f0511ad`/`53f2dab`: fib grid + pivot lines + anchored-VWAP overlays as three new `OverlayKind`s with a TS↔pydantic literal-parity guard + client-compute mirrors; then HH/HL/LH/LL labels + BOS/CHoCH markers + a structural-trend badge beside the indicator trend, glossary + symmetric en/ru keys). Clean **Mode 4 — no blockers/majors**. Assertion-level audit confirmed the real guards: `trend` byte-identical while `structural_trend` differs (`test_trend_unchanged_and_structure_is_a_separate_read` + `test_structural_trend_is_a_distinct_read` — `"range"` isn't in the `Trend` vocabulary, proving a genuinely separate classification); CHoCH-at-exact-bar (`StructureEvent`s pinned at bars 11/17); per-`k` truncation-invariance across `_UPTREND/_DOWNTREND/_RANGE/_CHOCH` and a future-bar fib re-anchor check; degenerate zero-volume/zero-range → `None` throughout (no divide-by-zero); MCP tools assert `tool == pure read`, honest `no_bars`, `as_of` trailing replay; no action field (`{action,signal,recommendation,buy,sell}` asserted absent). Gates re-verified at close: 81 Python tests (fibonacci/structure/pivots/anchored-vwap/snapshot/price-structure-tools) + `apiref --check` no-drift; renderer typecheck (5 tsconfigs) + lint + **101 jest** across the eight new/updated suites — all green. Pure/trailing (ADR-0023); additive, migration-free. Implemented directly in this working tree — no branch/worktree to merge or prune. **Phase 7 (`human` live smoke) deferred** (user-run: `analyze_symbol BTC-USD/ETH-USD 1d` structural_trend beside trend; fib auto-anchor + pivots + anchored VWAP; fib grid / pivot lines / HH-HL labels / BOS-CHoCH marker + badge on the chart with glossary tooltips). Followups: feed structure-vs-indicator agreement + fib confluence to the advisor (ADR-0029), fib/structure `create_watch` alerts, multi-timeframe structure confluence, order-blocks/FVGs (separate SMC plan). |
| 0091 | 2026-07-13 | **Momentum, divergence & money-flow analysis layer — [ADR-0090](../adrs/0090-cross-pane-divergence-delivery.md) accepted at close (the `chart.divergences v1` cross-pane delivery channel); analysis additions realize [ADR-0023](../adrs/0023-technical-analysis-surface.md), overlays follow 0077/0049, glossary 0060.** First of two plans from the 2026-07-12 `market-analyst` capability audit (companion [Plan 0092](0092-price-structure-and-levels.md) still `approved`). Closed the momentum/volume gaps verified absent in `src/`: **oscillators** (Stochastic, StochRSI, CCI, Williams %R, ROC — we had only RSI+MACD), the headline **price↔oscillator divergence detection** (regular+hidden bull/bear on rsi/macd_hist/obv/mfi — inputs long computed, never paired), and the **money-flow family** (MFI, A/D line, Chaikin Money Flow). All ten code phases on `main`, no branch, migration-free, no new dep: `dev` ph1–5 (`7347392`→`f4e004b`: the oscillator + money-flow indicators, snapshot `indicators` values, the pure `analysis/divergence.py` core, `recent_divergences` + the `detect_divergences` tool + apiref) → `ui-builder` ph6–7 (`6c70593`/`4ced129`: oscillator + money-flow **real v5 panes** on Plan 0095's `lib/panes.ts`, with LayersPanel toggles + `1e-6` client-compute parity + truncation-invariance mirrors) → `dev` ph8 (`c60c197`: the `chart.divergences v1` channel — `ChartDivergencesPayloadV1` in `TYPE_REGISTRY`, `detect_divergences` gains an `event_bus` and publishes exactly one event on a non-empty scan, nothing on empty/`no_bars`, tool data-contract unchanged) → `ui-builder` ph9 (`af156bb`→`89ba1a9`→`8e8c9ea`: reducer + Zod `.strict()` plumbing + the two-pane render — price-pivot segment on pane 0, oscillator-pivot segment on that oscillator's own pane, ensure-or-create). Clean **Mode 4 — no blockers/majors**, two minor items (below). Assertion-level audit confirmed the real guards: publish-on-nonempty / no-publish-on-empty-or-`no_bars` (`test_detect_divergences_tool.py`), the reducer's active-chart gate + clear-on-symbol/timeframe-switch (`chartHandlers.test.ts`), a genuine TS↔pydantic parity guard that dumps live pydantic schemas via subprocess (`types/events.test.ts`), and malformed-payload Zod-drop + `console.warn` (`useEventStream.test.tsx`). Gates re-verified at close: `ruff` + `mypy --strict` (255 files) + 72 divergence/event Python tests + `apiref --check` no-drift; renderer typecheck (5 tsconfigs) + lint + **903 jest / 90 suites** + `gen-types:check` no-drift — all green. **Sanctioned deviation (ratified):** ph9 expanded under Option A (user-authorized, commit-flagged) to build the **RSI + MACD-histogram oscillator panes** — pre-existing log-and-skip `OverlayKind`s with no pane (ph6–7 shipped the other eight), so divergences on the two commonest oscillators had no pane for the second segment; ph9 promoted them to first-class panes with Python-parity + truncation-invariance mirror tests, exceeding the plan's original ph9 Files-touched. **Minor 1 (test gap, not a defect):** no dedicated Python assertion pins `TYPE_REGISTRY["chart.divergences"] is …PayloadV1` / Python-side `VERSION==1` (holds in code; covered indirectly + on the TS parity side) — a cheap future dev add. **Minor 2:** the ratified pane expansion. Renderer→Node isolation + data→api layering intact; largest new file `lib/divergences.ts` at 352 lines; `scanned_at` run-provenance only, geometry path pure. Implemented directly in this working tree — no branch/worktree to merge or prune. **Phase 10 (`human` live smoke) PASS 2026-07-13** (architect MCP + user visual confirm): BTC/ETH `analyze_symbol` carry all nine oscillator + money-flow values; BTC `recent_divergences` + `detect_divergences … obv` surfaced a regular_bearish OBV divergence (RSI/MFI scanned-none, valid); the publish landed on the active BTC-USD 1d chart and the user confirmed the two-segment render + pane toggles + glossary tooltip; nothing reads as a buy/sell call. Followups: advisor consumption of divergence/money-flow (ADR-0029), a divergence `create_watch` alert / entry strategy, multi-timeframe divergence confluence, the ADR-0059 recompute-on-load `POST /scan_divergences` route (ADR-0090's deferred durability half). |
| 0095 | 2026-07-13 | **lightweight-charts v4→v5 migration — real multi-pane substrate — [ADR-0088](../adrs/0088-lightweight-charts-v5-panes.md) accepted at close.** Behavior-preserving precursor that unblocks [Plan 0091](0091-momentum-divergence-moneyflow-layer.md) ph6–8 (v4.2.3 had no panes API — sub-panes faked with `scaleMargins` bands, unusable for the 8+ oscillator/money-flow panes 0091 needs). Two `ui-builder` phases on `main`, no branch, migration-free, no new runtime concept: `cee1be3` ph1 (exact pin `4.2.3→5.2.0` + lockfile in one cooldown-gated commit naming the version per ADR-0013; **every** series-creation site migrated to v5's unified `chart.addSeries(<Type>Series, opts, paneIndex?)`; `lib/spans.ts` + `lib/trendlines.ts` primitives migrated to the v5 `IPrimitivePaneView`/`IPrimitivePaneRenderer`/`PrimitivePaneViewZOrder` names **verified against the installed 5.2.0 typings** — no `add*Series`/`ISeriesPrimitivePaneView` remnants remain; v5's ESM-only build handled with shared jest mocks `lightweightChartsMock.ts` + `chartMockShared.ts` routing `addSeries` by series-definition identity) → `9a10ad7` ph2 (`lib/panes.ts` `PaneRegistry` — stable-id create/reuse + reindex-on-remove over `chart.addPane()`/`removePane()`/`panes()`, the seam Plan 0091 ph6 consumes; the existing OBV strip relocated from its `scaleMargins` band onto a **real pane** as the first `addPane()` consumer; volume/volume-MA/VWAP stay price-pane bands as specified — the one intentional visual delta is OBV gaining its own pane). Clean Mode 4 — **no blockers/majors/minors/nits**. Every named test read at the assertion level: `lib/panes.test.ts` proves create/reuse/consecutive-index/remove-with-reindex/sizing against a v5-modeling chart stub; `CandlestickChart.layers.test.tsx` pins the OBV layer row + `priceScaleId:'obv'` series + layer-toggle `applyOptions({visible:false/true})`. Gates re-verified at close: typecheck (5 tsconfigs) + lint + **851 renderer jest / 87 suites** + **58 test:main** + `gen-types:check` no-drift all green; **no `desktop/renderer/index.html`/main CSP change** (ADR-0008 verbatim, confirmed by diff); renderer-internal — no wire/event/`OverlaySpec`/`TrendlineSpec`/schema change. Implemented directly in this working tree — no branch/worktree to merge or prune. **Phase 3 (`human` visual smoke) reported PASS.** **Unblocks Plan 0091 ph6–8** (oscillator/money-flow/divergence panes on `lib/panes.ts`) and benefits Plan 0092's overlay/annotation phases. Forward note for 0091 ph6: the `PaneRegistry` must be held in a ref (not re-created per effect) to add/remove oscillator panes on toggle. |
| 0090 | 2026-07-13 | **Squeeze metric (Bollinger band-width percentile + Keltner TTM `squeeze_on`) + counter-trend volume decomposition anchored to the snapshot trend — [ADR-0083](../adrs/0083-squeeze-and-counter-trend-volume-semantics.md) accepted at close (records the *semantics*; `bb_width_pct90`'s percentile mechanics ride [ADR-0023](../adrs/0023-technical-analysis-surface.md), the trend anchor is [ADR-0067](../adrs/0067-ichimoku-in-trend-classification.md)).** From a `market-analyst` BTC/ETH 1d read (2026-07-12) that confirmed two symbol-independent gaps: `atr_pct90` (Feb-crash-dominated) flatly contradicts the true Bollinger band-width, and counter-trend volume was only the aggregate `volume_confirmation` score hiding *which* bars carried opposing volume (and net-move-anchored, not snapshot-trend-anchored). All four `dev` phases on `main`, no branch, migration-free, no new dep: `63cf241` ph1 (`keltner()` + `bollinger_bandwidth()` primitives — new `KeltnerValue`, trailing/length-aligned/`None`-prefixed, div-by-zero guard) → `f9bdc9a` ph2 (snapshot `bb_width` / `bb_width_pct90` / `squeeze_on` — TTM BB-inside-Keltner on the latest bar, `1.0`/`0.0` like `supertrend_direction`, honest `None` on too-few bars; `atr_pct90` demoted to a volatility fact; frozen 30-key indicators guard added) → `adb5a74` ph3 (`counter_trend_volume()` + frozen `extra=forbid` `CounterTrendBar`/`CounterTrendVolume` models — per-bar direction (close-vs-open, the pinned basis) + trailing relative volume + counter-trend flag anchored to the supplied snapshot `trend`; `sideways` → `anchored_to_sideways`, share `None`, no bar flagged) → `12f49f7` ph4 (`counter_trend_volume` MCP tool mirroring the `volume_confirmation` `{result, partial_reason, scanned_at}` envelope, anchored to `condition_snapshot(...).trend`, honest `no_bars`, trailing `as_of` replay; `EXPECTED_FULL_TOOLSET` 50→51, apiref regenerated). Clean Mode 4 — **no blockers/majors/minors/nits**. Every named test read at the assertion level, all genuine: ph1 pins band-width + Keltner against independent mean/pstdev + EMA/ATR references within `1e-9`, truncation-invariance + `None`-prefix length via the generic `_INDICATORS` suite, zero-middle → `None`, bad-param `ValueError`s; ph2 pins the exact 30-key `indicators` set, squeeze `1.0` inside-channel + `0.0` wide-band (re-verified against the primitives directly), honest `None` short-series, independent `bb_width_pct90` percentile; ph3 pins the hand-computed UP share `0.75` + DOWN mirror `0.25`, the sideways-undefined case, truncation-invariance (overlap-window entries identical), `extra=forbid`, determinism, and the BTC-probe divergence shape; ph4 pins anchor `== analyze_symbol`'s trend on identical bars, `no_bars`, `as_of` trailing-replay matching a direct computation. Gates re-verified at close: **2475 Python passed / 7 known Windows POSIX skips**, `mypy --strict` + `ruff` clean, `apiref --check` exit 0. Implemented directly in this working tree — no branch/worktree to merge or prune. **Phase 5 (`human` live smoke) run by architect at close via MCP and PASS:** `analyze_symbol ETH-USD 1d` → `bb_width_pct90`=78.9 vs `atr_pct90`=3.3 (contradiction now visible/resolvable in one snapshot), `squeeze_on`=0; `BTC-USD 1d` → 43.3 vs 4.4, `squeeze_on`=0; `counter_trend_volume BTC-USD 1d` (trend=down) surfaced the per-bar split (share 0.53), `ETH-USD 1d` (trend=sideways) returned the explicit `anchored_to_sideways`/share-`null`/no-flag result; nothing reads as a buy/sell call. Authoring-day numbers drifted (ETH band-width 73rd→79th, BTC 36th→43rd, BTC trend up→down) — the market moving; the qualitative claims + the tool's correct live-trend anchoring hold (plan-sanctioned). Followups: Keltner overlay + squeeze marker render (`ui-builder`); squeeze-trajectory field; advisor consumption of squeeze / counter-trend divergence (ADR-0029); squeeze-breakout strategy / `create_watch` alert. |
| 0094 | 2026-07-13 | **CL Quoter revert taxonomy — omit thin pools instead of aborting the scan — [ADR-0086](../adrs/0086-cl-quoter-revert-omit-taxonomy.md) accepted at close (refines [ADR-0080](0080-executable-quote-pricing-concentrated-liquidity.md)'s self-contradictory omit-vs-raise clause; does not supersede; sharpens the [ADR-0031](../adrs/0031-data-source-adapter-contract.md) `ExecutableQuoteSource` contract).** Closes the Plan 0086 phase-4 live finding: `ConcentratedPoolPriceAdapter` raised `ConcentratedPoolError` on **any** Quoter revert and aborted the whole scan, but a *quote-leg* revert also legitimately means "this thin pool cannot source the size" (confirmed live: Slipstream WETH/USDC tick-50/200 dust pools revert `quoteExactOutputSingle`), so one dust tier made `scan_pool_discrepancies` return `error:"malformed_response"`/zero-obs against any un-curated venue set. Phase 1 on `main` `c63301b` (`dev`, one commit, no branch, migration-free, no new dep, no output-schema/apiref change): read handling split **by call site** (ADR-0086, never by revert string) — `_result_bytes` raises a narrow `_ExecutionReverted` subtype of `ConcentratedPoolError` on a JSON-RPC `error` object; the two quote-leg `eth_call`s in `_quote_pool` catch it and return `None` (the pool is omitted, mirroring the CP `_cp_executable_legs → None` depth-omit; **either** leg reverting omits the pool — a round trip needs both), while structural reads (`getPool`/`slot0`/`decimals`/`fee`) do **not** catch it and still surface as `ConcentratedPoolError` (operator-visible misconfig), and a decode failure (too short / non-hex) or a zero Quoter output still raises `ConcentratedPoolError`/`ValidationError` (shape drift / fabrication guard). Module + `ExecutableQuoteSource` docstrings refined to the sharpened taxonomy. Clean Mode 4 — **no blockers/majors/minors**. Every done-when read at the assertion level: `test_quote_leg_revert_omits_pool_not_raises` + `test_buy_leg_revert_also_omits_pool` + `test_dust_tier_omitted_deep_tiers_still_priced` (mixed venue → exactly the two deep-tier quotes, dust tier dropped) pin the omit path; `test_{getpool,slot0,decimals,slipstream_fee}_revert_raises_not_omitted` pin structural-raise; `test_truncated_quote_result_raises_not_omitted` + `test_zero_quoter_output_raises_at_boundary_not_zeroed` pin the shape-drift / fabrication guards; `test_adapter_source_is_read_only` re-proves the JSON-RPC method set `== {"eth_call"}` + a dependency-free keccak selector self-check. Gates re-verified at close: **25 CL-adapter + 224 defi** tests green, `mypy --strict` + `ruff` clean. Adapter-internal — `ExecutableQuote`/`ArbObservation`/`scan_pool_discrepancies` shapes byte-identical. Implemented directly in this working tree — **no branch/worktree to merge or prune**. **Phase 2 (`human` live smoke) PASS** (2026-07-13, `runs/defi/audits/2026-07-13-plan-0094-phase2/`): the **shipped** adapter + production screener ran the full un-curated WETH/USDC set on Base at sizes 1/5/20 WETH — the scan **completed with no abort** (`scan_aborted=false`; before phase 1 the first dust tier returned `error:"malformed_response"`), the dust Slipstream tick-50/200 pools were **omitted at every size** (`dust_tiers_ever_present=false`, 7/6/6 deep venues priced), and the net-of-cost verdict is the expected honest-null no-go on majors (best NET −3.82 / −8.80 / −46.38 USDC, none capturable — same as Plan 0086 phase 4). **Closes Plan 0094 in full and clears Plan 0086 phase-5** (neglected-niche ES-4 — niche venue sets are dust-heavy and would have tripped the old abort). |
| 0093 | 2026-07-12 | **DeFi P&L user-attested dust-token override — [ADR-0085](../adrs/0085-defi-pnl-user-attested-dust-tokens.md) accepted at close (amends [ADR-0036](../adrs/0036-defi-pnl-reconstruction.md) narrowly — only the unpriceable-leg-⇒-incomplete rule, for attested dust; every other ADR-0036 invariant + the machine-never-fabricates-a-value core stand; builds on [ADR-0082](../adrs/0082-defi-pnl-partial-totals-and-windowed-lp-profitability.md) partial totals).** Closes the C1 Wanderers finding from the 2026-07-12 consolidated smoke — by **attestation, not a new price source**. Phase 1 on `main` `cb64203` (`dev`, one commit, no branch, migration-free, no new dep, no output-schema/apiref change): a `defi_dust_tokens` set of `token_key`-form `chain:address` keys (config `defi_dust_tokens`, lowercased) resolves to `$0` in the replay price path instead of raising `_MissingPrice`, so an unpriceable dust leg contributes $0 to basis/realized/unrealized and no longer blocks an otherwise-complete position. Implemented as a **`_DustAwarePriceSource` wrapper** around the historical-price source (a superior deviation from the plan's per-call-site literal — one wrap covers every lookup site: `_legs_value`/`_holdings_value`/`_amounts_value`/`_income_total`/`_windows`), with a **sorted disclosing note** on the still-complete position (`incomplete=False` with a non-empty note — consumers key on `incomplete`, never notes-presence). Wiring threads `AppConfig.defi_dust_tokens` `__main__` → `create_app` → the `/defi/pnl` route (`app.state`) + MCP tool registration → `run_wallet_pnl` → engine; `portfolio_summary` rides the empty default (plan-sanctioned). Honest by construction: **attested-not-inferred** (the engine still never guesses a value — the user declares it negligible), **disclosed** (never a silent zero), **deterministic** (the dust set is a run input; `model_dump_json` byte-identical re-run), and **loud-failure stays the default** for every unlisted token (ADR-0036 intact, asserted on the same fixture). Clean Mode 4 — **no blockers/majors**; two minors (route/MCP wiring rests on the job-level test + the phase-2 live smoke rather than a route-level assertion; `portfolio.py` untouched, riding the plan-sanctioned empty default) + one nit (the dust note is emitted only on the complete path, so an already-incomplete position won't disclose a zeroed dust token — harmless, its figures are null regardless). Every done-when read at the assertion level: `test_pnl.py` pins listed→completes-at-$0 with the real figures (realized 70 / basis 700 / unrealized 100 — the $0 leg booked, not a fabricated non-zero), `partial=False`/`count=0`, the disclosing note; unlisted-same-token→still-incomplete with `realized_usd is None` and the leg named; case-insensitive match + byte-identical re-run; `test_pnl_job.py` proves the parameter threads end-to-end (without→incomplete, with→complete-at-$0-with-note, basis is the WETH leg only). Gates green at phase 1 (886-scope defi suite 216 pass, `mypy --strict` + `ruff` clean, apiref zero-drift); doc-close gates green (tsc ×5, gen-types no drift, prettier clean). Implemented directly in this working tree — **no branch/worktree to merge or prune**. Close docs landed at `753439d` **after** a concurrent architect session's Plan 0094 / ADR-0086 commit (`5c77e49`) flushed the shared index; the two README index refreshes were **deferred at the doc-close and applied here** once that session landed (same shared-index-collision pattern as Plan 0088's close — nothing owed). **Phase 2 (`human` live smoke) PASS**, recorded as Run 2 (C3) in [`../consolidated-smoke.md`](../consolidated-smoke.md): with `base:0xef0fd52e…` attested as dust, `compute_wallet_pnl` on `0xae5b…9790` reconstructs **5/5** complete (up from C1's 4/5) — the Wanderers position `incomplete=False` at ~$0 with the disclosing note, wallet total no longer `partial` (`incomplete_position_count=0`). Followups: a renderer control to mark a token/position as dust from the DeFi tab (writes `config.json`); per-wallet dust designation if the global list proves too coarse; emit the dust disclosure on the incomplete path too. |
| 0089 | 2026-07-12 | **Polymarket open-in-browser market links + explicit sort — followup to [Plan 0078](done/0078-polymarket-convergence-screener.md) from its live smoke (no new plan-paired ADR; realizes [ADR-0041](../adrs/0041-polymarket-odds-read-source.md), records a provenance-link boundary call inside [ADR-0029](../adrs/0029-advisory-recommendation-boundary.md), honours [ADR-0008](../adrs/0008-electron-shell-conventions.md) external-nav).** All three code phases implemented in the parallel worktree/branch `plan-0089-polymarket-market-links` (`155adf2` `dev` ph1 → `4d03c75` `dev` ph2 → `e83a651` `ui-builder` ph3, status-flip `68e0315`), **merged to `main` `--no-ff` at `4a22df6`** — verified conflict-free against `main`'s post-`6707bdd` parallel Plan 0088 work (the only file overlaps — `en.ts`/`ru.ts`/`docs/reference/mcp-tools.md` — additive at non-colliding ranges). Migration-free, no new dep, no new event/version bump. A canonical `market_url` is built at the data boundary from the Gamma **event slug** (`https://polymarket.com/event/<slug>`, host+`https`-scheme validated, `None` when the slug is absent/unusable — the numeric `market_id` does NOT resolve, and the event slug was already in the search payload but dropped during flattening), copied onto `ConvergenceOpportunity` as provenance, returned by `find_convergence_opportunities` (CLI-clickable) and rendered as one read-only **"View on Polymarket ↗"** link per Convergence card opened via `shell.openExternal` (never a renderer navigation). Clean Mode 4 — **no blockers/majors/minors**. Every done-when read at the assertion level: ph1 pins host+scheme validation + a parametrized path-injection slug set (`has/slash`/`has space`/`%2F`/`?query`/`#frag`/empty/non-string → `None`, never a raise), `None` on absent slug, and by-id `fetch_market` → `None`; ph2 pins present-as-`null` (no `exclude_none`, so the renderer mirror sees the key), a real `polymarket.com` URL surviving the ADR-0029 advice grep, and the tool description advertising the field; ph3 drives dispatch→Zod→render, asserts **zero trade controls** (`button/input/select/textarea/[role=button]`) plus exactly one host-validated external link (`rel="noreferrer"`, `preventDefault`), renders no link for an off-allowlist `market_url` (look-alike host / `http` / port / garbage), and pins edge-descending order from a shuffled payload. Gates re-verified on `main` at close: **78 Python** + **64 renderer jest** (incl. `locales` + `events` parity) green, `apiref --check` exit 0. **One benign, plan-sanctioned deviation:** ph3's done-when named a main-process allowlist spec *only if* no external-open path existed — one did (`shell.openExternal`, built for `NewsView`), so the polymarket-host allowlist lives renderer-side in `safePolymarketUrl` (asserted in `ConvergenceView.test.tsx`) layered over the existing IPC-boundary `http(s)`-scheme allowlist (`shellOpen.ts`, asserted in `shellOpen.test.ts`) — defense in depth across three layers, no `desktop/main/` change. Worktree + branch pruned at close. **Phase 4 (`human` live smoke — click a returned `market_url` from both CLI and panel → correct market page in the system browser, list largest→smallest by `implied_return_if_right`, nothing reads as a buy call) was verified in the 2026-07-12 consolidated live smoke.** Followups: extend `market_url` to the `search_prediction_markets`/`prediction_market_odds` tool outputs (the field already lives on `PredictionMarket`); a short ADR if the provenance-link boundary call warrants durable capture. |
| 0088 | 2026-07-12 | **DeFi P&L: per-LP time-windowed profitability + partial wallet totals — [ADR-0082](../adrs/0082-defi-pnl-partial-totals-and-windowed-lp-profitability.md) accepted at close (amends [ADR-0036](../adrs/0036-defi-pnl-reconstruction.md) on two points — partial totals + windowed/estimated figures; every other ADR-0036 invariant — block-time pricing, average-cost lots, determinism-by-snapshot, loud-failure-never-zero — stands).** Five phases on `main`, no branch, migration-free, no new dep/tool/route: `dev` ph1 `6a5d169` (partial wallet totals — `realized_usd`/`unrealized_usd` sum over the **complete** positions + `partial`/`incomplete_position_count`; an incomplete position is **excluded, not zeroed**, reversing ADR-0036's any-incomplete⇒null while keeping never-fabricate-a-price) → `dev` ph2 `bef5541` (per-position **exact** 7d/30d/90d/all realized from per-event `mined_at` deltas, anchored to an injected per-run `now` the job captures once — the engine never reads the wall clock, grep-test enforced; `all` equals the all-time realized) → `dev` ph3 `6707bdd` (per-window **labeled estimated** total-return = realized-in-window + unrealized drift via a contributed-lot mark at window-start block-time prices; `total_return_usd=None` for any window whose start-mark is unpriceable — an honest per-window gap that never marks the position incomplete nor touches its exact realized) → `dev` ph4 `9240e83` (`is_lp` + stable LP-first ordering; a non-LP incomplete position never suppresses the LP figures; apiref regen) → `ui-builder` ph5 `eaeaea5` (first DeFi renderer surface — a new top-level **DeFi** tab with a read-only paste-a-wallet **Wallet P&L** view: `getWalletPnl` client + `useWalletPnl` hook, client-side address validation + recent-address chips, a `partial` banner, an LP-first table with a muted estimated-total-return sub-row rendering `—` for `None` windows, and a muted Other section). Clean Mode 4 — **no blockers/majors**; two non-blocking doc items for a future `dev` fix-forward: a stale claim-(e) header in `tests/defi/test_pnl.py:14-16` still describing the old null-total rule (the tests themselves were correctly updated), and a theoretical `mined_at==cutoff` boundary double-count in `pnl.py` (effectively impossible at block-second vs microsecond-`now`, and `total_return` is labeled estimated regardless). Every done-when read at the assertion level: ph1 pins the partial sum equals the hand-summed complete-only total (not `None`, not a `0` for the incomplete leg), the `partial`/count flags, no-regression on the all-complete path, and an incomplete-contributes-nothing removal test; ph2 pins the hand-worked `{7d:10,30d:30,90d:70,all:150}` split with `all==realized_usd`, byte-identical with a fixed `now`, empty windows on an incomplete position, and a source-grep that the engine never reads the clock; ph3 pins the hand-computed total-return per window (`7d:30/30d:40/all:40`), the unpriceable-window-start → `None` with `realized` + `incomplete=False` intact, and the `estimated` label; ph4 pins LP-first order + correct `is_lp` + a Wanderers-shaped non-LP incomplete leaving every LP figure + the partial total intact. The `portfolio_summary` caller was correctly updated to pass `now=as_of` (discards windows, keeps its basis replay deterministic). Gates re-verified on `main` at close: **886 Python** + 5 known Windows POSIX skips, **18 renderer jest** (`useWalletPnl`/`defiWallets`/`DefiPnlView`), `apiref` regenerates with **zero drift**. Implemented directly in this working tree — no branch/worktree to merge or prune. Close docs (`ec47b07`) landed while a concurrent architect session held both README indexes uncommitted; this index refresh was **deferred** at close to avoid absorbing that session's work and applied once it landed. **Phase 6 (`human` live smoke on `0xae5b…9790`) reported passed by the user** — the windowed LP figures + a non-null `partial` total excluding the one incomplete Wanderers position. Followups: configurable/arbitrary-range windows; a keyed price source for the `0xef0fd52e…` Wanderers token (carried from Plan 0087) to complete it + re-enter the total; reconstruct historical CL composition to make the estimated total-return exact. |
| 0078 | 2026-07-12 | **Polymarket convergence screener — the read-only analysis layer for "pick up the last few percent on a near-decided market" (no new plan-paired ADR; consumes [ADR-0041](../adrs/0041-polymarket-odds-read-source.md), bounded by [ADR-0029](../adrs/0029-advisory-recommendation-boundary.md); prereq [Plan 0040](0040-polymarket-odds-adapter.md), closed 2026-07-11).** Three code phases on `main`, no branch, migration-free, no new dep: `dev` ph1 `763f113` (pure deterministic `screen_convergence` + `ConvergenceOpportunity`/`ResolutionRisk` models — both-filters, gross `implied_return_if_right = (1-p)/p`, labeled low/medium/high resolution-risk heuristic with spelled-out reasons, a liquidity caution from the volume hint, a fixed capital-lockup note, **no direction/size/action field** on any model) → `dev` ph2 `fc3a467` (`find_convergence_opportunities` MCP tool over the [ADR-0031](../adrs/0031-data-source-adapter-contract.md) prediction-market registry + `prediction.screen_completed v1` event; **publish-once/bus-untouched across every failure class**, [ADR-0046](../adrs/0046-mcp-large-result-delivery.md) bounded `too_large` pages, typed error taxonomy, `EXPECTED_FULL_TOOLSET` membership, `docs/reference/` regenerated) → `ui-builder` ph3 `7ef711e` (reactive read-only Convergence panel: colour-coded risk badge + reasons + visible liquidity caution + lockup note, gross return labeled never-EV, `formatDuration` for the ISO-8601 wire duration, `.strict()` Zod loud-drop, TS↔pydantic parity guard over the three new models, en/ru keys, **zero action controls asserted**, reactive-only NO auto-switch — the 0037/0060 posture). Clean Mode 4 — **no blockers/majors/minors**; one harmless nit (the error-path `queried_at` reads wall-clock even when `now` is injected — provenance-only, on the null-opportunities path, outside the deterministic ranking). Every done-when read at the assertion level: ph1 pins the both-filters set (far-from-close/low-confidence/closed excluded) + hand-computed edge math + the resolution-risk level table + byte-identical re-run + a word-boundary advice grep over the JSON dump AND the model field set; ph2 pins **exactly one** envelope on a ≥1-opportunity success and **zero** on empty-fetch / all-filtered / upstream-error / input-validation-rejection (the last driven through the real registered tool), the `rate_limited`/`upstream_unavailable`/`malformed_response` mapping, `too_large` paging + second-page-by-offset, and `search_limit`/`min_confidence` passthrough; ph3 drives a real dispatch→Zod→render (rows carry the edge + full risk context, elevated-risk badge+reason, thin-book caution shown while deep-book shows none, gross return labeled "not expected value", duration rendered `2d 6h`), asserts **zero** `button/input/select/textarea/a/[role=button]`, the `.strict()` drop on an extra `direction` field (handler never fires, loud `console.warn`), and the placeholder-before-any-screen. Gates re-verified on `main` at close: **37 Python** (`test_convergence` + `test_prediction_screener_tool` + `test_mcp_tools`) + **54 renderer jest** (`ConvergenceView` + `events.test`) green; `apiref --check` exit 0. Implemented directly in this working tree (no branch/worktree to merge or prune). **Phase 4 (`human` live smoke against live Polymarket — near-resolution high-confidence markets surface with plausible edge, the resolution-risk/liquidity flags fire on the by-eye-flaggable markets, nothing reads as a buy call) was verified in the 2026-07-12 consolidated live smoke.** Followups: feed convergence opportunities to the `advisor` as a new basis input (weigh the tail); Polymarket *execution* (the deferred venue under [ADR-0072](../adrs/0072-bounded-autonomy-and-prediction-market-execution.md)). |
| 0087 | 2026-07-12 | **Closed the DeFi P&L wallet-total gap — the two residuals from [Plan 0084](done/0084-defi-pnl-gauge-swap-completeness.md)'s close that kept the wallet total `null`; [ADR-0081](../adrs/0081-defi-pnl-wallet-total-gap.md) accepted at close (refines [ADR-0079](../adrs/0079-defi-pnl-gauge-swaps-unclaimed.md)/[ADR-0036](../adrs/0036-defi-pnl-transaction-replay.md); all invariants stand).** Two code phases on `main`, no branch, migration-free, no new dep, no new tool/route/schema: `dev` ph1 `4ce70b6` (`AlchemyHistoricalPriceAdapter` — keyed Alchemy Prices `tokens/historical`, 2d-half-width/1h-interval window-bracket + nearest-point over the in-house `ResilientHttpClient` so **just a secret, not a package**; snapshots into the SAME `PriceSnapshotRepository` keyed by the SAME `token_key` as DefiLlama; wired as the DefiLlama-primary fallback in `api/app.py`, replacing the inert keyless CoinGecko adapter — its `market_chart/range` 401s; `alchemy_prices_key` registered in `persistence/secrets.py`) + `dev` ph2 `ebcf366` (bare single-leg outbound `send` of a position token with no lifecycle hint → `custody_move` via the existing no-op booking, basis-neutral). Clean Mode 4 — **no blockers/majors/minors**. Every done-when read at the assertion level: ph1 pins nearest-point, key-in-`Authorization`-header-never-URL-path, key-never-in-a-401-failure-log, unkeyed-inert + chain-degrades-to-no-coverage, native-vs-contract body, shared-snapshot determinism (chain re-run = zero network), empty/garbage(`0`/`-1`/`nan`/non-numeric)→`None`-never-`0.0`-never-snapshotted, typed 429; ph2 pins both real tx hashes (`0x303f8366`/`0x1cbbb89c`)→`custody_move` joined to the LP position, bare inbound `receive` keeps its Plan-0035 `fee_claim`, two-transfer send stays `unclassified`, `send`+`unstake` hint is not custody. Two documented deviations, both benign, folded into the close (no ADR change): (a) **secret hygiene** — key sent as `Authorization: Bearer` against a keyless URL path (not ADR-0081's path-embedded `{apiKey}`) because the resilient client logs the path; strictly better, Alchemy supports Bearer, phase-3 confirms the live endpoint; (b) **send-only narrowing** — ph2 classifies outbound `send` only (not plan-literal `send`/`receive`) because a bare inbound `receive` already has a Plan-0035 `fee_claim`/`reward_claim` and the two real residuals are outbound sends — exactly the Risks/open-question-pre-authorized "the fixture narrows it" resolution, avoiding a Plan-0035 reversal. The `custody_move` is correctly scoped to the position's own token by the join (`moved <= held`), not by `_classify`. Gates re-verified on `main`: **59 Python** green + 2 known-correct Windows POSIX skips; `mypy --strict` + `ruff` clean on all touched source. Implemented directly in this tree — no branch/worktree to merge or prune. **Phase 3 (`human` live smoke on `0xae5b…9790`: provision the Alchemy key, add `alchemy_prices_key` + point `base_rpc_url`/`eth_rpc_url` at Alchemy, restart the sidecar, `POST /defi/pnl refresh=true` → 5/5 complete, non-null totals, `0xef0fd52e…` prices via Alchemy, custody transfers book as no-ops, unclaimed reads real) was verified in the 2026-07-12 consolidated live smoke** — a null on that token's Alchemy coverage is a documented finding (re-open the price-source decision), not a phase failure. Followups: known-own-address registry (removes the custody-move-can-misstate-external-send limitation); surface `unclaimed_rewards` in `portfolio_summary`. |
| 0082 | 2026-07-12 | **Bollinger Bands render + user-originated chart overlay controls — [ADR-0077](../adrs/0077-user-originated-display-overlays.md) accepted at close (refines [ADR-0015](../adrs/0015-claude-code-primary-control-surface.md) the way [ADR-0039](../adrs/0039-renderer-theming-localstorage.md)/[ADR-0062](../adrs/0062-user-chart-style-overrides.md) did — a user-added indicator overlay is client-computed + no-sidecar-call + no-domain-change ⇒ a renderer-owned display preference, never on the wire).** The user's two coupled asks — make Bollinger Bands visible, and give the chart real overlay controls — both fully delivered. Phases 1–4 on `main`, no branch, migration-free, no new dep, no MCP tool/SSE event: `dev` ph1 `5681c30` (`bbands` reuses the existing `multiplier` as std-dev `k`, defaults 20/2.0 — additive docstring+test, no payload version bump) → `ui-builder` ph2 `1df1c1b` (client `computeBbands` mirroring `analysis/indicators.py::bollinger` **population** stdev; dedicated 3-band `useBbandsSeries` draw on the price pane; agent path also draws `bbands` now) + ph3 `67baa8a` (`lib/userOverlays.ts`: `(symbol,timeframe)`-keyed `localStorage['ma.userOverlays']` store + pure `mergeOverlays` — bounded 50×12 LRU, degrades on blocked/malformed storage, scope-guarded to the client-computable kinds, **sticky across agent redraws**, dedup by `overlayKey`) + ph4 `299e71a` (`AddOverlayForm` + pure `overlayForm.ts` validator; `CandlestickChart` merges user∪agent into `effectiveOverlays` feeding every draw hook + the legend; provenance-scoped legend — remove-× for user overlays, hide-only for agent). Clean Mode 4 — **no blockers/majors/minors**. Every done-when read at the assertion level: ph1's 3 pydantic specs pin `{kind,period,multiplier:2.0}` round-trip + `ema` byte-unchanged + `price/label/role` rejection (re-ran: 3 passed); ph2 pins `computeBbands` against a Python-generated fixture within 1e-6 with a case that discriminates population-vs-sample denominator **and** a truncation-invariance test for no-lookahead (also verified structurally — trailing window `bars[i-period+1..i]`, defined from `period-1`, no double-shift); ph3's 18 assertions cover add/reload/`(symbol,timeframe)`-isolation/remove(+empty-bucket-drop)/dedup(latest-wins)/scope-guard(`price_line`+unknown dropped on add *and* on persisted-store sanitize)/bounds/blocked+malformed degradation/subscribe/stickiness/provenance; ph4 the accessible form (`role="alert"`, labelled inputs, real submit) + validator (period int>0, `k`>0, bbands-only std-dev, Ichimoku classic defaults) + removable-only-for-user legend rows. Gates re-verified at close: **795 renderer jest** (81 suites) + typecheck (4 configs) + lint green; phase-1 pytest green; `mypy --strict` clean; `apiref --check` exit 0; en+ru locale keys symmetric (12 each), glossary `bbands` entry present. Implemented directly in this working tree (no worktree/branch to merge or prune); close docs landed after the concurrent Plan 0081 session cleared. **Phase 5 (OBV in the picker) deferred to a followup, not built** — Plan 0076 shipped OBV as an *always-on toggleable bottom strip* (its own documented deviation), not the on-demand `obv` overlay-path kind ph5 assumed, so a picker entry would be redundant and needs a fresh design call; the implementer correctly excluded OBV/VWAP from the form for exactly this reason (both are always-on aggregate series on their own scales). **Phase 6 (`human` live smoke — add BB from the form, confirm 3 bands with period+k, persists across reload, survives an agent `chart.show`, agent-requested `bbands` also draws, user-remove clears while agent-overlay only hides) was verified in the 2026-07-12 consolidated live smoke.** Followups: whether/how to expose the always-on OBV + client-side VWAP through the picker; a `vwap` (and richer parameterized `bbands`) `OverlayKind` for the agent path; full Ichimoku parameterization in the form; the BB band fill (shipped lines-only — the stated requirement); a "clear my overlays on this chart" affordance. |
| 0075 | 2026-07-12 | **Ichimoku strategy + backtest — the sanctioned "call from Ichimoku within the honesty rules" (Option C; no new ADR — follows [ADR-0004](../adrs/0004-strategy-interface.md)/[ADR-0050](../adrs/0050-short-selling-strategy-backtest.md) like [Plan 0054](done/0054-chart-pattern-breakout-strategy.md); prereq [Plan 0073](done/0073-ichimoku-cloud-indicator.md) ph1's `ichimoku()`).** A contract-conformant `strategies/ichimoku.py` (`Params`+pure `generate_signals`+`META`) importing `analysis.indicators.ichimoku` (no re-impl): **TK-cross gated by the cloud** — long on Tenkan-over-Kijun with close above the cloud under the current bar, short on the mirror (ADR-0050), with Chikou/cloud confirmation + `long_only` + `exit_on_cloud_cross` as `Params` toggles (classic 9/26/52/26 defaults, stop-and-reverse default exit). Two phases on `main`, no branch, migration-free, no new dep: `strategy-author` ph1 `ddb9879` (module + registers the 9th strategy, `test_cli` 8→9) → `backtester` ph2 `185a260` (`tests/backtest/test_ichimoku_backtest.py`, backtest + walk-forward through the flat/long/short engine, no engine change). Clean Mode 4 — **no blockers/majors/minors**. Every done-when read at the assertion level: the long entry lands *at* the bull-cross bar (`bar_index==11 ∧ ENTER_LONG`, nothing before); the bear mirror emits the exact `[EXIT_LONG, ENTER_SHORT]` stop-and-reverse and `long_only` collapses it to `[ENTER_LONG]`; the Chikou withhold is derived from real indicator output (bar-11 close 88 < close-6-back 90); `extra="forbid"` + `ge/le` period bounds reject at construction; ph2 pins **both** a long and a short trade off `result.trades` (long before short), `model_dump(exclude={run_id,started_at,finished_at})` re-run equality (ADR-0018), and contiguous strictly-increasing walk-forward folds (ADR-0024). **The displacement-lookahead risk — the one real risk — is pinned by a truncation-invariance test AND verified structurally:** the imported `ichimoku()` series is trailing (`out[i]` from `bars[0..=i]`, displacement explicitly not baked in) and the strategy applies displacement only at consumption (`values[i-displacement]`), so there is no double-shift. Gates re-verified at close: **18 Python** (`test_ichimoku` + `test_ichimoku_backtest` + `test_cli`) green, `mypy --strict` + `ruff` clean. One nit folded in, no change: ph2's fixture is synthetic (not "historical") and writes no `runs/` artifact — the honest choice (determinism-pinnable, no edge claimed, `runs/` gitignored). Once live, `recommend strategy_id="ichimoku"` gets a corroborated Ichimoku leg (the Option-C payoff). Followups: parameter sweep over `conversion`/`base` + the confirmation toggles; compare its walk-forward edge against Supertrend and chart-pattern breakout. |
| 0074 | 2026-07-12 | **Technical-read advisory tier — a single-indicator directional call; [ADR-0068](../adrs/0068-technical-read-advisory-tier.md) accepted at close (extends [ADR-0029](../adrs/0029-advisory-recommendation-boundary.md) with a lesser tier, consumes [ADR-0023](../adrs/0023-technical-analysis-surface.md) indicators + [Plan 0073](done/0073-ichimoku-cloud-indicator.md)'s `ichimoku()`).** A directional read (long/short/flat) from **one** curated regime indicator by its textbook rule, with **NO conviction and NO entry/stop/target** — a distinct `TechnicalRead` artifact that can never be confused with the fully-corroborated fused `Recommendation` (ADR-0029's all-legs-agree `fuse()` stays untouched). Three code phases on `main`, no branch, migration-free, no new dep: `dev` ph1 `42ec117` (`TechnicalRead` model, frozen + `extra="forbid"`, no conviction/level fields + pure curated-registry regime→direction core in `advisor/technical_read.py` — supertrend/ema_stack/macd/ichimoku, each reading the last closed bar of a trailing indicator, reusing `analysis/indicators.py` via the extended advisor import allowlist) → `dev` ph2 `c17ef1d` (`technical_read` MCP tool + `technical_read.completed v1` SSE event + `TechnicalReadCompletedPayloadV1` + `TYPE_REGISTRY` entry, closed-bar/publish-once discipline, `EXPECTED_FULL_TOOLSET` 48→49, `docs/reference/` regenerated) → `ui-builder` ph3 `cf8d4fc` (reactive read-only Technical-read tab: not-corroborated banner as prominent first child, indicator + direction + regime_state + rationale, zero interactive elements, no auto-switch, `.strict()` Zod dispatcher drop, TS↔pydantic parity guard, en/ru locale keys). Clean Mode 4 — **no blockers/majors/minors**. Every done-when read at the assertion level: truncation-invariance pins no-lookahead (read on `bars[:k]` == full-series Supertrend direction at bar `k-1`); `extra="forbid"` rejects a `conviction`/`stop` at construction; unknown-id and empty-bars raise at the boundary; the tool publishes **exactly one** envelope on success and **zero** on every failure class (empty-bars, unknown-indicator, all-bars-still-forming — drained from a pre-opened subscription); the AST-level read-only source scan over tool + core pins no key/order/network-write (the ADR-0029/0025 boundary the forecast tools carry); the parity guard asserts `conviction`/`entry_zone`/`stop`/`targets` structurally ABSENT from `TechnicalRead`; the panel spec drives a real dispatch→Zod→render and pins zero interactive controls + no level rows + a Zod-dropped malformed (extra-`conviction`) payload. Gates re-verified at close: **35 Python** (`test_technical_read` + `test_technical_read_tool` + `test_mcp_tools`) + **51 renderer jest** (`TechnicalReadView` + `events.test`) green; `apiref --check` exit 0. Toolset landed 48→49 not the plan's 44→45 — the baseline moved as the coordination note anticipated (intervening plans 0079/0080/0040/0081 each added tools); not a defect. **Ichimoku eligibility satisfied** (Plan 0073 ph1's `ichimoku()` exists, so all four indicators are registered). **Phase 4 (`human` live smoke — `technical_read BTC-USD 1d indicator=supertrend/ema_stack/macd/ichimoku` returns a direction matching the visible regime, viewer renders each with the banner) was verified in the 2026-07-12 consolidated live smoke.** Followups: a compact "technical reads" strip showing all eligible indicators' current regime at a glance; a fifth regime indicator if wanted. |
| 0076 | 2026-07-11 | **OBV chart overlay — draw the already-computed On-Balance Volume as a toggleable chart layer (no paired ADR — realizes [ADR-0023](../adrs/0023-technical-analysis-surface.md)'s already-computed OBV, follows the Plan 0073-ph3 additive-overlay pattern).** Two code phases on `main`, no branch, migration-free, no new dep: `dev` ph1 `e0f0602` (add `obv` to the `OverlaySpec` `kind` literal — a **fieldless** additive kind since OBV is cumulative/unparameterized, no payload version bump, rejects `price`/`label`/`role`; documented on the show_chart/update_chart surface; `docs/reference/` regenerated) → `ui-builder` ph2 `bde7d04`. **Recorded deviation, no defect, folded into the close:** the plan's Phase-2 "compute OBV client-side and draw a separate pane" already existed — `lib/volume.ts::computeObv` has drawn always-on in its own auto-scaled bottom strip since Plan 0027 (fixture `[0,200,50,350,350,600]` + no-lookahead prefix-invariance tested) — so ph2 correctly reconciled plan-vs-reality and shipped the actually-missing piece: a **layers-legend toggle** for that strip (`buildChartLayers` emits one `series:obv` row via a new `series` layer kind whenever the chart has bars; `CandlestickChart` applies its visibility to the OBV series in place with `applyOptions({visible})`, re-applied on a candle-type rebuild), plus the `obv` renderer `OverlayKind` parity mirror. Clean Mode 4 — no blockers/majors/minors. Every done-when read at the assertion level: ph1's three pydantic specs pin the bare-`{kind:"obv"}` round-trip + an `ema` overlay byte-unchanged + rejection of `price`/`label`/`role`; the TS↔pydantic literal-parity guard (`events.test.ts`) now carries `obv`; the `CandlestickChart.layers` spec drives a real toggle (`applyOptions({visible:false})` then `{visible:true}`) and `buildChartLayers` lists the OBV row only when `hasObv` (hidden-set honoured, ordered right after the overlays, no glossary key); the pre-existing `computeObv` fixture + lookahead-free tests satisfy the ph2 OBV-series done-when. Gates re-verified at close: **5 Python** (obv pydantic) + **78 renderer jest** across the 4 touched suites green (commit claims full 746 renderer green + typecheck 4 configs + lint); `docs/reference/` lists the `obv` kind. **Phase 3 (`human` live smoke — `show_chart` with an `obv` overlay renders the strip visibly tracking accumulation/distribution) was verified in the 2026-07-12 consolidated live smoke.** Both phases implemented in this working tree during a live parallel session (the 0076 files are disjoint from the concurrent work). **Unblocks [Plan 0082](0082-bollinger-bands-and-overlay-controls.md) ph5** (OBV in the user-overlay picker — consumes this strip, doesn't duplicate it). Excludes `obv_slope` overlay + any OBV strategy/alert (followups). |
| 0085 | 2026-07-11 | **Candlestick pattern explanations — markers said only the pattern NAME; now each also explains what it means, by reusing the Plan 0065 glossary (no new ADR — realizes [ADR-0060](../adrs/0060-glossary-tooltip-interaction-posture.md), honours [ADR-0046](../adrs/0046-mcp-large-result-delivery.md) glossary-never-on-wire).** A new `"candlestick"` glossary category with 14 entries keyed by the wire token (`doji`…`three_black_crows`), each dual-hat (`howComputed` from **our** detector rule + `whatItMeans`), surfaced in **two** places: LayersPanel legend rows via the existing `glossaryKey` slot (`<GlossaryTerm>` already wired) and the floating chart marker tooltip (`ChartTooltip`, a token→glossary lookup added). **Renderer-only, no wire/schema change** (`Marker`/`PatternHit` untouched — the `pattern` token already joins to the glossary). Implemented on branch `plan-0085-candlestick-pattern-explanations`, merged to `main` no-ff: `ui-builder` ph1 `d9a20fe` (glossary content + `candlestick` category + legend `glossaryKey` + test pins) + ph2 `280864d` (chart-tooltip meaning line, single-vs-stacked-marker rule, locale-threaded via `useLocale`→`tooltipAtTime`). Clean Mode 4 — **no blockers/majors/minors**. Every done-when read at the assertion level: the `candlestick` glossary keys **==** `PATTERN_DISPLAY_NAMES` (bidirectional completeness pin, no extras — a new detector without an entry, or an entry for a token the chart never emits, fails here); both hats validated on every record with a **synthetic-reject** proof; the tooltip's single-marker meaning line, **names-only when several markers stack on a bar**, and **name-only degrade for an unknown token** each carry a matching `expect`; the existing name-only chart-tooltip assertion extended to name **+** meaning via a real `tooltip-marker-meaning` testid. Gates: **45/45** renderer jest green across 6 suites (glossary, tooltip, layersLegend, CandlestickChart.tooltip, useChartTooltip, trendline-tooltip), re-verified on `main` post-merge; merge **conflict-free** (the branch's 14 renderer files are disjoint from `main`'s post-fork Python/docs changes — `merge-tree` clean). **Phase 3 (`human` visual smoke) confirmed fine by the user.** No paired ADR. Followups: RU pass over the 14 `howComputed`/`whatItMeans` strings (`ru` is optional per-field, falls back to en); same glossary treatment for the Plan 0083 trendline legend rows if parity is wanted. |
| 0083 | 2026-07-11 | **Chart-pattern visual fidelity, pattern-by-pattern — classical patterns now render as their textbook shapes; [ADR-0078](../adrs/0078-chart-pattern-visual-fidelity.md) accepted at close (refines [ADR-0048](../adrs/0048-classical-chart-pattern-detection.md)'s connect-the-extremes anchors, extends [ADR-0049](../adrs/0049-chart-trendline-overlay-primitive.md)/[ADR-0061](../adrs/0061-trendline-pattern-identity-and-colour.md) render).** Fixes the symmetrical-triangle "big V" (a spike-low pivot no longer anchors a boundary), adds on-chart apex convergence, and draws the detector's existing measured-move projection as a confirmed-only breakout arrow — then reuses that foundation for head & shoulders (skeleton + shaded humps) and double top/bottom (base line + projection). Six code phases on `main`, no branch, migration-free, no new dep: `dev` ph1 `00ab25b` (converging-**envelope-with-outlier-tolerance** anchor selection for triangles+wedges — real pivots, no regression, `_envelope_pair` picks the pair maximising touches while tolerating a bounded outlier fraction; ADR-0048 core stands) + ph2 `c7f488e` (confirmed-only `projection` `LineSeg` at the breakout bar, vertical to `hit.target`, additive `"projection"` role on `LineSeg`/`TrendlineSpec`); `ui-builder` ph3 `e310fbd`+`3da9346` (`computeApex` extend/clip with a `APEX_MAX_HORIZON_FACTOR` near-parallel fallback, dotted arrowhead on `role==="projection"`); `dev` ph5 `d373d6c` (H&S skeleton + neckline-break projection via widened `_PROJECTION_PATTERNS`); `ui-builder` ph6 `bd67b3e` (skeleton polyline + `computeTrendlineFills` shaded humps between skeleton and neckline); `dev` ph8 `a1ba607`+`b596349`+`ddafb87` (`"base"` horizontal through the two matching extremes + up/down projection for doubles, no fill/skeleton). Clean Mode 4 — **no blockers/majors**. Every done-when read at the assertion level (exact envelope anchors + spike rejected as any anchor; projection endpoint == target with correct up/down per family; 5-point skeleton in pivot order; base-line extremes; apex intersection pixels; near-parallel unchanged-segments fallback; dotted-vs-solid dash arrays + arrowhead fill; hump-polygon vertices; forming-carries-no-projection in every family; no-lookahead — projection built only once the confirming bar is in `bars[0..=i]`). One recorded deviation (not a defect, folded into the close, no ADR change): ph5's skeleton ships as a draw-layer multi-point `TrendlineSpec` derived in `_hit_trendlines` (NOT a `LineSeg` — that model is 2-point), so `role="skeleton"` lives on `TrendlineSpec.role` only; the behavioral done-when (5-pivot skeleton polyline reaches `chart.trendlines`) is fully met, and `ddafb87` removed a consequently-vacuous `LineSeg.role != "skeleton"` test line (the guarantee is pinned at the wire level instead). One harmless nit noted only (a redundant intra-`computeTrendlineSegments` apex pass; the real work is the cross-spec pass in `currentSegmentsBySpec`). Gates: **43** detector/tool pytest + **54** `trendlines.test.ts` + **8** `CandlestickChart.trendlines` renderer jest green; ruff + mypy --strict + `gen-types:check` (no drift) clean. **Phases 4/7/9 (`human` visual smokes: symmetrical triangle + rising wedge, H&S, double bottom) are the user's outstanding step, not code gates** — closed now per the user's decision; a smoke that files a visual delta becomes a small followup, not a reopen. The **Pattern backlog** (ascending/descending triangle, falling wedge, inverse H&S — all foundation-covered coils / ph5-6 reuse, no new anchor logic) is deferred to followup phases: the foundation and every render capability already landed, so each is a screenshot-confirm, not new code. Geometry-fact stance throughout — no action field, ADR-0029 analyst boundary unchanged (tests assert no buy/sell keys). |
| 0079 | 2026-07-11 | **DeFi cross-pool discrepancy scanner — the read-only *evidence layer* for the high-speed-arbitrage idea; no new plan-paired ADR (it is the [ADR-0072](../adrs/0072-bounded-autonomy-and-prediction-market-execution.md) BA-7 evidence gate, which stays `proposed`).** A read-only `PoolPriceSource` Protocol ([ADR-0031](../adrs/0031-data-source-adapter-contract.md)) + `OnchainPoolPriceAdapter` reading constant-product (v2/Aerodrome-style) pools' marginal price via `eth_call` over a configured JSON-RPC read endpoint ([ADR-0038](../adrs/0038-third-party-api-key-storage.md) — a read URL, not a trade key), plus a pure `scan_discrepancies` screener computing **net-of-cost** spreads (gross − gas − slippage − fees) across venues and a bounded read-only `scan_pool_discrepancies` MCP tool ([ADR-0046](../adrs/0046-mcp-large-result-delivery.md)). Signs nothing, no private key, no state-changing RPC — **proven, not asserted**: `test_adapter_source_is_read_only` runs a regex token-scan + an AST walk pinning the only JSON-RPC method to `eth_call`. Three `dev` phases on `main`, no branch, migration-free, no new dep: ph1 `41c2922` (Protocol + adapter + `PoolQuote` + registry entry), ph2 `7f8e884` (screener core), ph3 `20f55a9` (MCP tool). Clean Mode 4 — no blockers/majors. **The Phase-1 open design call was resolved correctly by riding ADR-0031 + ADR-0038 without a new read-source ADR** (the adapter is a thin `ResilientHttpClient` extension issuing only sequential `eth_call`s — not the batched/multi-chain shape that would warrant an ADR-0052/0053-style record). **Net-of-cost decomposition verified mathematically exact** (`gross_spread − est_slippage == quote_out − quote_in`, the true round-trip proceeds; slippage ≥0 both legs; `net_spread` the only number called an opportunity, may go negative). Determinism held (`queried_at` = newest quote `as_of`, stable sort, byte-identical re-run pinned; boundary rejects NaN/Inf/non-positive, never zeroed). Clean layering through the `pool_price_sources` registry seam. Gates: 53 tests green, `apiref --check` exit 0, `ruff` + `mypy --strict` clean, `EXPECTED_FULL_TOOLSET` carries the tool. **Live plumbing smoke passed** (tool live on the running sidecar → well-formed bounded response, `source: "onchain"`, `capturability_note` present, empty observations by design — `DEFAULT_POOLS` empty until phase 4). Two folded items, no code change: a doc-nuance (the tool default is a successful empty page, not `unconfigured`, whenever `secrets_store` exists) and a nit (the tool is inert until phase 4 supplies verified addresses — intentional). **Phase 4 (`human`, the BA-7 live evidence smoke on Base → `runs/defi/`) was verified in the 2026-07-12 consolidated live smoke** — needs a live RPC URL + verified real pool addresses; a null result (discrepancies vanish net-of-cost) is a documented, legitimate success. Honesty pin throughout: RPC-observed persistence is an **upper bound** on capturability, never capture. Excludes all execution/signing/flashloans (ADR-0072), the scheduled persistence study, and UI (all followups gated on phase-4 evidence). |
| 0040 | 2026-07-11 | **Polymarket as a read-only prediction-market odds source — Pillar 3; [ADR-0041](../adrs/0041-polymarket-odds-read-source.md) accepted at close.** A new `PredictionMarketSource` per-capability Protocol ([ADR-0031](../adrs/0031-data-source-adapter-contract.md)) + `PolymarketOddsAdapter` over the **auth-free** Gamma API through the [ADR-0019](../adrs/0019-external-http-adapter-resilience.md) resilient client — no key, no signing, no funds; each outcome's price *is* its market-implied probability, boundary-validated into `[0,1]` (out-of-range / NaN / missing → typed `PolymarketError`, never a silently zeroed probability). Surfaced via two always-registered keyless read-only tools (`search_prediction_markets`, `prediction_market_odds`) that report odds as **facts, never a call** (ADR-0029/0041 — a structural no-advice test pins exact key-sets + a token blocklist). Two `dev` phases on `main`, no branch, migration-free, no new dependency: ph1 `4a3ecdb` (Protocol + adapter + `PredictionMarket`/`MarketOutcome` models), ph2 `0f4a4ab` (tools + `EXPECTED_FULL_TOOLSET` 45→47, `docs/reference` regenerated). Clean Mode 4 — no blockers/majors/minors. Every done-when read at the assertion level (52 tests: happy path pins labels+probs; a 7-way malformed matrix + explicit never-zeros-a-probability; 404→`UnknownMarketError` with `attempts==1` proving no-retry, 429→`RateLimitedError`, 500→`UpstreamUnavailableError`; search flatten/skip-not-trading/empty/limit; Protocol `isinstance` + one-registry-entry; source-grep for zero auth/key/signing tokens; all four tool error reasons mapped; keyless always-registered). Gates: ruff + mypy --strict + apiref --check clean, full non-network suite green (2192). One reconciliation nit, self-flagged by dev, folded into the close (no new ADR): the plan's *illustrative* shape put `volume_usd` on the outcome and drew a separate CLOB leg — dev correctly moved volume/liquidity to the market (upstream reports them per-market) and used Gamma's `outcomePrices` (verified live == CLOB midpoint) for one keyless call; the live-book (best-bid/ask) refinement is a documented followup. No trading/UI/historical-odds (all deferred followups). **Unblocks Plan 0078** (Polymarket convergence screener, which consumes this source). |
| 0081 | 2026-07-11 | **Coinbase as the USD-native crypto OHLCV source — deep `BTC-USD`/`ETH-USD`/every Coinbase-listed crypto `-USD` instead of Yahoo's shallow intraday caps; [ADR-0076](../adrs/0076-coinbase-usd-native-crypto-source.md) accepted at close (refines [ADR-0052](../adrs/0052-binance-exchange-data-source.md) routing two-source→N).** Keyless Coinbase Exchange adapter ([ADR-0019](../adrs/0019-external-http-adapter-resilience.md)); membership routing **Binance → Coinbase → Yahoo** over the cached `/products` set; native `15m/1h/1d`, **derived on read** `4h←1h · 1w←1d · 1mo←1d` from a Coinbase base only (single-provenance preserved); **source-scoped cache read + one-way purge migration `0009`** of orphaned Yahoo crypto `-USD` rows (provenance switch safe by construction); quote+search hold the [ADR-0026](../adrs/0026-symbol-search-bound-to-ohlcv-provider.md) chartable invariant; serves [ADR-0069](../adrs/0069-crypto-first-asset-class-positioning.md) + hedges ADR-0052's geo fragility. Four phases on `main`, no branch: `dev` ×2 (adapter `6d47f64`; N-source routing + provenance-scoped cache + migration `f34e0a2`) + emergent search-relabel fix `2e4537a` → `human` ×1 (phase-3 live smoke **GO** — reachable/US-clean, deep 15m past the 60-day wall for ≥2 pairs, derived TFs render) → `ui-builder` ×1 (picker source badges + deep-USD hint `cba63f9`). Clean Mode 4 — no blockers/majors/minors; one reconciliation nit folded into the close notes (phase 4's "no backend change" note did not hold — the routed-source relabel of dual-listed symbols needed the `default_provider.search_symbols` fix `2e4537a`, self-flagged by dev; no new ADR). Every done-when read at the assertion level: 3-source routing spies + both-sets precedence guard; the provenance switch proven (a pre-seeded Yahoo `BTC-USD` row physically present yet never returned once Coinbase routes it — source-scoped read, not a delete); `4h/1w/1mo` from the Coinbase base only with golden calendar-week/month buckets + weekly/monthly anti-lookahead prefix-invariance; migration `0009` scoped `source='yahoo' AND symbol LIKE '%-USD'` (network-free, downgrade no-op); the phase-1 adapter test thorough (paging/dedup, typed errors, 451→Geo no-retry, quote→USD, never-aliases-to-Binance). Gates: **111 Python** + **9 SymbolPicker jest** green. Close docs landed across two commits (`bb65d31`/`a134e89`) after a shared-index collision with the concurrent Plan 0082/ADR-0077 session — content intact, no history rewrite. No new dep, no derivatives (spot only), no alias/merge of `BTC-USD`↔`BTCUSDT`, no TradFi impact. |
| 0080 | 2026-07-11 | **Advisor recommendation track record — score the advisor's OWN live calls path-dependently against realized price; [ADR-0075](../adrs/0075-recommendation-outcome-attribution.md) accepted at close. The live instrument for [ADR-0074](../adrs/0074-edge-selection-criteria-for-execution.md) ES-3/ES-5.** Closes the "we called DOGE long, price rose — note it" loop honestly: an **append-only every-call ledger** (`advice_ledger`, migration `0008`) records every `recommend` call directional + flat with no update/delete path (cherry-picking a loser out is structurally impossible), a lifespan **scheduled scorer** ([ADR-0056](../adrs/0056-self-warming-metric-store.md) pattern) matures + scores each call **path-dependently** (stop-or-target-hit-first honoring the ticket — a stopped call ending higher is a LOSS; conservative stop-first tie-break; no-lookahead maturity gate) and publishes `recommendation.scored v1`, and a pure aggregation reports **hit-rate + calibration (Brier + reliability buckets) + a beats-buy-and-hold-baseline delta** with **honest small-n** (below `MIN_TRACK_RECORD_N=20` → every conclusion field `None`). Read-only `get_track_record` tool ([ADR-0046](../adrs/0046-mcp-large-result-delivery.md) bounded) + read-only Track-record view (baseline delta prominent, zero action controls) + `GET /track_record` route. Five code phases on `main`, no branch: `dev` ×4 (`1d8a5c9` ledger + write-on-recommend + one-shot `runs/advice` back-fill → `74aa67a` pure scoring → `ec5050b` scorer + event → `3560b29` aggregation + tool) → `ui-builder` ×1 (`04337ee`→`890f176` view + route). Clean Mode 4 — no blockers/majors/minors/nits; every done-when read at the assertion level (anecdote-killer `stopped` + `directional_correct=True`; lookahead-trap bar ignored; byte-identical re-score; baseline-mimic `vs_baseline ≈ 0`; overconfidence 0.80/0.55 flagged; n=3 withholds with no bare % leaking to the renderer; exactly-one-event-strictly-after-persist + per-row heartbeat containment + never-scores-flat; word-boundary no-advice assert + `EXPECTED_FULL_TOOLSET` set-membership). `create_app` defaults scoring `False` (no network in test apps); `AppConfig` ships it on; determinism preserved (`now` seam-routed to the maturity check only). Gates: 128 Python + 70 renderer jest green, `docs/reference/` regenerates byte-clean. **Phase 6 (human live smoke) was verified in the 2026-07-12 consolidated live smoke** — it accrues real calls and waits for horizons to mature, so it runs on the user's cadence; a null track record is a documented success. Scope = recommendations (forecast-tool calls a followup); no advice/execution/auto-tuning. |
| 0073 | 2026-07-11 | **Ichimoku Cloud indicator — analysis surface + cloud-aware trend + faithful displaced/filled render; [ADR-0067](../adrs/0067-ichimoku-in-trend-classification.md) (Ichimoku in the composed trend classifier, refines [ADR-0023](../adrs/0023-technical-analysis-surface.md)) accepted at close.** A pure trailing `ichimoku()` (Tenkan/Kijun/Senkou A-B/Chikou, tunable periods, classic 9/26/52/26 defaults) feeds `_classify_trend` via ADR-0067's **conjunctive veto** — Ichimoku can turn an EMA-stack `UP`/`DOWN` into `SIDEWAYS` when the correctly-**displaced** cloud-under-price (`senkou_*[i-displacement]`) disagrees, but never manufactures a direction; undefined-cloud falls back to the pre-0073 EMA/ADX path unchanged. A thin additive `ichimoku` `OverlaySpec` kind (no payload version bump) drives the renderer's client-side recompute — the ADR-0023 display-side duplication. Five phases on `main`, no branch, migration-free: `dev` ×3 (ph1 indicator `7e86b25`, ph2 classifier `98c92c1`, ph3 overlay kind `11ca0da`) → `ui-builder` ×1 (ph4 displaced filled-cloud + lagging-Chikou render, `fb79b9e`, ran after Plan 0072 ph8 via the Plan 0064/ADR-0059 off-grid future-axis extrapolation; `IchimokuPrimitive` attached in `CandlestickChart.tsx`, fed by `useIchimokuSeries` with a `rightOffset = displacement` reservation so the projected cloud lands on-screen) → `human` ×1 (ph5 live smoke, user-confirmed OK). Clean Mode 4 — no blockers/majors/minors/nits; every done-when read at the assertion level. Anti-lookahead genuinely defended: `ichimoku` is in the parametrized `test_anti_lookahead_truncation_invariance` set, `test_snapshot_ichimoku_anti_lookahead` pins the displaced read on prefixes, the veto matrix exercises all four UP/DOWN×confirm/veto branches plus cannot-manufacture and undefined-fallback, and the phase-4 lib test pins the displaced mapping (`i+disp` Senkou/cloud, `i−disp` Chikou, projection to `79+disp` past the last candle) + the cloud crossover colour split; the phase-3 test pins exact-set-fields serialization, byte-unchanged `ema` wire, and price/label/role rejection. Benign improvement over the plan text: `ichimoku()` defines from `max(conversion, base, span_b) - 1` (correct for custom periods; identical under classic defaults), documented in docstring/test. `docs/reference/mcp-tools.md` regenerated. Close gates: 80 analysis/events pytest + 84 renderer jest green. **Clears the `ichimoku()` prereq for Plan 0075 ph1 and the Ichimoku technical-read leg of Plan 0074.** Rejected: additive-only snapshot role (leaves regime computed-but-ignored), fixed periods, chart-style-settings coupling (entangles in-flight 0068), parallel render (contends 0068/0071 on the hot file). |
| 0077 | 2026-07-11 | **Forecast pivot: volatility + regime-transition forecasting, direction demoted — [ADR-0070](../adrs/0070-non-directional-forecast-targets.md) (non-directional forecast targets, extends [ADR-0030](../adrs/0030-forecasting-subsystem.md)) + [ADR-0071](../adrs/0071-advisor-non-directional-inputs-and-direction-demotion.md) (advisor non-voting inputs + direction demotion, amends [ADR-0029](../adrs/0029-advisory-recommendation-boundary.md)) accepted at close.** The direction forecaster's near-random target is answered by two **non-directional** forecast kinds sharing the existing walk-forward/tiered-feature/determinism/explainability harness (no new dependency; reuses sklearn `HistGradientBoosting*`): **volatility** (regression vs a deterministic EWMA/persistence baseline, QLIKE-scored) and **regime-transition** (trailing rule-based current-regime classification whose trend axis is pinned `== _classify_trend`, + a persistence-baselined next-period classifier, Brier-scored). Direction kept but **demoted** — non-gating below a `DIRECTION_SKILL_MARGIN`; advisor consumes vol/regime as **non-voting** conviction/sizing/stop inputs so ADR-0029's directional-agreement-of-voting-legs invariant stays intact. Six code phases on `main`, no branch, migration-free: `dev` ×4 (ph1 volatility `dd690ab`, ph2 regime `5012d95`, ph3 tools+SSE+explainability `5813462`, ph5 advisor demotion+non-voting `85366d3`) → `ui-builder` ×1 (ph6 views `35a57c7`). **Phase 4 (`backtester`) is an honest NULL result** (`runs/analysis/2026-07-10-plan-0077-vol-regime-edge/`): 0/12 volatility cells beat EWMA and 1/12 regime cells (BTC h=21, tier-robust) but it does **not** replicate to ETH, so no ML edge clears the cross-asset overfit guard — the deterministic EWMA/persistence baseline and the trailing regime label are the useful shipped outputs; also answers the ETH-history open question (ETH supports only v1 — the 200w-MA warmup, not exogenous depth, is the blocker). Clean Mode 4 — no blockers/majors/minors/nits; every named done-when read at the assertion level (future-bar perturbation leaves past feature rows + baselines byte-identical = no lookahead; honest `beats_baseline=False` on iid fixtures; `model_dump` determinism; regime trend `== _classify_trend`; exactly-once `*.completed` SSE with zero-on-failure; AST read-only source scan; the directional-agreement invariant — adverse vol/regime make a call smaller/wider-stopped/lower-conviction, never flip or manufacture one; direction-demotion skill-margin boundary pinned both sides). Gates: 154 forecast/advisor/api pytest · 113 renderer jest · `apiref --check` clean · `mypy --strict` + ruff + tsc + eslint green. **Phase 7 (human live smoke + go/no-go on keeping the advisor vol/regime wiring active) was verified in the 2026-07-12 consolidated live smoke**; a "wiring off" outcome is a small follow-up toggle, not a reopen. Coordination note for in-flight [Plan 0074](0074-technical-read-advisory-tier.md): 0077 landed the `forecast_volatility`/`forecast_regime` toolset bump first — 0074 rebases its `EXPECTED_FULL_TOOLSET`/`TYPE_REGISTRY` count (trivial). |
| 0072 | 2026-07-10 | **Codebase remediation from the 2026-07-09 whole-repo audit (security · tests · structure · docs) — [ADR-0065](../adrs/0065-neutral-ui-event-buffer-core.md) (neutral UI-event buffer core) + [ADR-0066](../adrs/0066-sse-short-lived-ticket.md) (short-lived SSE ticket) accepted at close.** All nine phases landed on `main`, no branch, migration-free. `dev` ×5 (ph 1–5, `49f1305`→`61bafc6`): neutral `ui_events/` core so `alerts/scheduler.py` stops importing the web layer [ADR-0065]; `events/__init__.py` split into `payloads.py`+`bus.py` (surface unchanged); determinism-critical `_merge_gaps` de-dup + cross-tool `_`-private reaches promoted to `mcp_tools/_shared` + `fusion.fuse()` blockers derived from `_build_checks`; SSE ticket mint endpoint + `?ticket=` on `/events` + `Cache-Control: no-store` on the mcp-secret response [ADR-0066]; Python test-honesty (`test_settings_route` `==200`, widened Yahoo smoke). `ui-builder` ×2 (ph 6–7): client bearer→ticket exchange + reconnect re-mint + prod CSP `img-src` `https:` drop; renderer test-honesty. **Phase 8 (`bba6c4b`, the gated decomposition — ran only after 0067+0068+0071 closed): the 1455-line `CandlestickChart.tsx` split into 10 per-concern hooks + 4 pure `lib/` modules + a `ChartToolbar` child (706 lines, −51%)** — all 15 existing CandlestickChart specs unmodified + green, +45 new hook/lib tests, renderer suite 670/670. `human` ×1 (ph 9, `c8f0955`): overdue cooldown-cutoff bump to 2026-07-03. Clean Mode 4 on ph 8 — no blockers/majors, one nit (`useLayersLegend` type-only import from a component). Close ceremony `9ab1860` flipped Status→done + moved the plan to `done/` + accepted both ADRs, but **deferred the plans/ADR index-README row refreshes** (both index files were dirty with a parallel architect session's uncommitted ADR-0069/0070/0071 + Plan 0077 work) — those roster/row refreshes were applied by that session's commit (this one). **Clears Plan 0072 phase 8's ordering gate for the render phases of Plans 0073 + 0076.** |
| 0071 | 2026-07-10 | **Candlestick pattern legend + declutter, and the two scan buttons disambiguated — renderer-only, no new ADR (reuses [ADR-0061](../adrs/0061-trendline-pattern-identity-and-colour.md)'s grouped-legend interaction, consumes [ADR-0045](../adrs/0045-candlestick-pattern-span-delivery.md)'s candlestick identity).** Diagnosed live 2026-07-09 on `AERO29270-USD` 1d: the two look-alike buttons ("Scan patterns" / "Scan chart patterns") were confusing and the candlestick sweep painted **all 104** in-view markers at once, burying the candles. Fix: (1) **renamed** the buttons to **"Candlesticks"** / **"Chart patterns"** through the Plan 0069 `t()` catalogs (`chart.scanPatterns`→`chart.candlesticks`, `chart.scanChartPatterns`→`chart.chartPatterns`, en + ru; `scan-patterns-button`/`scan-chart-patterns-button` testids stable); (2) **grouped legend + draw-on-select** — sweep markers group by (pattern type, direction) from the ADR-0045 identity (renderer-derived, no wire change) into the **reused-and-generalized** Plan 0067 `LayersPanel` grouped-legend (one legend model for both trendline groups and candlestick-marker groups), one row per group with count + show/hide + hover-highlight; markers **and** spans draw only for enabled groups; a single MASTER toggle governs the whole layer (the coarse per-direction rows + the standalone "Pattern spans" row folded into it, no desync); default seeds the single most-recent group so the chart is populated yet not walled. A phase-2 follow-up (`9981638`) added marker-hover pattern-name in the tooltip + a pattern-bar outline highlight and fixed a ghost-hover (crosshair read raw annotations, now `drawnMarkers` so a toggled-off group shows no hover). New pure guard-exempt `lib/candleGroups.ts` mirrors `lib/trendlines.ts`; the god-component grew by wiring only (the plan's risk-note mitigation). Four `ui-builder` commits on `main` (`d94b77f` → `9ef4ac5` → `9981638` → `6765bde`), no branch, migration-free; the last is a draggable, `localStorage`-persisted, accessible-splitter layers-panel width (ADR-0039 renderer-owned-prefs convention — clamped, keyboard-resizable; reviewed clean). Clean Mode 4 — no blockers, no majors, no minors; every phase-2 done-when read at the assertion level against the real component (5-marker/2-group sweep draws only the most-recent group `=== 2`, never 5; exactly K rows with correct counts; group toggle 2→5→2; master →0 without desync; hover grows the group; empty/error states intact). Close gates: **625/625** renderer jest (63 suites), `typecheck` + `lint` clean. Phase 3 (`human` live smoke) was verified in the 2026-07-12 consolidated live smoke. **Clears the last chart-file plan gating Plan 0072 phase 8** (0067 + 0068 closed 2026-07-09). Rejected a strength/recency filter (markers lack a comparable strength score; hidden ≠ discoverable) and per-type checkboxes + a density slider on LAYERS (keeps markers always-on; two inconsistent mental models). |
| 0069 | 2026-07-10 | **Russian localization (full parity) — the whole desktop UI in Russian, English default; [ADR-0063](../adrs/0063-in-house-i18n-and-reason-codes.md) accepted at close (extends [ADR-0039](../adrs/0039-renderer-theming-localstorage.md)'s renderer-owned prefs).** An in-house, zero-dependency `t(key, params?)` over typed `en`/`ru` catalogs (native `Intl.PluralRules` for Russian's 3 plural categories; `Intl.NumberFormat` left at **en-US** by decision, `format.ts` untouched) in a locale store shaped like `theme.ts`, English as **both default and test locale** so existing renderer specs stay green. Chrome extracted behind a `no-unkeyed-literals` eslint guard; the renderer-owned glossary became locale-keyed prose (structural `category`/`formulaAnchor` fixed in place so Plan 0065's cross-language anchor test still binds). The sidecar stays **English-authoritative, no locale negotiation**: for the verbatim-rendered surfaces (`RecommendationsView`/`ForecastView`/error toasts) `advisor/fusion.py` + `forecast/explain.py` emit additive structured `{code, params}` **reason-codes** beside their unchanged English prose (kept for the agent/MCP consumer) — ph4 covered rationale/blocker/gate-check, ph4b closed the gap by co-emitting `condition_codes`/`signal_codes` with the closed condition/signal enums as **raw tokens** the renderer maps through an enum-label catalog (no prose-parsing). Dynamic upstream text (external headlines, `str(exc)` data-layer errors, raw upstream classifications, the composed `fallback_reason`) stays English **by design**, documented in `ru.ts`. Six impl phases on `main`, no branch (`46df6cf` → `174ca17` → `7d8b8c0` → `ad291f6` → amend `90995aa` → `70d9318` → `9834eea` → `150fec0`); ph7 human live smoke **passed** (user-confirmed 2026-07-10). Owners: `ui-builder` ×5, `dev` ×2, `human` ×1. Clean Mode 4 — no blockers, no majors, no minors; every done-when read at the assertion level (real `Intl.PluralRules('ru')` one/few/many + en-US `#` pins; both-direction key-set + ICU-arm-aware placeholder-survival parity; `directional ⟺ every gate passed` on the reason-code surface + 1:1 condition/signal-code↔prose order with raw-token params + unchanged prose + determinism on both code surfaces; ph5 views render from codes with `PROSE_*` sentinels proving code-drive + a no-code prose fallback). Phase-4b `docs/reference/` regen correctly a no-op (apiref renders `Recommendation` one level deep; ph4 already added the top-level `reason_codes` row). Close gates: **585/585** renderer jest · **127/127** advisor+forecast pytest · `mypy --strict` + ruff clean · `tsc` (5 projects) + eslint clean. Rejected library + Python i18n layer (new dep + 2 catalogs + Accept-Language plumbing), renderer-only (English verbatim in the advisory/forecast views), and ru-RU number localization. |
| 0068 | 2026-07-09 | **User chart-style settings — per-theme colours, line widths, candle series-type; [ADR-0062](../adrs/0062-user-chart-style-overrides.md) accepted at close (extends [ADR-0039](../adrs/0039-renderer-theming-localstorage.md)).** A Settings "Chart style" surface (requested 2026-07-08): per-theme **colour** + **line width** for the built-in series (candles, volume + volume-MA, VWAP, OBV, bull/bear/neutral markers) and the agent overlay lines (EMA, SMA), plus a **candle series-type** switch (candlestick / OHLC bars / line / area). Overrides persist in renderer `localStorage['ma.chartStyle']` and resolve through **one typed store** (`chartStyle.ts`, shaped like `theme.ts`): `resolveChartStyle(container, theme)` reads the per-theme CSS default tokens off the DOM, layers the user's per-theme overrides, fills width defaults, and returns a concrete `ResolvedChartStyle` — replacing `readChartColors`/`overlaySeriesColor` entirely (ADR-0062 option B; the old read path is **gone**, not wrapped). `CandlestickChart` consumes it for every drawn colour + width (live via `applyOptions`, **no remount**), and rebuilds the instance on a candle-type change (series type is fixed at creation; span+trendline primitives and markers re-attach). Overrides keyed by element **type** (not per-instance) and **per-theme**. Four `ui-builder` phases on `main` (`86baa2c` store+resolver → `886b1fb` chart wired → `958ce77` Settings section → `bd2b702` candle-type switch), no branch, migration-free. Clean Mode 4 — no blockers/majors/minors; every done-when read at the assertion level: store done-when a–e (no-override defaults+widths+`candles`; per-theme colour/width independence; candle-type+reset round-trip through storage; malformed/blocked-storage degradation; subscribe fires-then-unsubscribes); phase-2 spec drives a live EMA colour+width override applied via `applyOptions` with `createChart` called once (no remount) + a theme-flip honouring the override; phase-4 spec proves rebuild (2nd `createChart` + old `.remove()`), new main series is a line, primitives (`attachPrimitive ×2`)+markers re-attach; SettingsView specs cover per-active-theme writes, the "Editing <theme>" live region, reset clearing every override incl. candle-type, and up/down colour controls `disabled` in line/area. Benign deviations: `ResolvedChartStyle` gained a `chrome` field (one place for every drawn colour); controls extracted to `ChartStyleControls.tsx`; `rebuildToken: candleType` threaded into the four chart hooks; width control is a `<select>`. Close gates: **539/539 renderer jest** (59 suites), `typecheck` clean, `eslint` 0 errors. **Satisfies the 0068 half of Plan 0072 phase 8's gate** (only 0071 remains). |
| 0067 | 2026-07-09 | **Trendline pattern identification — colour by type, de-clutter, hover + grouped legend; [ADR-0061](../adrs/0061-trendline-pattern-identity-and-colour.md) accepted at close (amends [ADR-0049](../adrs/0049-chart-trendline-overlay-primitive.md)'s colour-by-role).** Follow-on to 0064: chart-pattern trendlines drew but a sweep painted 15–35 overlapping lines coloured by *role*, so you couldn't tell which line was which pattern (diagnosed live BTC-USD weekly+daily 2026-07-08). Identity was **already on the wire** (`TrendlineSpec.pattern`/`style`/`role`/`label`) → **renderer-only, no sidecar/wire change**. Three `ui-builder` phases on `main` (`7af295f` colour-by-type + `dedupeTrendlines` collapse of the forming/confirmed twin → `759ad1e` hover tooltip via the primitive's `hitTest` → `591dc3f` grouped legend, one row per (pattern type, state) with count + show/hide toggle + hover-highlight), no branch, migration-free. Style still encodes confirmed/forming; role colour semantics given up by design. Colour/dedupe/hitTest/highlight logic lives in `lib/trendlines.ts` + `useTrendlines` + `LayersPanel` (tooltip/legend share the display-name + state helpers — one source of truth), pushing away from the `CandlestickChart` god-component rather than regrowing it inline (the added interaction state is the ADR-0061-acknowledged cost, deferred to the Plan 0072 ph8 decomposition). Clean Mode 4 — no blockers, no majors, no minors; every done-when read at the assertion level, the component claims driving the real `CandlestickChart`: dedupe drops the dashed twin (forming-only/confirmed-only intact, order preserved); `trendlineColor` gives each of 9 pattern types a distinct hue + a stable neutral (both light `:root` and dark `[data-theme]`/`prefers-color-scheme` token sets); `hitTest` returns the spec near a line and `null` past the 5px tolerance; the crosshair-over-a-line test renders "Head & shoulders — forming"; the legend lists one row per (type, state) with count, uncheck removes exactly that group's lines (re-check restores), row-hover drives `setHighlightedGroup`/clears on mouse-out. Close gates: 510/510 renderer jest (64 across the four touched suites), `typecheck` + `lint` clean; no diagram referenced trendline colour. **Rejected** per-instance colour + a wire instance id (ADR-0061 alt A/D — 15–35 colours illegible; per-type grouping is renderer-derivable). Manual live checks are the user's outstanding step, not a code gate. **Satisfies the 0067 dependency for Plans 0071 (reuses the grouped-legend component) and 0072 phase 8** (`CandlestickChart` decomposition, still gated on 0068/0071). |
| 0066 | 2026-07-09 | **Advisor tiered-forecast unification — `recommend` and the `forecast` tool now agree at a shared horizon (no new ADR; realizes [ADR-0057](../adrs/0057-forecast-feature-set-tiers.md) in the advisor).** The advisor's forecast leg ran the single-horizon **v1 OHLCV-only** core (`_compute_forecast`) that Plan 0059 left "deliberately untouched", so `recommend` and the standalone `forecast` tool could disagree at the same horizon (the 0063 live smoke: forecast said no-edge on v2-deep at h=1 while `recommend`'s v1 shipped a directional long — two tools, two verdicts for one question). Fix: `_assemble_and_fuse` computes its one horizon through the **same** `_compute_multi_horizon_forecast` (`horizons=(horizon_bars,)`, `metric_lookup` threaded `create_mcp_components → register_recommend → _recommend_response`) and adapts the single block to the `ForecastResult` fusion consumes via a new `_as_forecast_result` (untrainable block → the prior `ValueError`; honest no-edge flows through) — inheriting the v2-full→v2-deep→v1 ladder, the stated fallback, and the 0063 explanation with **no duplicated logic**; `_compute_forecast` retired (its determinism/edge-label specs reconciled onto the multi-horizon core). The forecast basis gains `feature_set_id`+`fallback_reason` scalars (open `dict[str, BasisValue]` — no wire-pin move) and the Recommendation panel renders a readable tier line (a renderer hash→name map mirrored from `forecast/features.py`, unmapped→raw-hash graceful). Three phases on `main` (`339d951` dev → `012d5fe` dev → `b081270` ui-builder), no branch, migration-free. Clean Mode 4 — no blockers, no majors; one minor followup (the renderer `FEATURE_SET_LABELS` map has no cross-language pin — all three ids verified matching at close, ADR-0057-frozen). Every done-when read at the assertion level, incl. the store-wired unification test asserting the advisor leg **==** the forecast tool's core (identical tier/prob/skill + byte-identical adapter) — the 0063 divergence closed. Close gates: 87 Python + 17 renderer specs, `mypy --strict` + ruff clean. Phase 4 (human live smoke) was verified in the 2026-07-12 consolidated live smoke. |
| 0070 | 2026-07-09 | **Generated, CI-gated sidecar API reference — every MCP tool / REST route / SSE event, rendered from the live fully-wired app so it can't drift; [ADR-0064](../adrs/0064-generated-sidecar-api-reference.md) accepted at close.** New dev-owned `src/market_analyser/apiref/` boots the fully-wired sidecar in memory (a `_CapturingFastMCP` subclass swap captures the server the shipped `create_mcp_components` builds internally — one wiring source of truth, now **shared with** `tests/api/test_mcp_tools.py`'s full-toolset assertion) and introspects three surfaces into ordered frozen records: the FastMCP tool registry (42 tools), the FastAPI OpenAPI document (25 routes), and the event `TYPE_REGISTRY` (20 SSE kinds — the phase-1 open question resolved with **no new registry**, the one Plan 0064 added is consumed as-is). A deterministic renderer emits full-detail markdown under `docs/reference/` (index + mcp-tools + rest-api + events), one entry per command: description, params table (type/required/default from the JSON schema), return/payload shape (output schema, with a return-annotation fallback for FastMCP's `<name>DictOutput` wrappers), and a **file-level** source link (no line numbers → no churn). A `--check` mode re-renders in memory and fails on any diff, wired into CI beside `mypy`/`gen-types:check` and aliased `pnpm gen:api-docs[:check]`. Four `dev` phases on `main` (`ef770da` → `c087d6e` → `4c3dc03` → `ed908a8`), no branch, migration-free. Clean Mode 4 — no blockers, no majors, no minors; every done-when read at the assertion level (tool-name set **==** `EXPECTED_FULL_TOOLSET`; exact renderer block incl. required-positional `—` / optional `` `None` `` / no-params line / field-table-vs-fallback; **byte-identical across two independent wiring passes**; `--check` 0-on-fresh / 1-on-one-byte-edit both directions; full `forecast` entry in the committed tree). Close gates: `apiref --check` exit 0, 27 apiref+toolset specs, `mypy --strict` + ruff clean; Windows determinism confirmed (`docs/reference/*.md` resolves `eol=lf`). Discoverability landed in `README.md`, `docs/onboarding/`, and (at close) `CLAUDE.md`'s orientation map. Phase-4 live red-CI-on-drift demo is the user's optional confirmation (the in-process drift spec already proves it). |
| 0065 | 2026-07-09 | **Glossary hover-explanations — every forecast/recommendation/indicator term gets a dual-hat on-hover/on-focus card; [ADR-0060](../adrs/0060-glossary-tooltip-interaction-posture.md) accepted at close.** Every term/score/verdict on the Forecast + Recommendation tabs and the chart-legend indicator labels now discloses, on hover **and** keyboard focus, a two-line card — *how it's computed* (developer) over *what it means for your decision* (trader), the 0063 dual-hat framing. Content lives in one shared language-neutral `desktop/renderer/glossary/glossary.json` imported at build (never on the wire, ADR-0046 preserved); an accessible `<GlossaryTerm>` renders it (focusable span trigger — never a `button`/`link`/`[role=button]`; `aria-describedby` card portaled to `body`, hover+focus reveal, Escape-dismiss; unknown `termKey` degrades to plain text). A **Python cross-language accuracy test** (`tests/glossary/test_glossary_accuracy.py`) reads the same JSON by repo-relative path and structurally pins it to the code: each `formulaAnchor`→its canonical constant (conviction→`SHARPE_FULL_CREDIT` in `advisor/fusion.py`, `edge_strength`→`EDGE_MARGIN_THRESHOLD` in `api/mcp_tools/forecast.py` — a rename breaks the import), the `indicator`-category keys **bidirectionally** equal the frozen `FEATURE_NAMES`/`_V2`/`_V2_DEEP` union, and a fifth `overlay` category (chart-legend ema/sma/supertrend copy) kept **disjoint** from `indicator` so the feature-name pin is unaffected. The focusable triggers are a **deliberate scoped relaxation** of the panels' no-interactive-element posture (ADR-0060 — informational disclosure ≠ action): the 0063 no-interactive specs were **re-scoped, not deleted** — they still assert zero action controls **and** additionally that every `[tabindex]` addition is a sanctioned glossary trigger, so the ADR-0025/0029 no-*action* guarantee stays asserted. Three phases on `main` (`c1f1884` ui-builder → `f2a747d` ui-builder → `b2ce3d4` dev), no branch, migration-free. Clean Mode 4 — no blockers, no majors; three minor deltas, all deliberate and disclosed in the phase-2 commit body (component at `renderer/components/` not the plan's `glossary/` path; third surface is `LayersPanel`+`CandlestickChart` not the plan-named `OhlcvView`; the added `overlay` category). Every done-when read at the assertion level; close-time gates green: **5** Python accuracy specs + **45** renderer jest specs across the six touched suites. Phase 4 (human live smoke) was verified in the 2026-07-12 consolidated live smoke. |
| 0064 | 2026-07-08 | **Chart-pattern trendlines: render, decouple, recompute — detected necklines/triangle-wedge bounds now actually draw; [ADR-0059](../adrs/0059-trendline-event-channel-and-recompute.md) accepted at close.** Diagnosed live on BTC-USD 1h (2026-07-08): `detect_chart_patterns` computed trendlines and the specs reached the renderer (the "Trendlines" legend row showed) but no lines drew. Root cause (ph1, `9a93e48`, ui-builder): the primitive's time→x used grid-snapped `timeToCoordinate`, which returns `null` for any endpoint not exactly on a loaded bar — a projected neckline or a bound past the last bar was dropped whole. Fix: `resolveTimeX` falls back to the bar-grid **logical scale** (interpolate/extrapolate via `timeToFractionalLogical` + `logicalToCoordinate`, mirroring how `priceToCoordinate` extrapolates), the `null` skip surviving only for a genuinely empty grid (< 2 bars). Then the two structural fixes from the same session (ADR-0059): the **`chart.show` wipe race** and the **durability gap** — trendlines moved onto a dedicated **`chart.trendlines v1`** event (`ChartTrendlinesPayloadV1`, `VERSION=1`, `extra="forbid"`; the `trendlines` field **removed** from `ChartShowPayloadV1`/`ChartUpdatePayloadV1`), `detect_chart_patterns` now publishes that layer-only event like `highlight_pattern` (`5496a59`, dev), a new bearer-gated **`POST /scan_chart_patterns`** sharing the factored `_detect_chart_patterns_response` core (`4e80004`, dev), the renderer reducer active-chart-gated (`applyChartTrendlines` mirrors `applyChartHighlight`; `applyChartShow` **preserves** on same symbol/tf, **clears** on switch — `f3329d6`, ui-builder), and **recompute-from-current-bars** on chart mount + debounced range-settle + a manual "Scan chart patterns" button via the typed client (`495405b`, ui-builder) — trendlines stay derived, **never persisted**, no migration. Ph6 (`a08ef3c`, ui-builder) clarified in copy only that agent mode governs gesture forwarding, not overlay visibility. Six phases on `main`, no branch, migration-free; architect prep `915b4f6` corrected the ph1 root cause. Clean Mode 4 — no blockers, no majors, no minors; two cosmetic nits (component off-grid test asserts segment length only — endpoints pinned in the sibling `lib/trendlines.test.ts`; recompute hook's `[chartRef]`-only dependency relies on `setData`/`fitContent` emitting a range change on symbol swap, behavior correct). Every done-when read at the assertion level, including the **ph1 red→green gate** (off-grid neckline → exactly one segment `{x1:130,x2:280}`; pre-fix 0 segments), the pydantic field-removal (`model_fields` + `extra="forbid"`), `detect` publishing **exactly one** `chart.trendlines` (empty range → nothing), the route's **401-without-bearer** + tool-parity, and recompute mount/coalesce/manual/no-call-when-absent. Close-time gates green: 47 plan Python specs, 94 renderer specs, `mypy --strict` + ruff clean. A pre-existing tracked nit (`events.test.ts:3` stale `api/events` docstring path, a 0047 follow-up) sits in this file and stays owned there. |
| 0063 | 2026-07-08 | **Forecast & recommendation explainability — every forecast/recommendation says exactly why, for both hats; [ADR-0058](../adrs/0058-forecast-recommendation-explainability.md) accepted at close.** Forecast side: a new `forecast/explain.py` computes per-horizon **out-of-sample permutation importances** over the walk-forward's own scored folds (captured via a `scored_fold_sink` on `validate()` — the validated models, never a final full-data fit; seeded per fold, deterministic) into a full ranking (mean + per-fold spread) beside the predict-row's actual feature values and a fixed association-not-causation disclaimer; `ForecastProvenance` gains a compact `explanation` summary (top-N drivers + artifact path), the full JSON persisted under `runs_dir/forecast/…`. Recommendation side: `fuse()` emits `basis.checks` — a structured trace of **every gate** (leg, check, threshold, actual, passed) in fixed order, on directional and flat verdicts alike, replayable line by line (`directional ⟺ every check passed`); the full verdict + per-leg inputs persisted under `runs_dir/advice/…`. Both wire pins (the 0061 provenance pin, the ADR-0029 recommendation pin) moved **deliberately**; both panels render the "why" (Forecast "Why" disclosure with driver bars + freshness + artifact path; Recommendation checks table, pass/fail as text). Delivery split per ADR-0058: summary inline (small-wire, ADR-0046), full explanation off-wire under `runs_dir`; `runs_dir` unwired → summary still rides, artifact skipped, determinism preserved. Three phases on `main`, no branch, migration-free: `2941363` (dev) → `6ec2618` (dev) → `4d8f641` (ui-builder), plus post-review fix-forward `31c6d9d` (shared artifact writer extracted to `mcp_tools/_artifacts.py`; `_build_checks` docstring corrected — the two Mode 4 items). Clean Mode 4 — no blockers, no majors; every named done-when read at the assertion level (OOS-only spy, replay invariant across directional/one-blocker-flat/all-legs-fail, determinism re-run modulo the two run-provenance exceptions). Close-time gates: 110 Python + 53 TS specs green, `mypy --strict` + ruff clean. **Phase-4 live smoke through the running sidecar (2026-07-08):** `forecast BTC-USD 1d` → per-horizon top drivers inline + a plausible v2-deep artifact (24 features ranked; h=21 leans adx/atr_pct/dist_200w_ma/funding_rate/mvrv; honest no-edge at all three horizons), a second identical call byte-identical modulo `started_at`+path; `recommend` BTC-USD/rsi → directional long, 13-gate trace replays to `long`, conviction 0.0666 (P×edge) honestly low, artifact numbers matching the wire; Forecast "Why" panel confirmed rendering. Followup: the advisor's forecast leg still uses the single-horizon v1 core (`_compute_forecast`), not the tiered multi-horizon path — unifying them is a future plan, not a 0063 defect. |
| 0062 | 2026-07-07 | **Tiered forecast feature sets — the first exogenous feature set ever to train; [ADR-0057](../adrs/0057-forecast-feature-set-tiers.md) accepted at close.** The conjunctive v2 join's structural starvation (dominance/OI accrual-only series vetoing every historical bar) is answered by the ADR-0057 ladder: **v2-deep** (24 features — v2-full minus the 3 accrual-fed ones; F&G/funding/MVRV + cycle, its own frozen `feature_set_id`, the two existing ids pinned unmoved) + **richest-first selection** `v2-full → v2-deep → v1` in new `forecast/tiers.py` (exogenous tiers gated on `max(2·n_splits, MIN_TIER_ROWS=500)` surviving rows, floor asserted at the exact 500/499 boundary; one shared exogenous column set for all tiers), with the Plan 0061 honesty property now per rung — `fallback_reason` carries the full skip chain (each skipped tier named with its row count; wire-absent when v2-full trains, byte-stability re-asserted) and `series_inputs` names exactly the selected tier's series. Two `dev` phases on `main` (`126a87c` → `c0050cf`), no branch, migration-free. Phase 3 (`backtester`) finally ran the 0059-blocked v1-vs-v2 comparison (`runs/analysis/2026-07-06-plan-0062-v1-vs-v2deep/`, + a beyond-plan `v1-deep-sample` ablation isolating the window confound): production ladder selects v2-deep (2,491 rows from 2019-09-11); v1 full-window no-edge repeats; **v2-deep passes the gate at all three horizons** — h=1/h=5 marginal (noise-level), h=21 clear (+0.0728; +0.0331 of it pure feature signal on the identical sample). Phase 4 verdict (user, 2026-07-07, after a pre-close `n_splits`/seed robustness sweep at `runs/analysis/2026-07-07-plan-0062-h21-robustness/`): **promising, not proven** — the h=21 baseline beat is fold-layout-sensitive (no_edge/clear/marginal across n_splits 4/5/6; the layout decides the scored window and its baseline), the identical-sample feature contribution steadier (+2 to +3.3 pts in 2 of 3 layouts; seeds a non-axis, the fit is deterministic). No new-sources plan; re-run when accrual widens the deep window / v2-full crosses the floor. Clean Mode 4 — no blockers, no majors; close-time gates: 38/38 plan specs, 1908 passed / 7 Win-skips (lone red = the standing calendar-blind Yahoo `network` smoke), `mypy --strict` + ruff green. **Unblocks 0063** (the explainability hold released). |
| 0061 | 2026-07-06 | **Metric-store self-warming + honest starved-forecast fallback — drafted, implemented, smoked, and closed the same day; [ADR-0056](../adrs/0056-self-warming-metric-store.md) accepted.** Both halves of the 2026-07-06 production finding (all five exogenous series at zero points; a wired-but-starved store shipping `beats_baseline=false` with `n_scored=0` as if it were an evaluated verdict) are dead. (1) `data/metric_accrual.py`: a lifespan-managed `MetricAccrualJob` (ADR-0055 pattern — constructed only when persistence is wired and the flag is on; cancelled on shutdown) with a **tick-first boot posture** (a cold store starts warming at boot, not one interval later), topping up all five `EXOGENOUS_SERIES_IDS_V2` series — full-history backfill for F&G/funding/MVRV (spy-pinned `start=None` cold / `start == latest stored ts` warm), the Plan 0056 seed-then-accrue for OI, the CoinGecko write-through bucket for dominance — with per-series containment (one raising source leaves the other four accrued; the error named in the heartbeat, cleared on recovery) and a `MetricAccrualHeartbeat` on `/healthz`. `AppConfig` gains `metric_accrual_enabled` (default true, the ADR's on-by-default) + `metric_accrual_interval_seconds` (3600); `create_app`'s own parameter defaults **disabled** — a documented inversion (the job ticks at startup; an engine-wired test app must never reach the network), with `__main__` passing the config through. (2) The `forecast` tool's v1-fallback trigger extends from "store unwired" to "wired but the v2 join leaves fewer usable rows than `_v2_min_usable_rows(n_splits) = 2·n_splits`"; the new `ForecastProvenance.fallback_reason` states why on **both** paths, `exclude_none` keeps a genuine v2 dump byte-stable (exact wire field-set pinned); the Forecast panel footer renders the reason (`.nullish()` Zod + one muted sentence, real-dispatcher spec + a no-regression footer spec). Three phases on `main` (`ad509c6` dev → `85f4c9d` dev → `70865ec` ui-builder), no branch, migration-free — plus two phase-4 live-smoke fix-forwards on the OI seed, both upstream behaviors probed live and pinned in the offline fake transport: `41db964` (Binance `openInterestHist` now 400s any paged startTime ≥ 720h old — seed window shrunk to (30·24−1)h) and `6746334` (a past-end cursor now echoes the newest row instead of answering empty — read as end-of-data past the probe-anchored latest; the stuck-cursor guard holds mid-window). Clean Mode 4 — no blockers, no majors; every named done-when read at the assertion level; **close-time live verification through the running sidecar**: all five series warm (F&G 3074 / funding 7475 / MVRV 5303 full backfills; OI 500 hourly buckets ≈ the ~21d real retention; first dominance bucket 2026-07-06T16:00Z) and a live `forecast BTC-USD 1d` returned genuinely-scored v1 blocks (`n_scored` 1025/1021/1005) with the insufficient-history reason stated. Close-time suite: 1894 passed / 7 Win-skips (lone red = the standing calendar-blind Yahoo `network` smoke, a 0041-close follow-up). **The v2 evaluability clock is now running** — the `runs/analysis/2026-07-06-plan-0059-v1-vs-v2/` re-run stays a calendar follow-up (months of dominance/OI accrual needed). Two phase-4 residuals are time-gated and self-evidencing, not gates: the second dominance/OI bucket after the next hour boundary, and the off-switch reboot check. |
| 0037 | 2026-07-06 | **Forecast UI surface — Pillar 1 (Forecasting) is complete** (0036 foundation → 0059 v2 features → 0037 UI). The forecast becomes visible: new `forecast.completed v1` SSE event (`ForecastCompletedPayloadV1` in the events core, the full `MultiHorizonForecastResult` inline — small and ephemeral, the `signal.evaluated`/`recommendation.completed` rationale) published by the `forecast` tool **exactly once per successful run, strictly after the result is built** — every raise above the publish leaves the bus untouched (pinned in both failure classes: input validation and empty bar fetch), and a no-edge horizon travels inside its block with `prob_*` wire-absent (`exclude_none`), the event never suppressed. Viewer: a Forecast nav tab + reactive read-only view (the 0026/0039/0060 pattern — App-level state from `useEventStream`, deliberately **no auto-switch**: a probability must not grab the screen) rendering one block per horizon: up/down/flat probability bars, skill-vs-baseline pair with margin, edge-strength badge, per-block provenance tooltip + the call-level feature set with the **v1 fallback stated out loud**. The honest-uncertainty contract (ADR-0030) is specs, not polish: a `beats_baseline: false` block renders an explicit "no edge over baseline" state and **zero probability bars**; a mixed event renders edge and no-edge side by side (asserted); emphasis requires BOTH a clear edge and a decisive probability — a 0.52 renders quietly even under a clear edge (asserted). The payload is Zod-validated in the dispatcher (`safeParse`, loud drop), the Zod schema `satisfies`-pinned to the TS mirror, the mirror parity-guarded against pydantic (7 new schema assertions over the nested result models). Two phases on `main` (`6f3409e` `dev` → `89bdc90` `ui-builder`), no branch, migration-free, **no new ADR** (renders ADR-0030/ADR-0054's artifact on ADR-0017's stream). Clean Mode 4 — no blockers, no majors; every named done-when read at the assertion level; gates re-verified at close (`test_forecast_tool.py` 22/22; ForecastView + events-parity jest green; implementer's full runs: pytest 1873, renderer jest 418 across 50 suites). Component-map diagram refreshed at close (`forecast.completed` in the event vocabulary + Forecast viewer tab, plus the stale 41→42 tool count / missing `portfolio_summary` left over from the 0041 close). Followups stand in the plan: optional on-demand `GET /forecast` refresh; forecast history/timeline. |
| 0041 | 2026-07-06 | **Cross-venue portfolio aggregation — Pillar 4's core; [ADR-0042](../adrs/0042-cross-venue-portfolio-aggregation.md) accepted at close.** New top-level `portfolio/` package + the read-only `portfolio_summary` MCP tool (42nd in the roster) folding three legs into unified holdings + average-cost basis + unrealized P&L + signed USD exposure by asset/venue: **Binance** (read-only account adapter over `/api/v3/account` + `/fapi/v2/positionRisk`, HMAC-signed reads, `binance_read_api_key`/`_secret` in the [ADR-0038](../adrs/0038-third-party-api-key-storage.md) store — deliberately not the Pillar-5 trade keychain; typed auth/geo/throttle taxonomy with 451 pinned PERMANENT per ADR-0052; **read-only pinned by an AST scan** — no HTTP write verb, only the two read URLs), **DeFi** (existing discovery, remaining basis joined by `position_id` from the **ADR-0036 replay reused verbatim** — the tool spec drives the real engine through a seeded in-memory `DefiTxRepository` and asserts the hand-worked 4900 basis), and a **manual positions file** (gitignored `positions/portfolio.json` — JSON not YAML, a documented reasoned deviation; required user-maintained `as_of` + per-row override, missing file = empty source, malformed row named by index+symbol, `extra="forbid"`). Provenance is structural: every holding carries its own `as_of` (freshness never blended; manual leg pinned to its **oldest** row) and `usd_value`⇄`pricing_source` are model-validator-paired (no single-oracle pretense); unpriced holdings stay visible and never enter exposure as zeros; shorts/borrows are signed-negative net. Aggregation is pure/deterministic (no clock/network/set iteration; `queried_at` injected) — byte-identical dumps pinned across runs *and* price-map insertion orders. Per-leg containment (the `multi_timeframe` lesson): a failing leg lands typed in `leg_errors` while the others aggregate. **No advice, pinned**: word-boundary scan + exact field-set pins (ADR-0029). Three `dev` phases on `main` (`18816b9`→`95d62d0`→`00b3ffe`), no branch, migration-free. Clean Mode 4 — no blockers, no majors; every named done-when read at the assertion level; 49 plan specs + full suite green at close (1878 passed; the lone red is the pre-existing calendar-blind `network` Yahoo smoke → new follow-up row below). Two minors carried to the plan's Followups: `exposure_by_asset` keys venue-native holding symbols (BTC vs BTCUSDT never merge — Plan 0043 should label it "by holding"; net-underlying view needs a normalization map) and the CWD-relative default manual path (a sidecar launched outside the repo root reads the leg as absent, surfaced by a note). **Unblocks 0042 (risk over these holdings) and 0043 (UI).** |
| 0059 | 2026-07-06 | **Forecast feature-set v2 + multi-horizon — the crypto intelligence program's join; [ADR-0054](../adrs/0054-exogenous-forecast-features-multi-horizon.md) accepted at close.** The 16-feature pipeline gains 11 features: 4 cycle (halving phase/days, Mayer, 200W-MA dist — trailing, from `analysis/cycles.py`) + 7 exogenous (F&G ±Δ7, dominance ±Δ7, funding, OIΔ7, MVRV) read through a new `forecast/exogenous.py` **lag-1 as-of join** — the ADR-0051 `as_of` primitive bounded at `bar_open − 1s` (strict `ts < T_open`, so a point stamped *at* the open is invisible to that bar; publication-lag lookahead structurally impossible, **asserted directly against the real `MetricPointsRepository`** plus a future-point byte-identity perturbation test). Missing exogenous → NaN → **row dropped, never zero-filled**; `FEATURE_NAMES_V2` frozen (27), v1's hash pinned as a literal and unmoved; delta features derived in-pipeline (the plan's open question, resolved as defaulted). Horizons `{1, 5, 21}` on `1d` (next-bar elsewhere; `horizons=[...]` override): **one shared feature matrix per call** (safe across folds by the truncation-invariance guarantee), then per-horizon independent label/purged-walk-forward/baseline-gate/train — independence proven by a genuine 1-bar signal + seeded-shuffled 21-bar labels shipping h=1 probabilities and h=21 no-edge **in the same call**; the purge test now parametrizes h=21 (the widest overlap). Bonus catch: **`label_params` folded into the ADR-0040 `model_version` hash** (two horizons over the same features previously collided) with the change matrix extended; artifacts carry `label_params` in `meta.json`. Tool returns `MultiHorizonForecastResult` (per-block prob/validation/edge/provenance; provenance gains `series_inputs [{series_id, last_point_ts}]`); **no metric store → v1 set, stated not silent** (feature_set_id + empty series_inputs, live-server asserted both ways). Three `dev` phases on branch `plan-0059-forecast-feature-set-v2` (`30fd6de`→`f1942f3`→`a1080e4`), merged `--no-ff`; phase 4 `backtester` honesty artifact `runs/analysis/2026-07-06-plan-0059-v1-vs-v2/`. Clean Mode 4 — no blockers, no majors; every named done-when read at the assertion level. Merged-tree suite: **1830 passed / 7 Win-skips** (one unrelated `network` Yahoo smoke flake — July-4 holiday calendar). **Phase-4 read, recorded as it came out:** v1 has **no edge at any horizon** on 11.4y of BTC-USD 1d (majority baseline wins, growing with horizon); **v2 was unevaluable — the production metric store has 0 points in all five exogenous series** (the plans-0055/0056/0057 accrual is not populated in this deployment; the tool's honest v1 fallback is what's live). Follow-ups: investigate/warm the metric store, re-run the comparison when series accrue, backfill stale BTC bars — see the plan's Followups. Worktree + branch pruned at close. |
| 0035 | 2026-07-05 | **DeFi P&L reconstruction — ADR-0036's transaction-replay method end to end; [ADR-0036](../adrs/0036-defi-pnl-reconstruction.md) accepted at close.** The whole vertical landed: **ingestion** (boundary-validated `DecodedTx` + `TxHistorySource` Protocol; `ZerionTxAdapter` following `links.next` to completion, spaced 1.1s/page, deterministic `(block, in_block_index)` ordering; **immutable `defi_tx` cache, migration `0006`** with gap-fetch — a warm re-scan issues zero Zerion fetches, asserted by a recording fake) → **pricing** (DefiLlama `HistoricalPriceSource`, keyless; **first-write-wins `price_snapshots` cache, migration `0007`** — the determinism mechanism, pinned by a snapshot-survives-upstream-revision test; garbage upstream prices never snapshotted; missing price = typed `None`, never `0.0`) → **taxonomy mapping** (`DecodedTx → PositionEvent`, precision-first join via act contract then single-candidate token fallback; unrecognized shapes surface as exactly one `unclassified`, never dropped) → **average-cost replay engine** (hand-worked figures pinned in the spec docstring; a missing price marks the position `incomplete` with the offending leg named and realized asserted **not** `0.0`; vs-HODL for LPs; unbooked v1 kinds — swap/liquidation — force honest `None` wallet totals; byte-identical re-run) → **surface** (`pnl_job` + masked-wallet `defi.pnl_*` SSE, `compute_wallet_pnl` tool + bearer-gated `POST /defi/pnl`, typed error→status map 422/400/429/502; Zerion `/pnl` as a best-effort advisory cross-check — gross-divergence/sign-flip flags only, a failing cross-check never fails the reconstruction). Phase 7 also landed the **exhaustive 41-tool `EXPECTED_FULL_TOOLSET` registration test** — closing the standing 0028-audit "no safety-net registration test" follow-up (and the 0050 `get_backtest` carry-over). Seven `dev` commits on `main` (`03861c7`→`8686496`, no branch) + smoke hardening `dfa7eb6` (NFT `nft_info` transfers skipped-not-fatal; zero-quantity position entries dropped as nothing-held — both fixture-encoded offline). **Phase-8 live smoke 2026-07-05:** cold run 52.9s (full multi-chain history, no 429s), warm 1.9s with zero Zerion fetches; 1 of 5 positions fully reconstructed (QuickSwap staking: realized **+$4,015.16**, basis $873.50), 4 honestly `incomplete` via the designed loud paths (unbooked swap + unclassified Aerodrome gauge shapes; one uncovered long-tail DefiLlama price, leg named); wallet totals honestly null per ADR-0036; Zerion FIFO cross-check $29,670.07 (survey saw ≈$29,193), no warning — and a Zerion-side 503 on the first attempt was absorbed best-effort as designed; realized/cost-basis/notes byte-identical across runs (unrealized rides live discovery `usd_value` — the plan's documented design, not a replay leak). Clean Mode 4 — no blockers, no majors; every named spec read at the assertion level; gates re-run at close (**1795 offline tests**, `mypy --strict` 382 files, ruff, `gen-types --check`). Migrations `0006`+`0007` form one linear chain after `0005_watches_alerts` — **the chain head is free again; 0044 is next in line**. **Unblocks the portfolio chain 0041 → 0042 → 0043** (0042's prereqs 0034+0035 both closed). Follow-ups: Aerodrome gauge hint vocabulary + swap booking (together would flip the 3 incomplete LPs to complete) — see Open follow-ups. |
| 0060 | 2026-07-03 | **Watchlist alerting loop — the app's first unprompted voice; [ADR-0055](../adrs/0055-in-sidecar-watch-scheduler.md) accepted at close.** Persisted watches (`watches` + `alerts` tables, migration **`0005_watches_alerts`** — the chain's head moved; next slots: 0035 → 0044) evaluated by a **lifespan asyncio scheduler**: each tick lists enabled watches, **coalesces bar fetches per (symbol, timeframe)** (upstream exposure scales with distinct series, asserted), runs pure evaluators (indicator threshold / pattern / strategy signal — each proven to read only the latest **closed** bar, forming-bar no-lookahead pinned per kind), folds through the edge reducer against persisted `last_state` (full transition matrix + a folded poll sequence: exactly one fire while true, re-fire only after re-arm; `last_state` survives restart via a **fresh engine over the same SQLite file**), and on a false→true edge persists the alert row + publishes one **condition-only** `alert.triggered v1` envelope to both delivery legs (SSE, ADR-0017; UI-event buffer for agent polling, ADR-0021). **The ADR-0029 boundary is pinned structurally**: the payload's exact field set is asserted, recommendation-shaped fields (`direction`/`action`/`conviction`/`stop`/`target`/…) proven absent, `extra="forbid"` — and the renderer's Zod schema is `.strict()` (a smuggled `action: "buy"` is dropped at the SSE boundary with a logged warning). Failures degrade loudly, not quietly: per-watch evaluator errors and per-series fetch failures are contained (sibling watches still fire, asserted) and surface in the `SchedulerHeartbeat` on **`/healthz`**. Four MCP tools (`create_watch` with deep creation-time boundary — strategy resolution, timeframe support, params validation, bar-period interval default; `list_watches`; `delete_watch`; ADR-0046-paged `list_alerts`) + three bearer-gated renderer routes (`GET /watches`, `POST /watches/{id}` enable/disable, `GET /alerts`) + an Alerts view (newest-first history, live-vs-fetched dedup on `(watch_id, fired_at)`, disable round-trip, honest empty/error states) and a most-recent-wins toast. Five commits on branch `plan-0060-watchlist-alerting-loop` (`7b2ba85`→`e40e5eb`, phases 1–4) **including a mid-plan `merge main into plan-0060` that integrated 0039's renderer changes before phase 4 — the fan-out stagger worked as designed; final merge `--no-ff` conflict-free.** Clean Mode 4 — no blockers, no majors; every named done-when read at the assertion level. Post-merge gates green: **pytest 1681 passed / 7 Win-skips**, renderer jest **404/404** (49 suites), `mypy --strict`. Implementer smoke: real sidecar booted (temp data dir, `--port=0`), scheduler ticking on `/healthz`. Bonus catches: **unanchored `models/` gitignore bug** fixed (was swallowing `persistence/models/`); dev-skill `--secret` doc drift noted (actual contract: `MARKET_ANALYSER_SECRET`). v1 scope holds: no forecast watches, no OS notifications, no advice in alerts, bar-close semantics only. Worktree + branch pruned at close. |
| 0039 | 2026-07-02 | **Advisor UI surface — Pillar 2's viewer half; the recommendation becomes visible, unmistakably advisory.** New `recommendation.completed v1` SSE event (`RecommendationCompletedPayloadV1` in the events core, full `Recommendation` inline — small and ephemeral, no follow-up fetch) published by the `recommend` tool **exactly once per successful fusion, strictly after `fuse()`** — any raise above the publish leaves the bus untouched (pinned in both failure classes: pre-validation and post-validation), the `signal.evaluated` discipline. Viewer: a Recommendations nav tab + read-only view rendering direction, conviction (derived number + strength band; **a low-conviction call is styled quietly**, asserted), rationale, the four-component basis breakdown (an absent flat-leg is *said out loud*, never hidden), and entry/stop/target as explicitly-advisory levels. **The ADR-0025 boundary is a spec, not a guideline:** the view contains zero interactive elements (`button/input/select/textarea/a/[role=button]` count == 0) and no action language; the advisory banner is asserted *first-child-prominent*. The SSE payload is **Zod-validated in the dispatcher** (`safeParse`, loud drop — the first event to get the treatment the 2026-05-31 audit asked for) with `label: z.literal('advisory')` at the renderer edge, and the TS mirror is parity-guarded against pydantic with the advisory label pinned as a single-value `const`. Reactive-only posture, deliberately **no auto-switch** — an advisory call must not grab the screen. Open question resolved: levels stay in the panel; the agent pushes labeled `price_line` overlays through the existing chart channel when wanted. Two phases on branch `plan-0039-advisor-ui-surface` (`9774ae0` `dev` → `25e5ab1` `ui-builder`), **merged `--no-ff` conflict-free** (disjoint from the concurrent 0038-close commits on `main` — the worktree fan-out's first pair worked as designed). Clean Mode 4 — no blockers, no majors; every named done-when read at the assertion level (exactly-one-envelope success both directional and flat; zero-on-failure both classes; the no-control spec; low-conviction styling; malformed-payload drop through the *real* dispatcher). Post-merge gates green: full offline pytest, renderer jest **385/385** (46 suites), `mypy --strict`. **No new ADR** (renders ADR-0029's artifact; ADR-0025 boundary held). Migration-free; worktree + branch pruned at close. |
| 0038 | 2026-07-02 | **Advisor layer — Pillar 2's core, the one sanctioned ADR-0029 crossing of "conditions are facts, decisions are the user's".** New `advisor/` package: frozen `Recommendation`/`RecommendationBasis` models enforcing the containment rules *structurally* (empty basis ⇒ `ValidationError`; a directional call must carry non-empty rationale + backtested basis + forecast basis + full entry/stop/target levels; a flat call carries no levels and zero conviction — "no actionable edge" can never look like a trade ticket) + a pure deterministic `fuse()` where **every leg must agree** (forecast argmax direction, non-conflicting live-signal votes in the same direction, positive walk-forward edge belonging to an agreeing strategy) or the verdict is an honest flat with each failed leg a named blocker. **Conviction is derived, never invented**: `P(direction) × clamp(sharpe_mean/1.0, 0, 1)` — the plan's open question resolved as a documented monotone product, pinned exactly and directionally by tests. Levels are chart geometry (±0.25 ATR entry band; stop beyond the opposing S/R level +0.1 ATR buffer, 2 ATR fallback; target at the favouring level, 2 ATR fallback). The `recommend` MCP tool assembles all four legs from **one shared closed-bar series** (single as-of bar — no leg peeks past another; `fuse()` re-validates symbol/timeframe/as-of alignment), returns the labeled advisory artifact or the honest flat. Two `dev` phases on `main` (no branch): `47e7ac9` → `339885e`, plus fix-forwards `97099a5` (hoisted `ForecastResult` into `forecast/result.py` — the advisor consumes a domain surface, not a tool module) + `6455786` (as-of alignment + walk-forward strategy-tie validation). Clean Mode 4 — no blockers, no majors; every done-when read at the assertion level: the full validation matrix in `test_models.py`, the exact conviction product + saturation, dump-equality determinism, the AST import-lint (advisor imports analyst *outputs* only), the source-level **no-key/no-order/no-network** scan over advisor + tool (forbidden tokens and imports — the ADR-0025 boundary held), per-blocker flat rationale, live-toolset registration. One honestly-documented deviation: registration lives in `mcp_app.py` (Plan 0017 consolidation), not the plan's stale `mcp_tools/__init__.py` file list. 1591 passed / 7 Win-skips + `mypy --strict` (341 files) + ruff green. **ADR-0029 accepted at close.** Migration-free. **Unblocks 0039** (advisor UI — its worktree is already cut). Companion work (the `advisor` skill; the `defi-analyst` rebalance-charter reconciliation) landed same-day via `skill-creator` — both done. |
| 0057 | 2026-06-15 | **On-chain valuation: MVRV — the crypto program's cost-basis lens, reshaped MVRV-only.** Keyless `CoinMetricsCommunityAdapter` (paced under the documented 10 req/6s) backfills full daily MVRV (`CapMVRVCur`, to 2011-12-29) into one ADR-0051 series `coinmetrics.btc.mvrv`; `btc_cycle_snapshot` gains `mvrv` + a trailing full-history `mvrv_percentile`, the on-chain lens beside the price-only Mayer/200W reads. **Phase-1 `human` probe (2026-06-14) reshaped the plan:** only MVRV is free keyless — realized-cap + SOPR returned `forbidden`, the blockchain.com cross-check endpoints are gone (`not-found`), SOPR-via-bgeometrics (4y window, 10 req/hr) declined; **phase 3 (cross-check) CUT.** Phases on `main` (no branch): phase 2 adapter+backfill `ebfe335`, phase 4 snapshot fields `d439928`, **phase 5 wire-the-population-path `50a84ee`** — added at the 2026-06-14 Mode 4 review as the close gate (phases 2+4 alone left the series unpopulated in production: the adapter was constructed nowhere and `backfill` was reachable only from tests, so `mvrv`/`mvrv_percentile` were permanently `None`). Phase 5 mirrors the `derivatives_snapshot` refresh precedent — `BtcCycleSnapshotInput` gains `refresh: bool = False`, the tool wrapper backfills MVRV from the latest stored ts forward (full history when empty) before reading, and `mcp_app` default-constructs the adapter network-free. Clean Mode 4 close — no blockers, no majors; phase 5 read at the assertion level: `refresh=false` never calls the source (`spy.calls == []`), `refresh=true` on an empty series fetches with `start: None` and the points persist + surface, `refresh=true` on a warm series fetches incrementally (`spy.calls[0]["start"] == seeded_ts`), the composition root default-constructs `CoinMetricsCommunityAdapter` offline (exercised by the full-toolset registration test), and the `_build_snapshot` trailing-only / exact-value tests are untouched (refresh lives in the wrapper, not the builder). 15/15 `test_cycle_snapshot_tool.py` + `mypy --strict` + ruff green. **ADR-0053 accepted at close** (reshaped MVRV-only; CoinMetrics-community-primary decision held). Migration-free, no branch. **Unblocks 0059** — its exogenous feature list names MVRV, not the cut SOPR/realized-cap, so 0057's reduction leaves 0059 whole. |
| 0054 | 2026-06-14 | **Chart-pattern breakout strategy + backtest — the chart-pattern initiative's join (0052 detection × 0053 shorts).** A contract-conformant `chart_pattern_breakout` strategy maps each **confirmed** `ChartPatternHit` to a directional entry at its confirming bar — **long** on bullish breaks, **short** on bearish — reading direction off the hit's own resolved `direction` field (so symmetrical triangles/wedges, whose direction is known only at the break, stay in lockstep with the detector for all nine patterns). Acts only on `confirmed` hits, never `forming`, so ADR-0048's trailing lifecycle carries the no-lookahead guarantee straight to signals. Exit policy is a `Params` choice (default: stop-and-reverse on the opposing confirmed breakout; optional measured-move target + stop); `long_only` and a per-family triangle/wedge toggle default to both-directions / enabled. Two phases on `main` (no branch): `f87f62d` (`strategy-author`) + `48eaa64` (`backtester`, backtest + walk-forward). Clean Mode 4 — no blockers, no majors; every named done-when read at the assertion level: phase-1 entries pinned at the *confirming* bar (`> _forming_bar`, both direction and confirming-bar read off `detect_chart_patterns` as the single source of truth, not a hardcoded index), forming-only fixture → no signal, a genuine truncation-invariance no-lookahead test (re-runs on every prefix), `discover()` lookup; phase-2 mixed fixture yields **both** a long and a short trade (long-before-short), determinism via `model_dump(exclude={run_id,started_at,finished_at})` equality, walk-forward per-fold + aggregate with contiguous strictly-increasing folds (anti-lookahead). Strategy imports only `analysis.chart_patterns` + `contracts` (no layering violation), pure/deterministic/trailing, 190 lines. The phase-1 `test_cli.py` roster bump (`seven → eight`) is an in-scope, commit-documented consequence of registering the 8th strategy. 13 plan tests + `mypy --strict` + ruff green; full suite 1505 pass / 7 Win-skips (the lone red — `test_settings_stop_removes_lockfile_on_windows` — passes in isolation: a pre-existing order-dependent flake in the Windows sidecar-lockfile suite, unrelated to this plan). **No new ADR** (consumes 0048/0050; applies 0004/0018/0024). Migration-free, no branch. **With 0054 closed, the chart-pattern initiative (0051–0054) is complete.** |
| 0058 | 2026-06-13 | **Binance klines as a second OHLCV source — the app's first crypto-exchange OHLCV.** Keyless `BinanceKlinesAdapter` over spot `/api/v3/klines` (paginated absolute-window fetch, 1000/page, native `4h`, typed errors incl. 451→`GeoRestrictedError`) + **exchangeInfo-membership routing** in `DefaultMarketDataProvider` (symbol → Binance iff in the cached symbol set, else Yahoo; `BTCUSDT` ≠ `BTC-USD`, single-provenance by construction) + per-source uncapped history (Binance skips Yahoo's 730d/60d intraday caps) + native-4h (no resample) + Binance pairs in `search_symbols` (source-labeled, dedup-after-Yahoo). Three `dev` phases on branch `plan-0058-binance-klines-ohlcv` (`4b0526b`→`6c5779b`→`ac4d3d3`), **merged `--no-ff` as `0260c5d`** (conflict-free; main had only the 0053 close + a sidecar-lockfile bugfix since the base — disjoint files). Clean Mode 4 — no blockers; every named spec read at the assertion level (3-page dedup/contiguity, absolute-window verbatim, typed bad-row/451 one-attempt, per-source cap both ways, native-4h spy-no-resample, membership-cache stale-but-present + lazy-fetch + failed-fetch-memoizes-empty, search ranking, cache round-trip, ADR-0033 empty split). Post-merge `main` green: **1476 passed / 7 skipped**, `mypy --strict` clean (326 files). **Phase-4 live smoke GREEN (2026-06-13):** backfilled `BTCUSDT 1h` 2017→now = **77,197 bars, earliest 2017-08-17T04:00:00Z** (Binance spot listing), ~9 years past Yahoo's 730d cap; no geo block (shared GREEN with 0056); routing/cache/native-4h/search/`analyze_symbol`-snapshot/chart-render all confirmed live. **ADR-0052 accepted.** **One follow-up surfaced** (live-quote path not Binance-aware → "disconnected" price header for Binance-only symbols) — logged in Open follow-ups. Migration-free; no branch left (pruned at close). |
| 0053 | 2026-06-11 | **Short-selling support — the strategy + backtest contract goes flat/long/short.** `SignalKind` gains `enter_short`/`exit_short` (full kind set pinned); `signals_to_trades` generalized to a single-direction-at-a-time state machine with a load-bearing **stable sort `(bar_index, exits-before-entries)`** so ADR-0050's same-bar flip executes exit-first-then-enter at the same next open **regardless of emission order** (pinned in both directions); equity curve values a signed position via `_position_value` — short = `entry_cash + units·(entry − close)`, frictionless v1 — while the long branch keeps the **unrearranged `units · price` expression so the long path is bit-identical in IEEE 754** (the merged golden fixture moved by *exactly* the `engine_version` field, `0.2.0 → 0.3.0` — the no-silent-long-regression pin); costs mirrored to always hurt (`sign = ±1`, `+1.0`-multiply exact); per-trade returns/win-rate direction-signed; new Calmar guard for short-driven `equity ≤ 0`; live-signal evaluator folds flat/long/short with the same adapter ordering. Three phases on branch `plan-0053-short-selling-support` (`a39b196` `dev` contract → `96478eb` + `84715a6` `backtester`), **merged `--no-ff` as `607c1c6`** (conflict-free — main hadn't moved). Clean Mode 4 — no blockers, no majors; one minor (backtester synced the renderer's `trade.ts` `kind` union — correct, ui-builder's domain) + one nit (cross-module private `_ordering_key` import). Every named spec read at the assertion level (hand-worked per-bar equity values; short-bearing full metric set against a hand-checked fixture; engine e2e short net of mirrored costs; the `SignalKind`-exhaustiveness open question closed as "no" by grep). **No migration — the chain stays free.** 1422 tests + 7 known Windows skips green on the merged tree. **ADR-0050 accepted.** **0054 (breakout strategy) is now fully unblocked.** |
| 0056 | 2026-06-10 | **Binance derivatives data — funding rate + open interest, the app's first derivatives signals.** Keyless `BinanceDerivativesAdapter` over `fapi.binance.com` (`ResilientHttpClient` subclass pinning **HTTP 451 → `GeoRestrictedError`, `PERMANENT`, never retried** — ADR-0052's never-improvise rule): paginated **full funding-rate backfill** (`binance.funding_rate.BTCUSDT`/`.ETHUSDT`, never-falsy `startTime` cursor, empty page = end-of-history, dedup-by-ts with drift-raises) + **OI seed-then-accrue** (`binance.open_interest.*`: one-time ~30-day `openInterestHist` seed anchored on upstream's own latest timestamp — no wall-clock — then hour-truncated snapshot accrual, first-write-in-a-bucket-wins across seed/accrual/re-seed; survives the undocumented non-clamping-upstream case via a bounded skip-forward walk). `derivatives_snapshot` MCP tool reads the ADR-0051 store **offline by default** (funding + cadence-from-data `next_funding_ts` + 7d mean + OI with 24h/7d deltas; `None`-never-zero warm-up honesty; only `refresh=true` touches the network — incremental funding fetch + one accrual sample), registered only when the metric store is wired. Four `dev` phases + the `human` live smoke on branch `plan-0056-binance-derivatives-data` (`7611c63`→`5746049`), **merged `--no-ff` as `799dc9e`** — the anticipated `mcp_app.py` union conflict with 0052's close resolved by keeping both registrations. **Phase 2 geo verdict GREEN: 7397 BTCUSDT funding points to 2019-09-10** — and the smoke caught a real phase-1 bug (Binance treats `startTime=0` as absent → latest-window truncation), fixed forward (`668fd20`) with the quirk reproduced in the offline fake transport + a `network`-marked live test encoding the smoke permanently. Clean Mode 4 — no blockers, no majors; every done-when read at the assertion level (451 one-attempt **with retries available**; exact-equality round-trip against wire literals; seed/accrual overlap both orders; spy-asserted zero source calls on the default path; injected-future-point invisibility). 1395 tests + ruff + `mypy --strict` green on the merged tree. **ADR-0052 stays `proposed` — accepts at 0058's close (the klines half).** The OI accrual clock is running; Plan 0060's scheduler gives it a cadence. **0058 is now unblocked** (shared Binance files landed; geo settled for both). |
| 0052 | 2026-06-10 | **Classical chart patterns — detection, rendering, analytics.** New `analysis/chart_patterns.py` over [Plan 0051](done/0051-support-resistance-levels.md)'s `swing_pivots`: pivot-matched detectors (H&S / inverse, double top/bottom) + **connect-the-extremes** trendline-fit detectors (asc/desc/symmetrical triangle, rising/falling wedge), named-constant tolerances, **`k·ATR`** breakout confirmation with hard invalidation, and a strictly-trailing **forming → confirmed** lifecycle. Additive `TrendlineSpec`/`trendlines` wire field on `chart.show`/`chart.update` (no version bump; `exclude_none` byte-stability pinned, the Marker-span precedent) + `detect_chart_patterns` compute-and-draw MCP tool (one `chart.show`, dashed = forming / solid = confirmed; derived, not persisted — no repository). Active patterns folded into `ConditionSnapshot` (latest-state hit per formation within the breakout-scan horizon; the no-action field-set pin extended in place). Renderer: `lib/trendlines.ts` `ISeriesPrimitive` (canvas-free pixel math, off-screen clip, theme-token colours by role) wired via a **`useTrendlines` hook** — the god-component mitigation held: `CandlestickChart.tsx` gained only the hook call + one legend row. Four phases on branch `plan-0052-classical-chart-patterns` (`34ffe28`→`bf6ba58`, format `ae1ea51`), **merged `--no-ff` as `2c53853`** (conflict-free). Clean Mode 4 — no blockers, no majors; one minor (the plan's phase-2 "full expected-toolset assertion" doesn't exist in the repo — the house per-tool registration test was used, same as `detect_levels`/`forecast`; subsumed by the standing 0028-audit full-toolset follow-up) + two nits (wire specs live in `test_show_tools.py`, not the plan's `tests/events/...`; `ChartPatternHit.pattern` is `str`, not `Literal`). Every named spec read at the assertion level — **per-pattern truncation-invariance for all nine fixtures (exact hit-set equality at every truncation `k`)**, state transition computed from the module's own constants, never-breaking fixture never confirms, exactly-one-`chart.show` anchor/style pin against a real bus, legend toggle on the real component. 114 plan-touched Python + 426 desktop tests green. **ADR-0048 + ADR-0049 accepted.** **0054's detection prerequisite now exists — 0053 (shorts) is its remaining gate.** |
| 0051 | 2026-06-10 | **First-class support/resistance levels — the chart-pattern initiative's foundation.** Promoted the private snapshot pivot helper to a public `analysis/levels.py`: `swing_pivots(bars, left, right)` (confirmed trailing pivots — the primitive [Plan 0052](0052-classical-chart-patterns.md) consumes) + clustered, **volume-weighted** strength-ranked `support_resistance_levels` (greedy per-role price clustering; documented touch+volume strength blend, named constants). New `analysis/volume_profile.py` trailing volume-by-price primitive (proportional bucket attribution, conserves volume). `detect_levels` MCP tool computes the ranked levels **and auto-publishes one `chart.show` of `price_line` overlays** (`S1`/`R1`/… strength-ranked labels) in one call — derived, not persisted. Nearest support/resistance folded into `ConditionSnapshot` as structured `Level`s (price + 0..1 strength; a wrong-side-of-close level yields `None` after a breakout/breakdown, never a misleading frame) + `market-analyst` skill docs updated. Four `dev` phases on branch `plan-0051-support-resistance-levels` (`b75b2a5`→`9346526`), **merged `--no-ff` as `1eb2abd`** (one trivial `mcp_app.py` import-union conflict with 0055's merge — exactly the note that close carried). Clean Mode 4 — no blockers, no majors; every named spec read at the assertion level (exact-equality truncation invariance at all three surfaces — pivots/profile/levels; snapshot delegation equivalence; exactly-one-`chart.show` with the full `(label, role, price)` overlay pin against a real bus; the analyst no-action field-set pin updated in place). Two nits carried (private `_detect_levels_response` in `__all__` — matches the `scan_patterns` house pattern; empty-result ack carries `type`/`version` for an unpublished event). 1309 tests + `mypy --strict` green on merged `main` (the one Yahoo network-smoke failure is pre-existing, reproduced on pre-merge `main`). **Unblocks [Plan 0052](0052-classical-chart-patterns.md).** |
| 0055 | 2026-06-10 | **Cycle & macro time-series spine — the crypto program's foundation, and its one migration.** Implements [ADR-0051](../adrs/0051-historized-metric-series-contract.md) (**accepted at close**) exactly: generic `metric_points` table (migration `0004`) + `MetricSeriesSource` Protocol + registry-is-the-schema series ids + a repository whose whole read surface is `range`/`as_of` (the anti-lookahead join primitive), upsert-once immutability with an explicit `refresh` path. Four `dev` phases on branch `plan-0055-cycle-macro-series-spine`, **merged `--no-ff` to `main`**: contract (`f00f848`) → F&G full-history backfill + write-through (`2aecfe7`) → dominance/total-mcap hourly-bucket accrual on macro fetches, first-write-wins, best-effort (`81a17c8`) → `analysis/cycles.py` (halving clock with labeled estimate + saturation, Mayer, 200W-MA dist — honest `None`s, never shortened windows) + `btc_cycle_snapshot` + ADR-0046-paged `get_metric_series` tools, registered only when the store is wired (`5e85cf6`). Clean Mode 4 — no blockers, no majors; every done-when read at the assertion level (`as_of` probed at bound+1s; rejected-batch-writes-nothing; same-hour-drifted-value accrual; exact Mayer `200/100.5` + phase `730/1402` fixtures; `too_large` flipped at exactly cap+1; injected-future-point invisibility both unit and e2e through the real MCP server; write-through asserted through the real `crypto_fear_greed`/`bitcoin_market_pulse` tools). 1267 tests + ruff + `mypy --strict` green (one pre-existing Yahoo network-smoke failure reproduced on `main`, unrelated). Two minors carried: the phase-2 history fixture is synthetic-in-captured-shape (disclosed; `network`-marked live test compensates → follow-up below) and `mcp_app.py` overlaps the in-flight plan-0051 branch (trivial merge-order note for that close). **Unblocks 0056/0057/0058 (all migration-free)** — 0056 is the time-sensitive one (OI accrual). |
| 0050 | 2026-06-09 | **Agent-surface fixes** from the 2026-06-08 live BTC MCP session — six verified gaps closed in one mixed-owner plan. Eight phases on `main` (`18fc3a4`→`41fc9d9`, no branch): `backtester` widened `metrics._TIMEFRAME_BARS_PER_YEAR` to `15m/4h/1w` (252-day/24h basis, sqrt-scaled Sharpe pinned; closes the standing 0020 follow-up — `18fc3a4`) → `dev` bounded `get_ohlcv` to paged inline results (`MAX_OHLCV_BARS=400`, typed `too_large`, `total_available/offset/returned`, **fetch-before-slice so the cache keeps the whole window**, cap pinned against worst-case char size — `36c64fb`) → new `get_backtest` MCP tool (trades + full metrics inline, equity opt-in & paged, typed not-found not 500 — `4abe3b8`) → widened the three backtest tools to one shared `BACKTEST_TIMEFRAME` alias cross-checked `== _TIMEFRAME_BARS_PER_YEAR ⊆ SUPPORTED_TIMEFRAMES` (drops unsupported `1m` — `e9b082a`) → native `1mo` timeframe (registry row + `1.5×bar_duration` gap bound: no false February gap, omitted month still flagged — `f48c28b`) → `forecast` `edge_margin`/`edge_strength` marginal-edge qualifier (the 0.490/0.488 incident ships as `marginal`, no-edge still `prob_*=None` — `130f447`) → `strategy-author` `rsi_stop` (close-fill stop at the breaching bar, wide-stop==plain-rsi, truncation-invariance no-lookahead guard, fill model flagged in docstring — `38d8c06`) → `ui-builder` `1mo` in the chart (parity test + month/year tick formatter — `41fc9d9`). **Paired [ADR-0046](../adrs/0046-mcp-large-result-delivery.md) (MCP paging) + [ADR-0047](../adrs/0047-variable-duration-monthly-timeframe.md) (monthly) accepted.** Clean Mode 4 — no blockers, no majors; every named spec read at the assertion level (see the close note in [`done/`](done/0050-agent-surface-fixes.md)). 127 phase-touched Python + 32 renderer specs green. **Two non-blocking test-coverage follow-ups** (unit-test `monthlyTickMarkFormatter`; move the `get_backtest` registration assert into the central toolset test) → carried below. No migration, no branch. |
| 0036 | 2026-06-07 | **Forecasting subsystem foundation — Pillar 1 of the trade/predict/portfolio program.** First forward-looking capability in the app: `forecast/` predicts **next-bar direction as a calibrated up/down/flat probability** from cached OHLCV, or an honest **no-edge** verdict. Four phases on branch `plan-0036-forecasting-subsystem-foundation`, **merged `--no-ff` to `main`**: `dev` causal feature pipeline over the ADR-0023 `analysis/` surface (`07a3c75`) → `dev` direction labels + deterministic `HistGradientBoostingClassifier` (sklearn, seeded + `threadpoolctl` single-thread; `683c36e`) → `backtester` exposed `fold_bounds()` as a public seam (`96b6cc8`) + walk-forward validation with a **baseline-beating gate** (`67e5d7b`) → `dev` versioned model registry (`82ca0c6`) + `forecast` MCP tool carrying validation basis + full provenance (`d14daf1`). A follow-on commit (`962c4aa`) **purges horizon-overlapping train labels** in the walk-forward — a subtle label leak the contiguous-feature partition alone didn't close. **Paired [ADR-0030](../adrs/0030-forecasting-subsystem.md) + [ADR-0040](../adrs/0040-forecasting-model-artifacts.md) accepted.** Clean Mode 4 — no blockers, no majors; **every named spec read at the assertion level** (per-row truncation-invariance leakage guard; label-perturbation no-leak; byte-identical determinism golden via `struct.pack`; overfit→no-edge gate with in-sample>OOS evidence; `align_samples`-spy purge proof; `model_version` 5-input stability/change matrix; save/load identical-prediction round-trip; `forecast` in the full-toolset registration assertion). 52 forecast/api/backtest specs green on merged `main`; merge conflict-free. **Two minor fix-forward items** (declare `joblib`/`threadpoolctl` as exact-pinned direct deps — currently transitive via sklearn; add a NaN/inf input-bar guard) → carried as follow-ups below. Adds `scikit-learn==1.8.0` + `statsmodels==0.14.6` (exact-pinned, within existing cooldown cutoff). No UI (Plan 0037), no recommendation (advisor/Plan 0038). |
| 0048 | 2026-06-07 | **DeFi deep-read fix — Slipstream staked-CL gauge indirection** (Plan 0034 followup; resolves the 0034→0048 Aerodrome-smoke finding). The 2026-06-05 live smoke proved phase-3's one-hop was wrong for **staked Slipstream CL**: Zerion returns the CL **gauge** as `pool_address`, which exposes neither `slot0()` nor `positions()`. Replaced with the decoded **gauge-indirection chain** (`gauge.stakedValues(owner)`→tokenId, `gauge.nft()`→NPM.`positions(tokenId)`, `gauge.pool()`→`slot0`) + a **shape discriminator** routing the three LP shapes (v2 AMM → clean skip, no RPC; staked CL → gauge chain; unstaked CL → existing wallet-NPM enum). 3 `dev` phases on branch `plan-0048-slipstream-staked-cl-deep-read`, **merged `--no-ff` to `main` (`f39b08e`)**. **Deferred phase-3 live smoke RAN + PASSED** on `0xae5b…9790`: both staked Aerodrome V3 LPs enriched with real ticks / `in_range` (AAVE `30800..32800`, AERO `84000..86200`, token_id `232923`), v2 skipped, **no 429**. The smoke surfaced an RPC rate-limit burst, fixed in two follow-on `main` commits (`0848b18` → `4b602f9`): final shape drops the double-probe (carries the discovery classification forward) and paces **single** `eth_call`s — RPC batching was tried and **dropped** because the provider per-call-throttles. Clean Mode 4 (no blockers). No new ADR (corrects the *keying* within accepted [ADR-0034](../adrs/0034-defi-portfolio-aggregator.md)). Staked-CL deep read is now **live-confirmed**; the unstaked Uni-v3 + Aave paths stay fixture-only → carried as the F3 followup below. |
| 0049 | 2026-06-06 | **Chart workstream — pattern sweep + spans, view-stability fixes, live price & Supertrend overlay** (follow-on to 0047). 13-phase mixed-owner plan on `main` (`d85121f`→`30e1ddf`). `dev` (1–5): `PatternHit` static span + trailing `resolve_span` (`d85121f`) → additive `chart.highlight` `Marker` schema — `pattern`/`span_start_ts`/`span_end_ts`/`strength` + `neutral_marker` kind (`35a81d5`) → pure `analysis/markers.py` detect→map core + `scan_patterns` MCP tool, writes no annotation row (`772db63`) → renderer-gated `POST /scan_patterns` reusing the same core (`d93adbe`) → additive `supertrend` `OverlaySpec` kind + ATR `multiplier` (`aa6422c`). `ui-builder` (6–13): TS mirror + dedup re-keyed `(event_ts,pattern,kind)` (`a623538`) → multi-bar span band via lightweight-charts `ISeriesPrimitive` (v4.2.3, no canvas fallback needed; `2d676cd`) → "Scan patterns" visible-range button (`85dfebf`) → client-side Supertrend pinned 1e-6 to `analysis.indicators.supertrend` w/ masked up/down flip series (`6a064cf`) → live forming-bar from the `/quote` poll, guarded to the current bar period (`e76a821`) → overlay-toggle keeps zoom/pan (`fitContent` only on a `bars` change; `915dce1`) → candle/label legibility + backed labels (`cecb89c`) → tooltip edge-flip/corner-clamp (`30e1ddf`). **Paired [ADR-0045](../adrs/0045-candlestick-pattern-span-delivery.md) accepted** (span-bearing markers, derived not persisted — no migration). Clean Mode 4 — no blockers; every named done-when value-asserting (read in review: trailing span resolve; the same-bar doji/hammer collision fix proven at the mapper *and* the reducer; the route↔MCP-tool identical-markers shared-core guarantee; the Supertrend fixture pin + direction flip; the forming-bar no-lookahead guards — predates/crossed-period/no-`setData`/closed-bar-never-rewritten; `fitContent`-only-on-data-change; visible-range scan; tooltip corner-clamp). 120 named Python + **344 renderer** + `mypy --strict` + `ruff` + `gen-types --check` (no drift) green. **One Major (non-blocking):** the phase-6/7 god-component decomposition the plan asked for was not done — `CandlestickChart.tsx` grew ~743→933 (pure math went to `lib/`, but new orchestration landed inline) → re-logged as an open follow-up. Two nits (stale `events.test.ts` docstring; dual timeframe-validation source). No branch (committed on `main`). |
| 0047 | 2026-06-06 | **Smoke-run data-layer fixes + chart/timeframe/price UX** — 9-phase mixed-owner plan from the 2026-06-05 BTC smoke + viewer walkthrough. `dev` (1–4): chunk `upsert_bars` under the SQLite host-parameter cap (atomic, constant-derived chunk size — `f283db8`) → per-timeframe `try/except` in `multi_timeframe_analysis` so one tf's typed 422 degrades to a `null` snapshot while non-typed faults still propagate (`46f66ca`) → renderer-facing `GET /quote` live-quote route (bearer-gated, cross-tenant MCP bearer rejected, full error map — `0c53144`) → generic `price_line` overlay kind as the S/R delivery channel (kept one model + `model_validator` rather than a discriminated union; `exclude_none` leaves indicator overlays byte-unchanged on the wire — `f9389ec`). `ui-builder` (5–9): timeframe dropdown sourced from a single canonical `lib/timeframes.ts` (drops unfetchable `5m`/`1m`, adds `15m`/`4h`/`1w`; pinned by `timeframes.test.ts` since `OverlaySpec`/timeframes aren't on the HTTP surface for `gen-types` — `2bb95be`) → live timeframe-independent price header polling `/quote` (`d6098e8`) → bigger strength-aware markers (`15c35a5`) → crosshair hover tooltips (`d083279`) → layers legend with per-item show/hide (`069b746`). Clean Mode 4 (no blockers): 53 Python + the named renderer unit suites + **21/21 Playwright e2e** green; every named spec value-asserting (atomic-rollback upsert via an injected mid-batch failure, typed-vs-non-typed MTF split, `/quote` 401 cross-tenant, `price_line` round-trip with the `ema` overlay byte-unchanged, single-source timeframe guard, price header unchanged across 1h→1d, `markerVisual` strength→size/intensity scaling, layer toggle add/remove + ephemeral reset; the TS↔pydantic parity guard re-run live). The Mode-4 e2e run caught one red — `ohlcv-view.spec.ts` "Refresh advances…" assumed request index `[1]` was the Refresh fetch, but the Plan 0030 lazy-history `loadOlder` request (earlier `end`) occupies it first; fixed test-only by identifying the Refresh fetch via its advancing window end (`6d07c7e`), product behavior was correct. **One Major (non-blocking) + two minors → see Open follow-ups.** Ephemeral UI state, no migration, no paired ADR. No branch (committed on `main`). |
| 0034 | 2026-06-05 | DeFi **deep LP detail** — the depth half of the ADR-0034 hybrid. Enriches Zerion's discovered Uni-v3 / Aerodrome LPs with precise on-chain state (tick range, current tick, in-range, uncollected fees — the `DefiPosition` fields Plan 0032 left `None`), via **our own RPC keyed on `pool_address`** (the survey-confirmed source; ADR-0034 holds, **no new ADR**). Five `dev` phases on `main`: F1 fix (`farming`/pool-bearing-`staked` LPs → `kind="lp"`, token de-dup, `pool_address` threaded onto `DefiPosition`) `326335c` → model fields + `LpPositionDetailSource` Protocol `ec4263d` → Aerodrome one-hop RPC adapter `612fb71` → Uni-v3 two-hop (`tokenId` resolution via NPM enumeration) `32d3998` → enrichment folded into the scan flow + `scan_wallet`/`POST /defi/scan` wiring `fea9cfd`. Enrichment is **best-effort + serialized/spaced** (~1.1s, the survey's rate-limit floor) and **fail-safe** (a typed error / unconfigured RPC / unresolved `tokenId` leaves a position at discovery depth, `usd_value` untouched, never fails the scan). Clean Mode 4 — no blockers/majors; every named spec value-asserting (F1 dedup `WETH==0.18`, exact tick/fee decode, `in_range` both branches, typed RPC errors 429/500/JSON-RPC, one-hop vs two-hop call patterns, exactly-one spacing pause). 1036 offline pytest + `mypy --strict` + `ruff` + `gen-types --check` (no drift) green. **Two live verifications deferred** to user-run steps (need real secrets + network), tracked as follow-ups below: the **Aerodrome Slipstream live smoke** (`0xae5b…9790`, confirms the flagged one-hop `positions()`-on-pool ABI unknown) and **F3 Uni-v3 live-confirm** (ships fixture-tested). Resolves the Plan 0032 **F1** follow-up. No branch (on `main`). |
| 0026 | 2026-06-05 | Live-signal evaluator — the missing primitive: evaluate a strategy's `generate_signals` against the **current** bar (vs `run_backtest`'s historical window). Pure `backtest/live_signal.py` core + `SignalEvaluation`/`EvaluatedSignal` models (`d65e841`), an `evaluate_signals` MCP tool + `signal.evaluated v1` SSE in the neutral `events/` core (`5f54510`/`d12f4f4`), and a reactive `LiveSignalView` panel (`f042e47`). Stays a **condition-reporter** (no recommendations) — the prerequisite primitive the advisor (Plan 0038) consumes. Clean Mode 4 (no blockers/majors; 3 fix-forward nits). 28 Python + 20 renderer specs green; every done-when value-asserting (the `signals_to_trades`-divergence pinned by an actual `== []`). No ADR accepted (ADR-0029/0030 accept at Plans 0038/0036). No branch (on `main`). _(This recently-closed row was reconstructed 2026-06-05: the parallel close session's README edit was lost to a shared-index commit collision (commit `14b3709`); the plan-file move to `done/` + status flip landed correctly — this row re-syncs the index to that reality.)_ |
| 0033 | 2026-06-04 | UI theme control + visual polish — in-app **Light/Dark/System** (Settings *Appearance* segmented control + header `ThemeToggle`, one source of truth in `lib/theme.ts`), persisted in `localStorage['ma.theme']`, applied **pre-paint** via a `data-theme` attribute set by a tiny inline `<head>` bootstrap so there's no flash. The prod main-process CSP is `script-src 'self'` (no `'unsafe-inline'`), so the bootstrap is admitted by a **SHA-256 hash** — `window.csp.test.ts` recomputes it from `index.html` and asserts the hash is present and `unsafe-inline` is not. Plus a token/chrome visual-polish pass (palette/elevation/type-scale, both themes) and a **theme-aware candlestick chart** (candle/overlay/marker colors → CSS tokens, recolored in place via `applyOptions` on an `effectiveTheme` change — `CandlestickChart.theme.test.tsx` proves `createChart` fires once across the flip, i.e. no remount). [ADR-0039](../adrs/0039-renderer-theming-localstorage.md) accepted (renderer-owned theming, no sidecar route/IPC). Four `ui-builder` commits on `main` (`e87dea3`→`143f334`→`c0a2d9c`→`b8d48c6`). Clean Mode 4 — no blockers; one commit-hygiene nit (phase-1 commit carried an R100 rename of Plan 0032 into `done/`, left as-is per no-history-rewrite) + the visual-only AA-contrast/reduced-motion done-whens accepted on the implementer's verification. 31 renderer + 3 main specs green at close. No branch (committed on `main`). |
| 0032 | 2026-06-03 | DeFi wallet discovery — first plan of the DeFi program. Paste an EVM address → decoded positions across Ethereum/Base/Arbitrum/Optimism via Zerion. Four `dev` phases on `main`: `SecretsStore` (`0600` `secrets.json`, env-override-first, write-only `GET/POST /settings/secret(s)`, value never logged/echoed); `WalletPositionsSource` Protocol + `ZerionAdapter` (HTTP Basic, lazy key, typed `ZerionAuthError`/`ZerionError` taxonomy) + boundary-validated `DefiPosition` model; a source-agnostic `defi/` discovery service + async scan job streaming `defi.scan_*` SSE (masked wallet, "never silently zero"); `scan_wallet` MCP tool + renderer-gated `POST /defi/scan`. Correct layering (ADR-0032: `defi→events/data`, never `api`). **Paired ADR-0035 + ADR-0038 accepted; ADR-0034 held `proposed` pending the live smoke** (Zerion decoding quality unverified offline). Clean Mode 4 — one Major carried to the live smoke (LP `usd_value` sums per-token `value` against a *synthetic* fixture; the smoke must confirm it's not 2×) + two doc nits. 964-pass offline pytest + `mypy --strict` + ruff green (pre-push). No branch (committed on `main`). **Live smoke ran 2026-06-05 → [ADR-0034](../adrs/0034-defi-portfolio-aggregator.md) accepted:** scanned a real wallet, 5 Base positions, DeFi total reconciled to Zerion's net worth (no 2× — Major cleared); the smoke caught + fixed a `filter[positions]` bug (`4955b2e`) that had excluded all DeFi positions. Two non-blocking followups for the deep-adapter plan: F1 staked-LP classification (`kind="staking"`/`pool=null` → fold to `lp` + pool + de-dupe tokens) and F3 live-confirm Aave/Uni-v3. |
| 0023 | 2026-06-03 | News view in the app — first renderer surface for Plan 0010's news/sentiment. A renderer-bearer-gated `GET /news` (`news.py` + frozen `NewsResponse` envelope; window via the shared `SentimentWindow` literal; blank/whitespace symbol = browse-all, no aggregate) feeding a standalone **News** view (`NewsView` + typed `api.getNews` + nav tab). Untrusted feed content is rendered as text (no `dangerouslySetInnerHTML`), hrefs sanitized to `http(s)` and opened via `shell.openExternal`; server-side ±0.05 sentiment bucketing is not re-implemented (per-headline badge = compound-score sign only). No new ADR (a renderer read route like `/ohlcv`/`/search`). Clean Mode 4 (no blockers; two nits logged — stale `App.tsx` "Four views" docstring, optional `GET /news` smoke step). 8 route specs + 7 `NewsView` specs + full suites (renderer 229, API 328) + `mypy --strict` + `gen-types --check` no drift green. Branch `plan-0023-news-view` merged to `main` at close. |
| 0020 | 2026-06-03 | Extended backtest metrics + walk-forward + comparison tools — appended six metrics to `BacktestMetrics` per [ADR-0024](../adrs/0024-extended-backtest-metrics.md) (Calmar/Sortino/profit-factor/expectancy/best-worst; genuinely-undefined ratios are `null`, never `0.0`/NaN), bumped `ENGINE_VERSION` 0.1.0→0.2.0 and regenerated the 0008 golden fixture; added `walk_forward()` rolling out-of-sample evaluation (contiguous array-split anti-lookahead folds, per-fold metrics + aggregate + full-run baseline; `WalkForwardConfigError` on bad `n_splits`); and the `compare_strategies` (ranked leaderboard, deterministic None-last/`strategy_id` tie-break) + `walk_forward_backtest` MCP tools (both constrain `timeframe` to `1d/1h/1m`). Paired ADR-0024 accepted. Clean Mode 4 (no blockers); the four implementer-flagged decisions all confirmed sound — field defaults = degenerate value (engine sets them explicitly); `BacktestRunSummary` unchanged (Calmar column is a `dev` persistence migration); no pinned "material degradation" flag (baseline+aggregate exposed instead); UI rendering deferred to a `ui-builder` follow-up (logged below). 88 backtest specs (incl. golden/determinism) + 13 tool specs + mypy `--strict` green. Branch `plan-0020-backtest-metrics` merged to `main` at close. |
| 0022 | 2026-06-03 | Macro context — `bitcoin_market_pulse` (CoinGecko: BTC price/24h, dominance, total mcap/24h + a neutral structural `regime`) via a new `get_macro_context` Protocol method on a `CoinGeckoAdapter` (**two** keyless calls — `/global` for dominance + total-cap trend, `/simple/price` for BTC price + its own 24h move, which `/global` lacks); plus `market_snapshot` fanning `get_quote` over a fixed 8-symbol basket (`^GSPC`/`^IXIC`/`^VIX`/`BTC-USD`/`ETH-USD`/`EURUSD=X`/`SPY`/`GLD`) with per-symbol graceful degradation (one bad symbol → typed-null entry, rest return; "no other data dependency" enforced structurally in the test). Paired [ADR-0027](../adrs/0027-crypto-macro-regime-classification.md) accepted — `regime` is a closed neutral 4-value vocabulary (`btc_led`/`alt_structure`/`risk_off_structure`/`neutral`); thresholds pinned (−5% risk-off floor, 1pp dominance-trend deadband) and reconciled at close, with BTC-vs-whole-market 24h relative performance as the (direction-exact) dominance-trend proxy. Clean Mode 4 (no blockers; two pre-existing tracked nits — inline `datetime.now` seam, partial tool-registration test). 28 specs + full 307-pass API suite + mypy `--strict` green. Branch `plan-0022-macro-context` merged to `main` at close. |
| 0031 | 2026-06-03 | Yahoo absolute-range fetch — switched the OHLCV fetcher from now-relative `range=` to absolute `period1`/`period2`, so a past-ending window is fetched verbatim (`_yahoo_fetch.py`/`yahoo.py`); lifted the artificial 732-day span cap for uncapped timeframes; added the 2026-05-31 error-envelope guard. Phase 2 (finding M1) mapped the typed `UpstreamDataError` family to `/ohlcv` HTTP status (404/429/422/502) over the real provider+adapter chain — the route's 502 branch was dead for the real path — and recency-gated the empty-response branch ([ADR-0033](../adrs/0033-empty-ohlcv-response-by-recency.md) accepted): a strictly-historical empty window returns `[]`, a leading-edge empty window raises `UnknownSymbolError`→404. **Unblocked 0030.** Clean Mode 4 (no blockers; one redundant-catch nit left as-is). Live smoke: 249 AAPL 1d bars over a past year (pre-fix ~11). |
| 0030 | 2026-06-03 | Lazy historical loading (scroll-left to fetch older bars) — `useOhlcvHistory` accumulating buffer hook + `useLazyHistoryTrigger` visible-range trigger + scroll-anchored prepend (shift-right-by-N) + left-edge affordance/error chip. Rides the existing synchronous `GET /ohlcv` (no new route, no `dev` work); reacts to 422 (clamp) / empty (`reachedStart`) instead of mirroring data-layer constants. The deterministic seeded-cache Playwright e2e (`0cd7f7a`, real — asserts barCount growth + second fetch) is the close gate; the live path was dead until [Plan 0031](done/0031-yahoo-absolute-range-fetch.md), closed in the same ceremony. No new ADR (applies ADR-0007/0008). Clean Mode 4 (no blockers). |
| 0028 | 2026-06-02 | Data-layer boundary & extensibility hardening (2026-05-31 audit): per-capability source Protocols (`data/sources.py`, 7 `@runtime_checkable`) + selector-registry dispatch so adding a sentiment/market-sentiment source is one dict entry, not a 4-5-file edit; extracted the inlined VADER aggregation into `RssVaderSentimentAdapter`; relocated `EventBus`+schema from `api/events/` to a neutral top-level `events/` core, killing the lone `data→api` import (`data.backfill` now pulls zero `api` modules). [ADR-0031](../adrs/0031-data-source-adapter-contract.md) + [ADR-0032](../adrs/0032-data-layer-no-api-dependency.md) accepted at close. Behavior-preserving — 802 offline specs + renderer parity guard green. Clean Mode 4 (no blockers; one docstring nit left as-is). |
| 0029 | 2026-06-02 | Decompose the 618-line `CandlestickChart.tsx` god component (audit): extracted `useChartGestures` (the 8 prop-mirror refs are gone), moved marker/overlay math to `lib/markers.ts` + `lib/overlays.ts`, collapsed the 4-spot overlay seam into one `OVERLAY_REGISTRY` table (adding a kind is now one row). Behavior-preserving — component 618 → 364 lines; 200 renderer specs + the `__test_chart_render__` gate green. No new ADR (applies ADR-0008). Clean Mode 4 (no blockers); two nits left as opportunistic cleanup (pre-existing `act()` warning in `gestures.test.tsx`; a fix-forward two-commit phase-2 split). **Unblocks 0030.** |
| 0021 | 2026-06-02 | Multi-timeframe alignment (`multi_timeframe_analysis`, W→D→4H→1H→15m) + three volume-scanner tools (`volume_breakout`/`volume_confirmation`/`smart_volume`) over a supplied symbol list. Consumes the 0018 snapshot + 0027 volume primitives; default ladder unblocked by 0025. No new ADR (within ADR-0023/0007). Clean Mode 4 (no blockers); one Minor + 3 nits fixed pre-close in `fd35da3`. |
| 0027 | 2026-05-31 | Volume bars + volume-aware analysis — `analysis/volume.py` (volume MA, relative volume + percentile, OBV + slope, trailing VWAP, `volume_summary`); folds a `volume_stance` + volume measures into `condition_snapshot`; renders volume/VWAP/OBV chart bands client-side from `bars`. No new ADR (within ADR-0023/0008). **Unblocks 0021's phase 2.** |
| 0019 | 2026-05-31 | Live quote — `get_quote` (`YahooQuoteAdapter` on the chart endpoint) + `quote_for` tool. Discharged the last `MarketDataProvider` stub. **Unblocks 0022.** |
| 0025 | 2026-05-30 | Timeframe expansion — `15m`/`1w` (native) + `4h` (in-house resample); canonical `data/timeframes.py` registry; `history_exceeded` reason. ADR-0028 accepted. **Unblocks 0021's timeframe half.** |
| 0018 | 2026-05-30 | Technical-analysis surface — `analysis/` (9 indicators + 14 patterns + `condition_snapshot`) + `analyze_symbol` tool. ADR-0023 accepted. **Unblocks `market-analyst` + 0021.** |
| 0017 | 2026-05-30 | MCP tool-registration refactor — migrated 8 inline tools to the `register_*` pattern; `mcp_app.py` 576 → 131 lines. Behavior-preserving. |
| 0014 | 2026-05-30 | Interactive chart + agent-mode — UI-event vocabulary + `POST /ui_events` + `get_pending_ui_events` + agent-mode toggle + single-instance Electron. ADR-0021 accepted. |
| 0024 | 2026-05-29 | Symbol search + autocomplete — `search_symbols` over Yahoo `/v1/finance/search` + `GET /search` + debounced `SymbolPicker`. ADR-0026 accepted. |
| 0013 | 2026-05-26 | Auto-backfill on cache miss — honest `get_ohlcv` shape + `backfill_ohlcv` tool + `ohlcv.backfill_*` SSE + typed `UpstreamDataError` taxonomy + `BackfillCoordinator`. |
| 0012 | 2026-05-25 | StockTwits sentiment — second per-symbol source (explicit labels, no NLP); `source` param on `get_sentiment`; first `ResilientHttpClient` subclass. |

Earlier closed plans (0001–0011, 0015, 0016) are in [`done/`](done) and `git log`.

## Open follow-ups (no plan needed)

Small items carried from closed plans — pick up opportunistically; remove the row when it lands. If one grows past ~half a day or starts coupling other changes, promote it to a real plan.

| From | Item | Owner | Note |
|------|------|-------|------|
| 0008 | Trade-log P&L $ formula compounds inconsistently with the engine for multi-trade runs | `architect` → `ui-builder` / `backtester` | Plan 0008 pinned UI P&L as `(exit-entry)*(initial/entry)`, but `_build_equity_curve` compounds (`units = cash/entry` where `cash` is running equity). Row N>1 disagrees with the equity-curve delta (~10% on the 2-trade example). Either add a `Trade.pnl_usd` field (ADR-0018 amendment — becomes its own plan) or derive in the renderer from `equity_curve`. |
| 0012 | `news_for.py:31` still declares the window vocabulary inline | `dev` | The window hoist (`data/_windows.py`, commit `bccfc61`) migrated `sentiment_for_news` + `stocktwits_sentiment` but missed `news_for`, which carries the same `Literal["1h","4h","24h","7d"]`. Import `SentimentWindow` there too, then `gen-types --check` (stays clean — pydantic inlines the alias). |
| 0014 | Confirm whether Claude Code surfaces `notifications/resources/updated` to the model | `human`/`architect` | The phase-4 smoke confirmed the polling tool but not the best-effort push path. Polling is the contract regardless; re-measure before any plan leans on push (ADR-0021 open question). |
| 0020 | The `float \| None` extended metrics and the comparison/walk-forward reports have no UI surface | `ui-builder` | Plan 0020 added Calmar/Sortino/profit-factor/expectancy/best-worst (some `null`) to `BacktestMetrics` and the `compare_strategies` (leaderboard) + `walk_forward_backtest` (per-fold + aggregate) tools — all agent-only today. A renderer follow-up would render `null` as "—" and add a leaderboard / fold-report view. Optional; only if the user wants these on-screen. _(The `metrics._TIMEFRAME_BARS_PER_YEAR` extension to `15m/4h/1w` is now **Plan 0050 phase 1** — no longer carried here.)_ |
| 0047 / 0049 | **`CandlestickChart.tsx` decomposition still outstanding — now ~1041 lines (re-measured 2026-07-03).** Plan 0029 cut it to 364; 0047 (ph 8+9), 0049 (spans, Supertrend up/down series, forming-bar, scan-button), and later chart touches regrew it. Plan 0049 was meant to extract the tooltip + price-line/overlay reconcile into `lib/`/hooks first (its Risks section) — pure math *did* move to `lib/` (`spans.ts`, `computeSupertrend`, `tooltipPosition`) but the new orchestration landed inline, so the component grew rather than shrank. **Mode-4 finding at the 0049 close (2026-06-06), non-blocking.** Next `ui-builder` chart touch should lift the per-effect reconcilers (overlay/price-line/span/Supertrend visibility, crosshair subscription) into hooks. If it grows past ~half a day or couples other changes, promote to a real plan. | `ui-builder` | Behavior is correct + fully tested; this is structural debt, not a bug. |
| 0023 | `App.tsx:1` header comment still says "Four views"; the app has 8 views / 7 nav tabs (re-verified 2026-07-03) | `ui-builder` | One-line comment fix, opportunistic — the nit from the 0023 close that keeps drifting further as views land. |
| 0047 | `events.test.ts:3` file docstring still says `src/market_analyser/api/events/__init__.py`; Plan 0028 relocated the bus to the neutral top-level `events/` core (the import in the same file is already correct). | `ui-builder` | One-line comment fix, opportunistic. *(The companion `markers.ts` `markerVisual` dormant-strength note is now moot — Plan 0049 phase 2/6 wires `strength` end-to-end, lighting the path.)* |
| 0036 | Declare `joblib` + `threadpoolctl` as exact-pinned **direct** deps in `pyproject.toml` | `dev` | `forecast/model.py` imports `threadpoolctl` and `forecast/registry.py` imports `joblib` directly, but both resolve only transitively via `scikit-learn` (pinned in `uv.lock`, not the manifest). ADR-0013 pins directly-imported deps explicitly; `threadpoolctl` in particular gates the determinism contract (single-thread training), so a future sklearn bump shifting it could silently change behavior. Add both as `==`-pinned, then `uv lock`. |
| 0036 | NaN/inf guard on input bars in the feature pipeline | `dev` | `features._safe_ratio` guards zero denominators but a `NaN`/`inf` close propagates as a `NaN` feature (not `None`), which `HistGradientBoostingClassifier` silently absorbs as "missing" rather than rejecting. Low risk (bars are pre-validated from cache) but a bad-bar guard would match the data-integrity best-practice. Optional; reject or `None`-out non-finite feature values at row assembly. |
| 0050 | Unit-test `monthlyTickMarkFormatter` (the `1mo` month/year axis ticks) | `ui-builder` | `CandlestickChart.tsx:228` is the one genuinely `1mo`-specific piece of phase-7 logic and has zero coverage — the phase's "no duplicated day labels" done-when is asserted nowhere. Add a unit test feeding a known timestamp under `TickMarkType.Year` (→ `"2025"`) and a month tick (→ `"Jan"`). Behavior is correct; the assertion is missing. Mode-4 minor at the 0050 close (2026-06-09). |
| 0035 | Extend the phase-5 hint vocabularies to Aerodrome gauge-interaction shapes + book `swap` events in the replay engine | `dev` | The 2026-07-05 smoke's 3 Aerodrome LPs came back honestly `incomplete`: gauge deposit/withdraw/claim transactions surface as `unclassified` (outside the classifier's hint vocabulary) and `swap` is deliberately unbooked in v1. Together the two items would flip those LPs to complete. Details + the long-tail-price (Alchemy fallback) note in the plan's Followups section ([done/](done/0035-defi-pnl-reconstruction.md)). |
| 0055 | Run the F&G live verification once: `uv run pytest -m network tests/data/test_crypto_fear_greed_series.py` | `human` / `dev` | The phase-2 backfill is pinned by a synthetic-in-captured-shape fixture (disclosed in the test docstring — a verbatim `?limit=0` capture wasn't possible in the implementing environment). The `network`-marked test asserts the real endpoint: ≥2900 daily points, first day 2018-02-01, all values in [0, 100]. Run it once locally before relying on the backfill in anger; remove this row when it passes. Mode-4 minor at the 0055 close (2026-06-10). |
| 0053 | Tool-description lag behind the flat/long/short contract | `dev` | `mcp_tools/evaluate_signals.py:57` tool description still says "(flat/long)" — stale vs Plan 0053's flat/long/short live-signal contract. **The renderer half landed 2026-06-14 (`22ff538`):** `events.ts` unions (`EvaluatedSignal.kind` +`enter_short`/`exit_short`, `current_position` +`short`), the `events.ts`↔pydantic parity test, and `LiveSignalView` `KIND_LABEL` + `pos_short`/`kind_*_short` styles — renderer suite green again. Only this Python tool-description string remains; surfaced at the 0053 close (2026-06-11). |
| 0041 (close) | `tests/network/test_yahoo_smoke.py` is calendar-blind — "≥5 daily AAPL bars in the last 7 days" fails across a market holiday + weekend (seen at the 0059 and 0041 closes, July-4 week: 3 bars) | `dev` | Widen the window to ~12 days or lower the floor. `network`-marked so CI never sees it; it only reddens local `-m network` runs and close-gate full-suite runs. Pre-existing test-design flake, not a product bug. |
| 0032 (F3) / 0034 / 0048 | Live-confirm **Aave** (lending supply/borrow) **& Uniswap-v3** decoding | `human` / `architect` | Live-smoke finding 2026-06-05, narrowed at the 0048 close (2026-06-07): the smoke wallet (`0xae5b…9790`) is Aerodrome-heavy, so **only the staked Slipstream-CL path is now live-confirmed** (Plan 0048 smoke — real ticks/`in_range`, no 429). Aave classification and the **unstaked** Uni-v3 two-hop deep-read (`tokenId` resolution + `positions(tokenId)`) remain **fixture-only** — Plans 0034/0048 built and fixture-tested those paths but no in-scope wallet holds one. Re-run `scan_wallet` on a wallet holding them before the risk plan (0042) leans on those shapes — Plan 0035 (P&L) closed 2026-07-05 against the same Aerodrome-heavy wallet, so its smoke did not exercise them either. _(Plan 0032 **F1** — gauge-staked-LP classification + token de-dup — was **resolved by Plan 0034 phase 1**, `326335c`.)_ |

_The Plan 0013 `data→api` smell was promoted to [Plan 0028](done/0028-data-layer-boundary-hardening.md) phase 3 ([ADR-0032](../adrs/0032-data-layer-no-api-dependency.md)) after the 2026-05-31 audit re-flagged it — **resolved at 0028 close (2026-06-02)**: the event bus now lives in the neutral top-level `events/` core._

### From the 2026-05-31 architecture audit

Minors surfaced by the codebase-health audit. The three majors became Plans [0028](done/0028-data-layer-boundary-hardening.md)/[0029](done/0029-candlestick-chart-decomposition.md) (both closed); these are the small items. Same rule — pick up opportunistically; promote if one grows past ~half a day or couples other changes.

| Area | Item | Owner | Note |
|------|------|-------|------|
| data | `Bar` permits zero prices → latent `ZeroDivisionError` | `dev` | `data/types.py` validator rejects `< 0` but allows `0.0`; `backtest/metrics.py:102,238` divides by entry/price. Tighten OHLC to `> 0` or guard the division. |
| api | MCP tools read wall-clock inline (snapshot-testability) | `dev` | ~8 tools call `datetime.now(tz=UTC)` for `queried_at`/`analyzed_at` with no seam (`analyze_symbol.py:97,100`, `quote_for.py:61`, `news_for.py:74`, …), blocking output snapshot-equality tests. Route through a module-level `_now()` seam like `default_provider._now`. |
| api | `ohlcv` route imports a data-internal underscore symbol | `dev` | `api/routes/ohlcv.py:15` imports `ResilientHttpError` from `data._http`; re-export it via the public `data/errors.py` and import from there. |
| desktop | SSE payloads cast unvalidated (IPC/SSE asymmetry) — **partially resolved** | `ui-builder` | Plans 0039/0060 gave `recommendation.completed` and `alert.triggered` Zod `safeParse` in `dispatchEnvelope` (loud drop on mismatch); the other 8 payload kinds (`chart.show`/`chart.update`/`chart.highlight`/`run.completed`/`signal.evaluated`/the 3 `ohlcv.backfill_*`) are still raw `as` casts (re-verified 2026-07-03). Extend the established `safeParse` pattern to the rest. **Consider promoting** — the event count keeps growing. |
| desktop | `sidecar:status` not parsed + raw channel literal | `ui-builder` | `main.ts:80` sends `'sidecar:status'` as a string literal (every other channel uses `IPC_CHANNELS`); `client.ts:62-73` consumes the payload without `SidecarStatusSchema.parse()`, forcing two `!` assertions. Parse on the renderer side and reference the constant. |
| desktop | Test gaps: `format.ts` + IPC handlers | `ui-builder` | `renderer/lib/format.ts` (NaN→"—", rounding, sign) has no direct test; `appHandlers.ts`/`dialogHandlers.ts`/`shellHandlers.ts` have no unit test (only `sidecarHandlers` does — and `shellHandlers` is a security boundary). Add `format.test.ts` + handler registration/validation specs. |
| desktop | `client.test.ts` shares module state across cases | `ui-builder` | `client.test.ts:69-171` reads "whatever port a prior test left" instead of pinning — order-coupled. Wrap the config-refresh block in `jest.isolateModules` or reset the module cache in `beforeEach`. |

_The `data` "Yahoo error-envelope not guarded" item was absorbed into [Plan 0031](done/0031-yahoo-absolute-range-fetch.md) (same file `_yahoo_fetch.py`, same parse path) and **shipped with it (closed 2026-06-03)** — the `_parse_chart_payload` guard now raises the typed taxonomy instead of a raw `KeyError`/500._

_The `api` "no safety-net test that every MCP tool is registered" item (and the Plan 0050 `get_backtest`-registration carry-over it subsumed) was **closed by Plan 0035 phase 7 (2026-07-05)**: `tests/api/test_mcp_tools.py` now asserts the exhaustive 41-tool `EXPECTED_FULL_TOOLSET` by exact set equality against a fully-wired `create_mcp_components`._

## Recommended execution order

**State as of 2026-07-07 (Plan 0062 close):** everything outside the active roster is closed — the original committed roster, the capability-expansion and analysis batches, the full DeFi read stack (0032 discovery / 0034 depth / 0035 P&L), **Pillar 1 (Forecasting) in full (0036 foundation → 0059 v2 features → 0037 UI, closed 2026-07-06)**, the chart-pattern initiative (0051–0054, complete), **the entire crypto intelligence program (0055–0060, complete — 0059 closed 2026-07-06)**, Pillar 2 of the trade/predict/portfolio program (0038 advisor + 0039 advisor UI + the companion `advisor` skill), the watchlist alerting loop (0060), **the portfolio core (0041, closed 2026-07-06 — ADR-0042 accepted)**, **the metric-store self-warming fix (0061, drafted and closed 2026-07-06 — ADR-0056 accepted; the accrual clock is live)**, and **the tiered forecast feature sets (0062, closed 2026-07-07 — ADR-0057 accepted; v2-deep trains today, h=21 verdict "promising, not proven")**, and **chart-pattern trendline rendering (0064, closed 2026-07-08 — ADR-0059 accepted; detected necklines/bounds now draw, on a dedicated recompute-on-load event)**, and **the advisor tiered-forecast unification (0066, closed 2026-07-09 — no new ADR, realizes ADR-0057 in the advisor; `recommend` and `forecast` no longer disagree at a shared horizon)**, and **trendline pattern identification (0067, closed 2026-07-09 — ADR-0061 accepted, amends ADR-0049's colour-by-role; trendlines now colour by pattern type, de-clutter, and carry a hover tooltip + grouped legend)**, and **codebase remediation (0072, closed 2026-07-10 — ADR-0065/0066 accepted; phase 8 decomposed the 1455-line `CandlestickChart.tsx` into 10 hooks + 4 lib modules + a `ChartToolbar`, −51%)**. The active roster is all `approved` (Plans 0099, 0107, 0108 promoted from `draft` 2026-07-15):

- **Upstream-parity batch (Plans 0100–0103, from the 2026-07-13 upstream gap analysis) — all `dev`+`human`, migration-free, no chart-file contention.** Four independent additions closing the interesting-but-outstanding tail vs the inspiration project (we already match/exceed its core; these are the genuine gaps the user opted into): **0100** watchlist condition scanners (squeeze / gainers-losers / momentum) — **CLOSED 2026-07-15, ADR-0095 accepted; the shared `_scan_symbols` fan-out harness now exists in `analysis/scanners.py`**; **0101** composite quality rank (ADR-0096 — a screening rank, never a call) is **CLOSED 2026-07-15, ADR-0096 accepted; `quality_rank` shipped on the harness**; **0102** crypto sector rotation (ADR-0097 — a self-defined versioned taxonomy) **builds on 0100's harness and is unblocked — the next of the batch to run**; **0103** Reddit keyless crowd sentiment (ADR-0098) is **independent, runnable any time**. All four bump the shared `EXPECTED_FULL_TOOLSET` counter + regenerate `docs/reference/`, so running them in parallel risks a trivial index/apiref collision — assign tool counts in order or let one session own the counter (the shared-append pattern, per Conventions note 3). None adds a DB migration, so they don't touch the 0044/execution migration chain. (The `human` phase in each is the live smoke, deferrable like the other read-only smokes.)
- **Chart-file contention — fully resolved; the chart chain is entirely closed.** The chain `0096 → 0105 → 0106 → 0097 → 0104 → 0098` landed in order, all closed by 2026-07-15. **Plan 0098 (god-component refactor) closed 2026-07-15** — ADR-0092 accepted; the imperative `ChartController` was extracted over the now-fully-settled surface exactly once (never fighting a moving target), `CandlestickChart.tsx` is a thin adapter importing no lightweight-charts types, gates green. See Recently closed. **No chart-file plan remains in flight** — any future `CandlestickChart.tsx` work starts from the controller architecture (imperative wiring → a `ChartController` method + one forward effect; only React-state/JSX stays in the component). Phase-6 human visual smoke PASS 2026-07-15 (pixel-equivalent) — plan fully complete.
- **The portfolio chain continues 0042 → 0043** — 0041 (the core) closed 2026-07-06 with ADR-0042 accepted; 0042 (DeFi risk/forecast over these holdings) is now **input-complete** (0034 + 0035 + 0041 all closed, [ADR-0037](../adrs/0037-defi-position-risk-forecast.md) implementable) and migration-free; 0043 renders what 0041 + 0042 produce (0041's per-leg as-of + no-advice output shape is exactly what 0043's view pins).
- **0040** (Polymarket odds) **closed 2026-07-11** — ADR-0041 accepted; read-only odds source landed key-free on `main`. See Recently closed. **Unblocks Plan 0078** (Polymarket convergence screener consumes it).
- **0063** (forecast & recommendation explainability) **closed 2026-07-08** — implemented directly on `main` (`2941363`→`6ec2618`→`4d8f641` + fix-forward `31c6d9d`), ADR-0058 accepted, phase-4 live smoke verified. See Recently closed. Its standing followup — the advisor's forecast leg still ran the single-horizon v1 core, not the tiered multi-horizon path — was **drafted as Plan 0066 and closed 2026-07-09** (the two tools now share `_compute_multi_horizon_forecast`; the divergence is gone).
- **0064** (chart-pattern trendline rendering) **closed 2026-07-08** — implemented directly on `main` (`9a93e48`→`a08ef3c`, six phases, no branch, migration-free), ADR-0059 accepted. The live BTC-USD 1h non-drawing bug is fixed (off-grid time→x logical fallback) and trendlines now ride a dedicated `chart.trendlines v1` event recomputed from current bars. See Recently closed.
- **0044 → 0045 → 0046** (execution, testnet-only) run last; 0044 adds a migration and is now **next in the migration chain** (0035's `0006`/`0007` landed 2026-07-05).
- **0067 (trendline *pattern* colour) closed 2026-07-09** — ADR-0061 accepted (amends ADR-0049's colour-by-role). Renderer-only, three `ui-builder` phases on `main` (`7af295f`→`591dc3f`), no branch. It introduced the grouped-legend component that 0071 reuses. See Recently closed.
- **0068 (user chart-*style* settings) closed 2026-07-09** — ADR-0062 accepted (extends ADR-0039). Renderer-only, four `ui-builder` phases on `main` (`86baa2c`→`bd2b702`), no branch, migration-free. The old `readChartColors`/`overlaySeriesColor` read path is gone; `resolveChartStyle` is the single source for every drawn colour+width. See Recently closed.
- **0071 (candlestick pattern legend + declutter) closed 2026-07-10** — no new ADR (reuses ADR-0061's grouped-legend interaction, consumes ADR-0045's candlestick identity). Four `ui-builder` commits on `main` (`d94b77f`→`9ef4ac5`→`9981638`→`6765bde`, the last a draggable persisted layers-panel width), no branch, migration-free. The `LayersPanel` grouped-legend from 0067 was reused and generalized to serve both trendline and candlestick-marker groups; the always-on 104-marker dump is gone (draw-on-select, most-recent-group default). **This was the last chart-file plan gating Plan 0072 phase 8 — 0067 + 0068 + 0071 are all now closed, so phase 8's hard ordering constraint is satisfied.** See Recently closed.
- **0069 (Russian localization, full parity) closed 2026-07-10** — ADR-0063 accepted (extends ADR-0039). Six impl phases on `main` (`46df6cf`→`150fec0`, `ui-builder` ×5 + `dev` ×2 + `human` ×1), no branch, migration-free, no new dependency; ph7 human live smoke passed. In-house zero-dep `t()` over `en`/`ru` catalogs (en-US numbers kept); sidecar stays English-authoritative, emitting additive reason-codes (ph4) + condition/signal codes (ph4b) beside unchanged prose. See Recently closed.
- **0072 (codebase remediation, from the 2026-07-09 audit) closed 2026-07-10** — ADR-0065 + 0066 accepted. All nine phases on `main`, no branch, migration-free; the gated phase 8 decomposed the 1455-line `CandlestickChart.tsx` into 10 per-concern hooks + 4 pure `lib/` modules + a `ChartToolbar` (706 lines, −51%, renderer suite 670/670), ph 9 bumped the cooldown cutoff. Its close deferred the index-README refreshes to the parallel architect session (this one), which applied them alongside ADR-0069/0070/0071 + Plan 0077. **Clears phase 8's ordering gate for the render phases of Plans 0073 + 0076.** See Recently closed.
- **0077 (forecast pivot to volatility + regime) closed 2026-07-11** — ADR-0070 + ADR-0071 accepted. Six code phases on `main`, no branch, migration-free; **phase 4's cross-asset (BTC+ETH) edge verdict is an honest null result** — no ML edge replicates, so the deterministic EWMA/persistence baseline + trailing regime label are the shipped outputs and the advisor consumes vol/regime as non-voting sizing/stop/conviction inputs (ADR-0029 directional invariant intact). Phase 7 (human live smoke + go/no-go on keeping the advisor wiring active) was verified in the 2026-07-12 consolidated live smoke. Coordination residue for in-flight 0074: 0077 landed the `forecast_volatility`/`forecast_regime` toolset bump first, so 0074 rebases its `EXPECTED_FULL_TOOLSET`/`TYPE_REGISTRY` count (trivial). See Recently closed.
- **0073 (Ichimoku Cloud indicator) closed 2026-07-11** — ADR-0067 accepted (refines ADR-0023's composed trend classification). Five phases on `main` (`7e86b25`→`fb79b9e`, `dev` ×3 + `ui-builder` ×1 + `human` ×1), no branch, migration-free; the render phase (ph4) ran after Plan 0072 ph8 as gated. The cloud-aware `_classify_trend` conjunctive veto and the displaced/filled render both land, anti-lookahead defended by truncation-invariance + displaced-mapping tests. **Unblocks its two dependents in the active roster: Plan 0075 ph1 (`ichimoku()` prereq) and Plan 0074's Ichimoku technical-read leg.** See Recently closed.
- **0084 (DeFi P&L completeness: gauge/swap/unclaimed) closed 2026-07-12** — ADR-0079 accepted (refines ADR-0036). All 5 code phases + 3 phase-6 fix-forwards on `main` (`700b641`→`7fd069d`), no branch, migration-free; 196 DeFi/route/tool tests green (re-run at close). The headline **capability** is live-validated — gauge→pool resolution attributes `getReward` to the right position, `swap` books as average-cost lot conversions, and the labeled current-state `unclaimed_rewards` `earned()` read all work end-to-end (QuickSwap fully reconstructed $4,015.16 realized; 140.4 AERO ≈ $72.9 unclaimed, priced). **The plan's 5/5-complete / non-null-wallet-total headline is NOT met**: the phase-6 live smoke corrected the root-cause diagnosis (the 35 unclassified were dominated by **261 zero-transfer approvals**, not gauge join failures — `7fd069d` skips asset-less txs) and left **two new-scope follow-ups** that keep the wallet total `null` (see Open follow-ups). The three fix-forwards each enforce the plan's own "degrade to honest incomplete, never crash" risk rule (gauge resolution best-effort per chain; price fallback asymmetric; asset-less-tx skip). Determinism, no-lookahead, read-only stance intact; `unclaimed_rewards` excluded from the byte-identical guarantee. See Recently closed / the plan's Close notes.
- **0074 (technical-read advisory tier) closed 2026-07-12** — ADR-0068 accepted (extends ADR-0029 with a lesser tier). Three code phases on `main` (`42ec117`→`c17ef1d`→`cf8d4fc`, `dev` ×2 + `ui-builder` ×1), no branch, migration-free. The single-indicator `technical_read` tool + distinct `TechnicalRead` type/event shipped with the all-legs-agree `fuse()` untouched; Ichimoku eligibility was already satisfied by 0073 ph1. Phase 4 (human live smoke) was verified in the 2026-07-12 consolidated live smoke. See Recently closed.
- **0075 (Ichimoku strategy + backtest) closed 2026-07-12** — no new ADR (follows ADR-0004/0050). Two phases on `main` (`ddb9879` `strategy-author` → `185a260` `backtester`), no branch, migration-free. The `ichimoku` TK-cross-gated-by-the-cloud strategy is the 9th registered strategy, backtested + walk-forwarded through the flat/long/short engine; the displacement-lookahead risk is pinned by truncation-invariance (and holds structurally — the imported series is trailing, displacement applied only at consumption). **Consumed [Plan 0073](done/0073-ichimoku-cloud-indicator.md) ph1's `ichimoku()` prereq — that dependency is now discharged.** Once live, `recommend strategy_id="ichimoku"` gains a corroborated Ichimoku leg (the plan's Option-C payoff). See Recently closed.
- **0080 (advisor recommendation track record) closed 2026-07-11** — ADR-0075 accepted. Five code phases on `main` (`1d8a5c9`→`890f176`, `dev` ×4 + `ui-builder` ×1), no branch, one migration (`0008_advice_ledger`). Every `recommend` call is now indexed in an append-only ledger and scored path-dependently against realized price by a lifespan job; the read-only track record reports hit-rate + calibration + a beats-buy-and-hold delta with honest small-n. Phase 6 (human live smoke) was verified in the 2026-07-12 consolidated live smoke — it accrues over real calls + horizons. **Migration `0008` landed, so the chain head advanced past `0007`; the next migration-bearing plan (0044 or 0081) branches off `0008`.** See Recently closed.
- **0079 (cross-pool discrepancy scanner) closed 2026-07-11** — no new plan-paired ADR (it is the [ADR-0072](../adrs/0072-bounded-autonomy-and-prediction-market-execution.md) BA-7 evidence gate, which stays `proposed`). Three `dev` phases on `main` (`41c2922`→`20f55a9`), no branch, migration-free, no new dep; the standalone read-only arb *evidence layer* (`PoolPriceSource` + on-chain adapter + net-of-cost screener + `scan_pool_discrepancies` tool) landed and its live plumbing smoke passed. **Phase 4 (`human`, the BA-7 live evidence smoke on Base → `runs/defi/`) was verified in the 2026-07-12 consolidated live smoke** — the arb-viability finding accrues whenever the user points the tool at a live chain with verified pool addresses; a null result is a documented success. **No execution plan is written until phase-4 evidence justifies it** (ADR-0072 stays posture). See Recently closed.
- **0086 (concentrated-liquidity executable-quote pricing, cross-pool scanner v2) closed 2026-07-12** — ADR-0080 accepted (refines the Plan 0079 cost model; breaking `ArbObservation` change). The direct **Plan 0079 follow-on**: three `dev` phases on `main` (`2fd385d` executable-quote model + CP adapter + screener v2 → `77efe15` Quoter-based CL adapter + fee-tier discovery + read-only AST proof → `dc601fa` CP+CL wiring + MCP v2-schema cutover, v1 `PoolPriceSource`/`PoolQuote` retired), no branch, migration-free, no new dep. Both pool families now price behind one `ExecutableQuoteSource` Protocol — CP via `x·y=k`, CL via the DEX Quoter (`quoteExactIn/Out` over `eth_call`, staticcall) — and the screener ranks pre-costed quotes (`net = max(sell_proceeds) − min(buy_cost) − gas`, slippage measured). Clean Mode 4, no blockers/majors (one `scripts/defi/README.md` kwarg-rename doc-staleness fixed forward at close); every done-when read at the assertion level, and the **read-only claim is a real AST walk** pinning the JSON-RPC method set `== {"eth_call"}` on both adapters + a dependency-free keccak selector self-check. Gates re-verified at close: 61 Python (discrepancy + CP + CL + tool) + 13 full-toolset green, `apiref --check` exit 0, `mypy --strict` + ruff clean. **Phases 4-5 (`human` live evidence — completeness on deep majors, then neglected-niche ES-4) remain OUTSTANDING**: they need a permissive/paid Base RPC + verified venue addresses the operator supplies; a null on either is a documented success (same posture as Plan 0079 phase 4). Default pool/venue sets ship empty (no fabricated addresses). See Recently closed.
- **0078 (Polymarket convergence screener) closed 2026-07-12** — no new plan-paired ADR (consumes ADR-0041, bounded by ADR-0029). Three code phases on `main` (`763f113` `dev` → `fc3a467` `dev` → `7ef711e` `ui-builder`), no branch, migration-free, key-free. The read-only screener core + `find_convergence_opportunities` tool + `prediction.screen_completed v1` event + zero-action Convergence panel all landed and gates re-verified at close (37 Python + 54 renderer jest green, `apiref --check` exit 0). Its prerequisite (Plan 0040) closed 2026-07-11. Phase 4 (`human` live smoke against live Polymarket) was verified in the 2026-07-12 consolidated live smoke. Execution stays deferred — [ADR-0072](../adrs/0072-bounded-autonomy-and-prediction-market-execution.md) records the Polymarket-as-venue posture, no execution plan written yet. See Recently closed.
- **0089 (Polymarket open-in-browser market links + explicit sort) closed 2026-07-12** — no new ADR (realizes ADR-0041, records a provenance-link boundary call inside ADR-0029, honours ADR-0008 external-nav). The **first Plan 0078 followup** from its live smoke: three code phases in the parallel worktree/branch `plan-0089-polymarket-market-links` (`155adf2` `dev` → `4d03c75` `dev` → `e83a651` `ui-builder`), **merged `--no-ff` at `4a22df6`** (conflict-free against the parallel Plan 0088 work), worktree + branch pruned. `market_url` is built from the Gamma event slug at the data boundary (host-validated, `None` when absent), threaded onto `ConvergenceOpportunity`, returned by `find_convergence_opportunities`, and rendered as one read-only "View on Polymarket ↗" link per card (opened via `shell.openExternal`, never a renderer navigation; a renderer-side polymarket.com host allowlist over the existing `http(s)` IPC-scheme allowlist). Clean Mode 4, no findings; 78 Python + 64 renderer jest green, `apiref --check` exit 0. Phase 4 (`human` live smoke — link opens the correct market page from both surfaces, order largest→smallest, nothing reads as a buy call) was verified in the 2026-07-12 consolidated live smoke. See Recently closed.
- **0093 (DeFi P&L user-attested dust-token override) closed 2026-07-12** — ADR-0085 accepted (amends ADR-0036 narrowly for attested dust). One `dev` phase on `main` (`cb64203`), no branch, migration-free, no new dep, no output-schema change: a `_DustAwarePriceSource` wrapper zeros user-attested `defi_dust_tokens` (`chain:address`) legs in the replay price path (config → route/MCP → engine), with a sorted disclosing note on the still-complete position; loud-failure stays the default for every unlisted token (ADR-0036 intact). Closes the C1 Wanderers finding by **attestation, not a new price source**. Clean Mode 4, no blockers/majors. **Phase 2 (`human` live smoke) PASS** — the test wallet reconstructs 5/5 with the Wanderers position complete at $0 (Run 2 / C3 in the consolidated smoke). Close docs (`753439d`) landed after the concurrent Plan 0094 / ADR-0086 session (`5c77e49`); the README index refreshes were deferred at doc-close and applied once that session landed (the Plan 0088 shared-index pattern). See Recently closed.
- **0094 (CL Quoter revert omit-taxonomy) closed 2026-07-13** — ADR-0086 accepted (refines ADR-0080's self-contradictory omit-vs-raise clause). One `dev` phase on `main` (`c63301b`), no branch, migration-free, no new dep, no output-schema/apiref change: the CL adapter now omits a thin pool whose Quoter *quote leg* reverts (parity with the CP depth-omit) instead of aborting the whole scan, while a structural-read revert / decode failure / zero output still raises — classified by call site, never by revert string. Closes the Plan 0086 phase-4 live finding. Clean Mode 4, no findings; 25 CL-adapter + 224 defi tests green, `mypy --strict` + ruff clean, read-only AST `{eth_call}` intact. **Phase 2 (`human` live smoke) PASS 2026-07-13** (`runs/defi/audits/2026-07-13-plan-0094-phase2/`): the shipped adapter ran the un-curated set on Base at 1/5/20 WETH — scan completed with no abort, dust Slipstream tick-50/200 omitted at every size, deep tiers priced, net-of-cost the expected honest-null no-go on majors. **Plan 0094 fully closed; Plan 0086 phase-5 (neglected-niche ES-4) unblocked** — niche venue sets are dust-heavy and would have tripped the old abort. See Recently closed.
- **0090 (squeeze metric + counter-trend volume decomposition) closed 2026-07-13** — ADR-0083 accepted (records the *semantics*; band-width mechanics ride ADR-0023, the trend anchor is ADR-0067). All four `dev` phases on `main` (`63cf241`→`f9bdc9a`→`adb5a74`→`12f49f7`), no branch, migration-free, no new dep: `keltner()` + `bollinger_bandwidth()` primitives, the snapshot squeeze trio `bb_width`/`bb_width_pct90`/`squeeze_on` (TTM BB-inside-Keltner; `atr_pct90` demoted from squeeze proxy to a plain volatility fact), the pure `counter_trend_volume()` + frozen models, and the `counter_trend_volume` MCP tool (toolset 50→51, apiref regenerated). Clean Mode 4 — no findings; 2475 Python green, `mypy --strict` + `ruff` clean, `apiref --check` exit 0. **Phase 5 (`human` live smoke) run by architect at close via MCP and PASS** — the ETH `bb_width_pct90`(≈79)≠`atr_pct90`(3.3) contradiction is now visible/resolvable in one snapshot with `squeeze_on`=0, and `counter_trend_volume` correctly anchors to the live snapshot trend (BTC down → per-bar counter-trend split; ETH sideways → explicit no-counter-trend read); nothing reads as a buy call. Implemented directly in this working tree — no branch/worktree to merge or prune. First of the three 2026-07-12 `market-analyst` capability-audit plans; **Plans 0091 + 0092 remain the open technical-analysis-completeness batch** (both `approved`, backend-paired-with-UI). See Recently closed. Followups: Keltner overlay + squeeze marker render, squeeze-trajectory field, advisor consumption, squeeze-breakout strategy/alert.
- **0095 (lightweight-charts v4→v5 migration) closed 2026-07-13** — ADR-0088 accepted. Two `ui-builder` phases on `main` (`cee1be3`→`9a10ad7`), no branch, migration-free. The behavior-preserving engine swap: v5's real `chart.addPane()` replaces the v4 `scaleMargins`-band fake-pane trick, every series site on the unified `addSeries`, both `ISeriesPrimitive` files on the v5 type names, OBV moved onto its own real pane behind a reusable `lib/panes.ts` `PaneRegistry`. Renderer-internal, no wire/CSP change; 851 renderer jest + 58 test:main green; phase-3 human visual smoke PASS. **This directly unblocks Plan 0091 ph6–8** (the ui-builder wall that stalled 0091 — 8+ oscillator/money-flow panes need real panes, not `scaleMargins` slivers) and rides under Plan 0092's overlay/annotation phases. With 0095 closed, **Plan 0091's ui phases can resume** (dev ph1–5 already landed `7347392…f4e004b`); Plan 0091 becomes the next in-flight technical-analysis-completeness work. See Recently closed.

- **0034** (DeFi deep LP detail) — **closed 2026-06-05** ([done/](done/0034-defi-deep-lp-detail.md)). The depth half of the ADR-0034 hybrid: enriches Zerion's discovered Uni-v3/Aerodrome LPs with on-chain tick range / in-range / uncollected fees, via our own RPC keyed on `pool_address` (ADR-0034 holds; no new ADR). All five `dev` phases on `main` (`326335c`→`fea9cfd`); enrichment best-effort + serialized/spaced + fail-safe. Two live verifications deferred to user-run smokes (Aerodrome Slipstream ABI; F3 Uni-v3) — see Open follow-ups.
- **0035** (DeFi P&L reconstruction) — **closed 2026-07-05** ([done/](done/0035-defi-pnl-reconstruction.md), **ADR-0036 accepted at close**). The full transaction-replay vertical on `main` (`03861c7`→`8686496` + smoke hardening `dfa7eb6`): Zerion `/transactions/` ingestion with the immutable `defi_tx` cache (migration `0006`), the DefiLlama `HistoricalPriceSource` + first-write-wins `price_snapshots` cache (migration `0007`), the taxonomy mapper, the average-cost block-time replay engine, and the `compute_wallet_pnl` tool + `POST /defi/pnl` route with the advisory Zerion cross-check. Live smoke green (see the recently-closed row). The cost basis it produces is the risk engine's input ([ADR-0037](../adrs/0037-defi-position-risk-forecast.md)) — 0042 is now input-complete.
- **0061** (metric-store self-warming) — **closed 2026-07-06, drafted and closed the same day** ([done/](done/0061-metric-store-self-warming.md), **ADR-0056 accepted at close**). The time-sensitive one ran first, as this section recommended: the accrual clock is live (all five series warm from one boot; the starved forecast states its v1 fallback reason instead of shipping a vacuous no-edge). Its standing calendar follow-up (re-run the 0059 v1-vs-v2 comparison once dominance/OI accrue) was **superseded by Plan 0062** (closed 2026-07-07): the comparison ran at 0062's phase 3 via the v2-deep tier; what remains is 0062's own follow-up — re-compare including v2-full when dominance/OI cross the 500-row floor.

### Trade / predict / portfolio program (provisional, approved-in-shape 2026-06-05)

A five-pillar program taking the app from read-only analyst toward forecasting, advice, cross-venue portfolio management, and **testnet-only** assisted execution. The shape was approved by the user 2026-06-05 after a gap analysis + a verified deep-research pass; the per-plan breakdown below is **provisional** (only Plan 0036 + ADR-0040 are written so far). Key user-set constraints: **forecasting + advisor first**; execution is **paper/testnet only** (no real-funds gate designed yet); Polymarket is a **read-only odds signal now, trading venue later**; Binance leg is **USDⓈ-M Futures testnet** (`testnet.binancefuture.com` — *not* the spot `testnet.binance.vision`, a correction the research caught).

| Pillar | New ADRs | Plans (provisional) | Accepts existing ADR | Notes |
|--------|----------|---------------------|----------------------|-------|
| **1 · Forecasting** | **0040** (accepted — model library/determinism/versioning) | **0036** foundation (**closed 2026-06-07**) → **0037** UI (**closed 2026-07-06**) | 0030 (accepted) | sklearn `HistGradientBoostingClassifier` (no extra native dep); deep-learning + LightGBM/XGBoost deferred. **Pillar 1 is complete** — foundation (0036), v2 features + multi-horizon (0059, crypto program), and the viewer surface (0037). |
| **2 · Advisor** | — | **0038** advisor layer (**closed 2026-07-02**) → **0039** advisor UI (**closed 2026-07-02**); companion `advisor` skill **created 2026-07-02** | 0029 (**accepted at 0038's close**) | Consumes forecasts + Plan 0026 live-signal eval. **Pillar 2 is complete** — plans, skill, and the `defi-analyst` "rebalance suggestion" charter reconciliation (ADR-0037 note, done 2026-07-02: rebalance asks now route to `advisor`). |
| **3 · Polymarket data** | **0041** (**accepted at 0040's close, 2026-07-11** — read-only odds source) | **0040** odds adapter (**closed 2026-07-11**) | — | Auth-free reads (Gamma; `outcomePrices` == CLOB midpoint = implied probability). Landed key-free; **unblocks Plan 0078** (convergence screener). **Trading target remains the new `Polymarket/py-sdk`; `py-clob-client` is archived.** |
| **4 · Portfolio** | **0042** (**accepted at 0041's close, 2026-07-06**) | **0041** aggregation (**closed 2026-07-06**) → **0042** DeFi risk/forecast → **0043** UI (both written, approved) | 0037 | Tools-only (no operator skill). Average-cost venue-wide; Binance+DeFi+manual-file; Binance *read* key via ADR-0038. 0041 landed the `portfolio/` package + `portfolio_summary`; the risk engine (Plan 0042) implements ADR-0037 and is now **input-complete** (0034+0035+0041 all closed). |
| **5 · Execution (testnet)** | **0043** (written — ExecutionVenue Protocol + order state machine), **0044** (written — segregated trade-secret store, keychain) | **0044** `execution/` skeleton + `trader` skill → **0045** Binance USDⓈ-M testnet adapter (official SDK) → **0046** pending-queue + confirm UX (all written, approved) | 0025 | Heaviest, last. All six ADR-0025 invariants are hard gates; Plan 0046 close accepts ADR-0025. 0044 adds a migration → serialize. Polymarket *trading* is a deferred second venue adapter behind its own later ADR. |

Sequencing: **1 → 2** is the critical path (user priority). **Pillar 3 (Polymarket data)** is file-disjoint + key-free and may run in parallel (own worktree). **Pillar 4** is gated on the in-flight DeFi plans. **Pillar 5** is last. The two research corrections above (Binance futures-testnet host; archived `py-clob-client` → `py-sdk`) are pinned into Pillars 5 and 3 respectively so the implementers don't re-derive them from stale memory.

**Skill ecosystem (decided 2026-06-05):** the program adds **exactly two new skills**, both at trust-boundary crossings — **`advisor`** (Pillar 2, mandated by [ADR-0029](../adrs/0029-advisory-recommendation-boundary.md); crosses the recommend line) and **`trader`/`execution`** (Pillar 5, mandated by [ADR-0025](../adrs/0025-trade-execution-feasibility.md) invariant 3; holds the only write path + trade-secret). Both are created via `skill-creator` as companion steps, not plan phases. **No new implementer skills** — all build work routes to existing `dev`/`backtester`/`ui-builder`. **Forecasting (Pillar 1)** and **Polymarket odds (Pillar 3)** stay read-only and **extend existing analyst surfaces** (a forecast is a calibrated *condition*, not a call; odds are a *signal*) rather than adding a skill. **Pillar 4 (portfolio) is tools-only — no operator skill:** the cross-venue aggregation ships as agent-callable tools, with `market-analyst` and `defi-analyst` each reporting their own slice and the agent assembling the unified view, preserving the TradFi/DeFi hard split rather than relaxing it.

**Plan 0049 (pattern sweep + span rendering) — closed 2026-06-06** ([recently-closed](#recently-closed); [ADR-0045](../adrs/0045-candlestick-pattern-span-delivery.md) accepted at close). The follow-on to Plan 0047: it made candlestick-pattern identity first-class (a `scan_patterns` tool + `POST /scan_patterns` route emitting every in-range pattern in one event, an extended span-bearing marker model, multi-bar span rendering) plus the Supertrend overlay, live forming-bar, and three chart-view fixes. All 13 phases on `main` (`d85121f`→`30e1ddf`); no migration (sweeps derived, not persisted). **One non-blocking Major carried forward:** the `CandlestickChart.tsx` decomposition the plan called for was not done (component grew ~743→933) — tracked in Open follow-ups; the next `ui-builder` chart touch should lift the per-effect reconcilers into hooks.

**Chart-pattern initiative (Plans 0051–0054) — complete: all four closed (0051 + 0052 2026-06-10, 0053 2026-06-11, 0054 2026-06-14).** Four plans / three ADRs, with this dependency DAG: **0051 → 0052**, and **0052 + 0053 → 0054**.
- **0051** (first-class S/R, `dev`) — **closed 2026-06-10** ([done/](done/0051-support-resistance-levels.md), merged `1eb2abd`). Landed `analysis/levels.py` (`swing_pivots` + clustered volume-weighted `support_resistance_levels`), the `volume_profile` primitive, the `detect_levels` compute-and-draw tool, and nearest S/R in `ConditionSnapshot`. No new ADR. **The `swing_pivots` foundation 0052 consumes now exists — 0052 is unblocked.**
- **0052** (classical-pattern detection + rendering + analytics, `dev → ui-builder`) — **closed 2026-06-10** ([done/](done/0052-classical-chart-patterns.md), merged `2c53853`). Landed `analysis/chart_patterns.py` (nine patterns over two recognition models, `k·ATR` confirmation, strictly-trailing forming/confirmed lifecycle), the `detect_chart_patterns` compute-and-draw tool + additive `trendlines` wire field, active patterns in `ConditionSnapshot`, and the `trendline` `ISeriesPrimitive` via a **`useTrendlines` hook** (the god-component mitigation held — the chart gained only a hook call + legend row). **ADR-0048 + ADR-0049 accepted at close.** **0054's detection prerequisite now exists; 0053 (shorts) is its remaining gate.**
- **0053** (short-selling support, `dev → backtester`) — **closed 2026-06-11** ([done/](done/0053-short-selling-support.md), merged `607c1c6`). Landed the flat/long/short contract + engine on one signed code path (`ENGINE_VERSION` 0.3.0; golden moved by exactly that field — no silent long regression), **migration-free**. **ADR-0050 accepted at close.**
- **0054** (breakout strategy + backtest, `strategy-author → backtester`) is the join — **closed 2026-06-14** ([done/](done/0054-chart-pattern-breakout-strategy.md)). Landed the `chart_pattern_breakout` strategy (each confirmed `ChartPatternHit` → a directional entry at the confirming bar, direction read off the hit's resolved field; stop-and-reverse default exit) + a backtest/walk-forward validation proving both a long and a short trade on a mixed fixture. No new ADR; migration-free; no branch.

With all four closed, the chart-pattern initiative (0051–0054) is **complete**.

### Crypto intelligence program (Plans 0055–0060) — approved 2026-06-09

From the 2026-06-09 gap review ("what's missing for comprehensive analysis, price forecast and signals, crypto-focused, BTC cycles included"): six plans / five ADRs (0051–0055), shape approved in interview, external-API facts verified against official docs before drafting. Dependency DAG: **0055 → {0056, 0057, 0058} → 0059**; **0060 independent**.

- **0055** (cycle & macro series spine, `dev`) — **closed 2026-06-10** ([done/](done/0055-cycle-macro-series-spine.md)). Landed the shared [ADR-0051](../adrs/0051-historized-metric-series-contract.md) `metric_points` contract (**accepted at close**) — the program's only migration — plus F&G full-history backfill, dominance accrual, and the no-new-source cycle metrics (halving clock, Mayer, 200W). The gate is open: 0056/0057/0058 are now unblocked and migration-free.
- **0056** (Binance derivatives, `dev → human → dev`) — **closed 2026-06-10** ([done/](done/0056-binance-derivatives-data.md), merged `799dc9e`). Funding-rate full history (7397 BTCUSDT points to 2019-09) + OI seed-then-accrue landed; **geo verdict GREEN** (no 451) — settled for 0058 too. The OI accrual clock is running. **0057** (on-chain valuation, `human → dev`) — **closed 2026-06-15** ([done/](done/0057-onchain-valuation-metrics.md)) — and **0058** (Binance klines, `dev → human`) — **closed 2026-06-13** ([done/](done/0058-binance-klines-ohlcv.md), merged `0260c5d`) — are both done. With 0055/0056/0057 all closed, the data half of the crypto program is complete and **0059 is fully unblocked**.
- **0059** (forecast feature-set v2 + multi-horizon, `dev → backtester`) — **closed 2026-07-06** ([done/](done/0059-forecast-feature-set-v2.md), merged `--no-ff`, **ADR-0054 accepted**). The join landed: lag-1 as-of exogenous features + independent multi-horizon gating. Phase-4 caveat: the production metric store is empty, so v2 runs are not yet live — see the recently-closed row. **With 0059 closed, the crypto intelligence program (0055–0060) is complete.**
- **0060** (watchlist alerting, `dev → ui-builder`, [ADR-0055](../adrs/0055-in-sidecar-watch-scheduler.md)) is independent of the data plans but **adds a migration → serialize** against 0035/0044 (0055 and 0053 are closed — neither holds the chain anymore).

**Interleave decision (user, 2026-06-09):** the chart-pattern initiative (0051–0054) runs first; the crypto program follows — with one carve-out below for 0056's data loss. The advisor chain (0037 → 0038 → 0039) is unchanged and complementary — 0059's richer forecast feeds 0038's existing contract without modifying it. Geo note: Binance public APIs return HTTP 451 from restricted regions even for read-only endpoints; **settled 2026-06-10 by 0056's phase-2 smoke — GREEN** (full backfill from the user's network, no 451), so the ADR-0052 Bybit fallback stays dormant and 0058 inherits the verdict.

**Recommended sequence (architect, 2026-06-09 — whole approved roster):**

1. **0055 ∥ 0051** — **both closed 2026-06-10** (0055 merged `fc34aff`; 0051 merged `1eb2abd` — the predicted `mcp_app.py` conflict was the trivial import union). The parallel-worktree fan-out worked as designed; both worktrees pruned at the 0051 close.
2. **0056 ∥ 0052** — **both closed 2026-06-10** (0052 merged `2c53853`, ADR-0048/0049 accepted; 0056 merged `799dc9e` — the predicted `mcp_app.py` conflict was the trivial import union, same as the 0051/0055 close). 0056's smoke settled the geo question **GREEN** and the OI accrual clock is running.
3. **0053** (shorts) — **closed 2026-06-11** (merged `607c1c6`, ADR-0050 accepted). The engine track is clear; it shipped migration-free, so the migration chain head stays open.
4. **0057 ∥ 0058** — **both closed.** 0058 (klines) closed 2026-06-13 (merged `0260c5d`, **ADR-0052 accepted**); 0057 (on-chain, MVRV-only after the `human` probe) closed 2026-06-15 (**ADR-0053 accepted**). Both migration-free; the parallel-disjoint pair landed as planned.
5. **0054** (breakout strategy) — the chart-initiative join; needed 0052 (closed 2026-06-10) + 0053 (closed 2026-06-11) — **closed 2026-06-14.** The chart-pattern initiative (0051–0054) is complete.
6. ~~**0059** (forecast v2)~~ — **closed 2026-07-06** ([done/](done/0059-forecast-feature-set-v2.md), merged `--no-ff` from its worktree branch, ADR-0054 accepted; migration-free, worktree/branch pruned). The crypto-program join landed; the crypto intelligence program (0055–0060) is **complete**. One operational tail: the production metric store must warm before v2 forecasts go live (phase-4 finding).
7. **0037 → ~~0038~~ → ~~0039~~** (forecast UI → advisor → advisor UI) — **0038 (advisor layer) closed 2026-07-02** ([done/](done/0038-advisor-layer.md), ADR-0029 accepted; implemented directly on `main`, no branch) and **0039 (advisor UI) closed 2026-07-02** ([done/](done/0039-advisor-ui-surface.md), merged `--no-ff` from its worktree branch; worktree pruned). **Pillar 2 is complete**; only 0037 (forecast UI — independent and small) remains from this chain. 0038/0039 landed before 0059's richer forecast, as permitted — 0059 feeds the existing contract without modification.
8. ~~**0060** (alerting)~~ — **closed 2026-07-03** ([done/](done/0060-watchlist-alerting-loop.md), ADR-0055 accepted; merged `--no-ff` from its worktree branch, migration `0005_watches_alerts` landed — the chain head is taken and released). It ran ahead of 0059 (the 2026-07-02 fan-out decision), watching the already-warm indicator/pattern/signal primitives; forecast watches stay out of scope per ADR-0055.
9. ~~**0035** (DeFi P&L, two migrations)~~ — **closed 2026-07-05** (migrations `0006`/`0007` landed; ADR-0036 accepted). Remaining: ~~**0040** (Polymarket)~~ **closed 2026-07-11** (ADR-0041 accepted; unblocks Plan 0078) → **0042–0043** (portfolio — **0041 closed 2026-07-06**, ADR-0042 accepted) → **0044–0046** (execution, last, serializes on its migration) — otherwise unchanged from the standing program order.

Migration serialization summary (the one global constraint): **~~0055~~ → ~~0053~~ → ~~0060~~ → ~~0035~~ → 0044** in some order, never two at once (0055's `0004_metric_points` landed 2026-06-10; 0053 closed migration-free; 0060's `0005_watches_alerts` landed 2026-07-03; 0035's `0006_defi_tx_cache` + `0007_price_snapshots` landed 2026-07-05 as one linear pair — **the chain head is free again; two approved plans now add migrations — 0044 (execution) and 0080 (advice ledger) — so those two must serialize against each other, never fan out together**); all other plans are migration-free and parallel-able subject to the file-adjacency notes above.

Note for any future `ui-builder` plan: Plan 0033 rewrote the shared `styles.css` token system and established `lib/theme.ts` as the single source of truth for theme state. A plan that touches the chart, `App.tsx`, or the token file is inherently serial with other in-flight `ui-builder` work regardless of worktrees, and should read its colors from the theme tokens rather than reintroducing hardcoded hex.

The architect close ceremony serializes plan completion regardless: data-adapter plans (`data/adapters/`, `mcp_tools/`) and renderer plans (`App.tsx`/`client.ts`) shouldn't run in parallel against the same files. Plan history (the per-plan Gantt of who shipped what when) lives in `git log` and each plan's `done/` file.

## Parallel execution

Multiple `approved` plans can be implemented at once by giving **each implementer its own git worktree** (one branch off `main` per plan), so no two agents share a working tree. The default is still one session per plan; this is the opt-in pattern when you want throughput across plans the roster already marks disjoint. Worktrees isolate *files* — but three collisions survive file isolation in this repo and must be handled explicitly:

1. **Shared runtime (port + DB + lockfile + agent-mode file).** All resolve to one per-user app-data dir, and the sidecar refuses to start if a lockfile is already present (ADR-0016). Any done-when check that launches the **real sidecar process** (Playwright/Electron e2e, manual smoke, `pnpm dev`) collides across worktrees. Fix — set both per worktree before running such checks: `MARKET_ANALYSER_DATA_DIR=<unique temp dir>` (isolates DB + lockfile + agent-mode file in one move; taken verbatim per `config.py`) and run the sidecar with `--port=0` (OS-picked port, prints `PORT=<n>`). In-process `TestClient` unit tests bind no port and use temp DBs, so they parallelize without this.
2. **Alembic migrations are one linear chain.** `migrations/versions/` is a single sequence; two plans that each add a migration both branch off the same head → two heads at merge. There is no runtime fix — **do not parallelize two plans that touch `persistence/migrations/`**, or serialize just the migration-adding phase. Confirm the chosen set is migration-free before fanning out.
3. **Index + numbering races.** This README, `adrs/README.md`, and the next-free `NNNN` are shared append-points. **Assign all plan/ADR numbers up front** (architect, before fan-out) and expect trivial merge conflicts on the index files — resolve them at the close ceremony, or let one session own index writes.

**Workflow:**
1. **Architect, up front:** pick a disjoint set from the roster (the "largely disjoint files" sets the execution-order section already curates), confirm no two touch `persistence/migrations/`, assign any new numbers now.
2. **Per plan:** one worktree + branch; export a unique `MARKET_ANALYSER_DATA_DIR`; run sidecar/e2e on `--port=0`. The agent runs the plan's contiguous phases, committing per phase on its branch. **Never push.**
3. **Merge back through the close ceremony** — which is already serialized (architect reviews one plan at a time, flips status, moves to `done/`). That is the natural merge gate; do the branch merges one at a time there.

A **mixed-owner plan** (e.g. 0026 `backtester → dev → ui-builder`) is a *sequential* skill chain *within one worktree* — that is the cross-skill handoff protocol, not parallelism. Parallelism is **across plans** (worktree per plan), never across phases of one plan.

## Status vocabulary

| Status | Meaning |
|--------|---------|
| `draft` | Author wrote it; no user "go" yet. Implementers ignore. |
| `approved` | User signed off. Implementers may pick up. |
| `in-progress` | An implementing skill flipped it at Step 2 of its session. |
| `implementation complete — pending …` | All phases shipped; close blocked on a named followup or review delta. |
| `done` | Close ceremony fired; file lives in `done/`. |
| `abandoned` | Killed before completion. Stays here for the record. |
| `superseded by NNNN` | A later plan replaced this one. |

Only `architect` and the implementing skill (at Step 2) may mutate `Status:`. Implementers flip `draft → in-progress`; architect handles every other transition.

## Owner-skill vocabulary (per phase)

Each phase carries `**Owner skill:**` with exactly one value, backticked:

- `` `dev` `` — Python sidecar, persistence, CI, tooling, non-UI Electron.
- `` `ui-builder` `` — anything under `desktop/`.
- `` `strategy-author` `` — `src/market_analyser/strategies/`.
- `` `backtester` `` — `src/market_analyser/backtest/` and `runs/`.
- `` `human` `` — user-only task (rare).

Mixed-owner plans hand off at every boundary per the [cross-skill handoff protocol](../../../.claude/skills/architect/references/templates/cross-skill-handoff.md). Missing or ambiguous tags fail Mode 4 review as blockers.

## Conventions

- **Numbering** is sequential, zero-padded to four digits, independent for plans and ADRs. **Next free plan: 0111. Next free ADR: 0105** (see [`../adrs/`](../adrs/)). Architect runs `Glob docs/architecture/plans/*.md` and `…/adrs/*.md` before drafting — never trusting memory. **Note:** the trade/predict/portfolio program (plans 0036–0046, ADRs 0040–0044) is **fully drafted** (see the program reservation below). Plan 0047 = the 2026-06-05 smoke-fix + chart-legend plan; Plan 0048 = the Plan 0034 Slipstream deep-read followup; Plan 0049 = the pattern-sweep + span-rendering followon to 0047 (paired ADR-0045); Plan 0050 = the 2026-06-08 agent-surface fixes (paired ADR-0046 + ADR-0047); **Plans 0051–0054 = the chart-pattern initiative (0051 first-class S/R; 0052 classical-pattern detection/rendering, paired ADR-0048 + ADR-0049; 0053 short-selling support, paired ADR-0050; 0054 breakout strategy)**. **Plans 0055–0060 = the crypto intelligence program (0055 cycle/macro series spine, paired ADR-0051; 0056 Binance derivatives + 0058 Binance klines, paired ADR-0052; 0057 on-chain valuation, paired ADR-0053; 0059 forecast feature-set v2, paired ADR-0054; 0060 watchlist alerting, paired ADR-0055)**. **Plan 0061 / ADR-0056 = metric-store self-warming (2026-07-06, paired)**. **Plan 0062 / ADR-0057 = tiered forecast feature sets (2026-07-06, paired)**. **Plan 0063 / ADR-0058 = forecast & recommendation explainability (2026-07-06, paired)**. **Plan 0064 / ADR-0059 = chart-pattern trendline rendering (2026-07-08, paired)**. **Plan 0065 / ADR-0060 = glossary hover-explanations (2026-07-08, paired)**. **Plan 0066 = advisor tiered-forecast unification (2026-07-08, no ADR)**. **Plan 0067 / ADR-0061 = trendline pattern identification (2026-07-08, paired; amends ADR-0049's colour-by-role)**. **Plan 0068 / ADR-0062 = user chart-style settings (2026-07-08, paired; extends ADR-0039's renderer-owned presentation prefs)**. **Plan 0069 / ADR-0063 = Russian localization / full parity (2026-07-08, paired; in-house zero-dep i18n + sidecar reason-codes, extends ADR-0039)**. **Plan 0070 / ADR-0064 = generated CI-gated sidecar API reference (2026-07-09, paired; live-introspection docgen under `docs/reference/`)**. **Plan 0071 = candlestick pattern legend + declutter (2026-07-09, no ADR — reuses ADR-0061's grouped-legend interaction, serial-after Plan 0067)**. **Plan 0072 / ADRs 0065 + 0066 = codebase remediation from the 2026-07-09 audit (2026-07-09, paired; neutral UI-event buffer core + short-lived SSE ticket)**. **Plan 0073 / ADR-0067 = Ichimoku Cloud indicator (2026-07-09, paired; refines ADR-0023's trend classification, render gated on Plan 0072 ph8)**. **Plan 0074 / ADR-0068 = technical-read advisory tier (2026-07-09, paired; extends ADR-0029 with a single-indicator, no-conviction, no-levels directional call)**. **Plan 0075 = Ichimoku strategy + backtest (2026-07-09, no ADR — follows ADR-0004/0050; prereq Plan 0073 ph1)**. **Plan 0076 = OBV chart overlay (2026-07-09, no ADR — additive overlay kind, render gated on Plan 0072 ph8)**. **Plan 0077 / ADRs 0070 + 0071 = forecast pivot to volatility + regime-transition, direction demoted (2026-07-10, paired; ADR-0070 extends ADR-0030's targets, ADR-0071 amends ADR-0029's fuse gating)**. **ADR-0069 = crypto-first asset-class positioning (2026-07-10, standalone, accepted; no paired plan)**. **Plans 0078 + 0079 / ADR-0072 = the trade-the-discrepancy + Polymarket-convergence expansion (2026-07-11): Plan 0078 Polymarket convergence screener (read-only, consumes ADR-0041 / prereq Plan 0040, no new plan-paired ADR), Plan 0079 cross-pool discrepancy scanner (read-only arb evidence layer), and exploratory ADR-0072 (bounded-autonomy atomic-arb execution + Polymarket-as-venue, extends ADR-0025 — posture, no paired plan yet).** **ADR-0073 = execution engine topology (2026-07-11, exploratory posture, no paired plan): the desktop app is the control plane, not the execution host; the latency/autonomous tier is a separate top-level Go `engine/` package in this monorepo, colocated + headless, reached over a private tunnel with a dead-man's switch — the foundation ADR-0043/0044/0072 + future execution plans hang off.** **ADR-0074 = edge-selection criteria for execution (2026-07-11, accepted, standalone strategic positioning, sibling to ADR-0069): build execution only for capacity-constrained / analytically-sourced / honestly-validated / neglected-niche / decay-monitored edges (ES-1..6), refuse latency-arms-race niches outright; gates every future execution plan.** **Plan 0080 / ADR-0075 = advisor recommendation track record (2026-07-11, paired): score the advisor's own live calls path-dependently against realized price, baseline-relative + calibrated, append-only every-call ledger + scheduled scorer + read-only view; the live instrument for ADR-0074 ES-3/ES-5. Approved 2026-07-11 and user-designated the NEXT implementation — migration-bearing, so serialize vs Plan 0044.** **Plan 0081 / ADR-0076 = Coinbase USD-native crypto OHLCV source (2026-07-11, paired; refines ADR-0052 routing two-source→N, serves ADR-0069, adds a migration).** **Plan 0082 / ADR-0077 = Bollinger Bands render + user-originated chart overlay controls (2026-07-11, paired; ADR-0077 refines ADR-0015 like ADR-0039/0062 — user-added indicator overlays are renderer-owned display prefs, never cross the wire; renderer-only + migration-free; OBV picker entry gated on Plan 0076).** **Plan 0083 / ADR-0078 = chart-pattern visual fidelity (2026-07-11, paired; refines ADR-0048's connect-the-extremes anchors to a converging envelope + extends ADR-0049/0061 render with apex + confirmed-only breakout arrow; pattern-by-pattern, symmetrical triangle first; migration-free).** **Plan 0084 / ADR-0079 = DeFi P&L completeness for gauge-staked Aerodrome positions (2026-07-11, paired; refines ADR-0036 — gauge→pool resolution so gauge `getReward`/stake txs join the right position, swap booking as lot conversions, fallback price source, and a bounded current-state `unclaimed_rewards` on-chain read; from a live re-run showing 4/5 positions still incomplete; read-only, determinism preserved by snapshot-caching).** **Plan 0085 = candlestick pattern explanations (2026-07-11, no ADR — realizes ADR-0060's glossary for a new `candlestick` category, honours ADR-0046 glossary-never-on-wire; renderer-only, legend + chart-tooltip, English-first).** **Plan 0086 / ADR-0080 = concentrated-liquidity pricing for the cross-pool scanner v2 (2026-07-11, paired; from Plan 0079's Phase-4 null — the CP-only v1 measured only the dust tail, so move to an executable-quote model that prices Uni-v3 / Aerodrome Slipstream via the DEX Quoter over `eth_call`, still read-only; completeness-then-niches evidence per ADR-0074; ADR-0080 refines the Plan 0079 cost model, breaking `ArbObservation` schema change).** **Plan 0087 / ADR-0081 = DeFi P&L wallet-total gap close (2026-07-12, paired, closed; refines ADR-0079/0036 — keyed Alchemy historical-price fallback replacing the inert keyless CoinGecko adapter + a bare outbound-`send` position-token `custody_move` classification; a secret not a package, migration-free).** **Plan 0088 / ADR-0082 = per-LP time-windowed profitability + partial wallet totals (2026-07-12, paired, closed; amends ADR-0036 — partial wallet totals over complete positions never null-everything, per-position 7d/30d/90d/all realized (exact, `now`-anchored) + labeled estimated total-return, LP-first; from the Plan 0087 smoke + the user's stated priority; read-only, migration-free).** **Plan 0089 = Polymarket open-in-browser market links + explicit sort (2026-07-12, no ADR — followup to Plan 0078 from its live smoke; realizes ADR-0041, records a provenance-link boundary call inside ADR-0029, honours ADR-0008 external-nav; additive `market_url` from the Gamma event slug, migration-free).** **Plan 0090 / ADR-0083 = squeeze metric (Bollinger band-width percentile + Keltner TTM `squeeze_on`) + counter-trend volume decomposition anchored to the snapshot trend (2026-07-12, paired; ADR-0083 records the *semantics*, `bb_width_pct90` mechanics ride ADR-0023; from a `market-analyst` BTC/ETH gap-find).** **Plans 0091 + 0092 = the two-plan technical-analysis completeness program from the 2026-07-12 `market-analyst` capability audit (both draft, backend-paired-with-UI per the user directive): Plan 0091 momentum/divergence/money-flow (oscillators + price↔oscillator divergence detection + MFI/A-D/CMF, realizes ADR-0023 + paired ADR-0090 — the `chart.divergences v1` cross-pane delivery channel, added via a 2026-07-13 backend-delivery amendment), Plan 0092 price-structure & levels (Fibonacci + market-structure trend + pivots + anchored VWAP, paired ADR-0084 — market structure is a distinct second trend read beside the ADR-0067 `trend`).** **Plan 0093 / ADR-0085 = DeFi P&L user-attested dust-token override (2026-07-12, paired; amends ADR-0036 narrowly — a user-maintained `chain:address` dust list values attested-dust tokens at $0 in the replay so they stop blocking an otherwise-complete position, disclosed in notes, loud-failure default intact; from the consolidated-smoke Wanderers finding).** **Plan 0094 / ADR-0086 = CL Quoter revert omit-taxonomy (2026-07-12, paired; refines ADR-0080's read-error taxonomy from the Plan 0086 phase-4 live finding — a quote-leg revert omits the thin pool instead of aborting the scan, a structural-read revert / decode failure still raises; adapter-internal, no schema change; unblocks Plan 0086 phase-5).** **Plan 0095 / ADR-0088 = lightweight-charts v4→v5 migration (2026-07-13, paired; behavior-preserving precursor unblocking Plan 0091 ph6–8 — v4.2.3 has no panes API, v5's `addPane()` is the real substrate for 8+ oscillator/money-flow panes; renderer-internal, no wire/CSP change).** **Plan 0096 / ADR-0089 = chart & app declutter (2026-07-13, paired; renderer-only `ui-builder` overhaul from a UI-clutter review against a TradingView reference — inline chart legend, segmented timeframe, collapsible right dock, named chart presets (built-in + saveable, default Clean), and a nav collapse folding 11 tabs into one grouped menu; ADR-0089 records the preset composition model + the visibility-persistence promotion; drawing dock deferred to a future plan).** **Plan 0097 / ADR-0091 = chart drawing dock (2026-07-13, paired; the drawing tools deferred out of Plan 0096 — a left-edge rail for trendline/ray, h/v-line, rect/zone, and fib, with per-symbol `(time,price)`-anchored persistence + full select/drag/delete/snap editing; ADR-0091 records the two-source annotation model — user drawings renderer-owned/local like ADR-0077, agent drawings on the wire (`annotate_chart` tool + `chart.annotations v1` event) because agent annotation is analysis (ADR-0015), one `DrawingSpec` + one render/merge path with provenance-scoped editing; cross-skill `dev`→`ui-builder`→`human`, serializes after Plan 0096).** **Plan 0098 / ADR-0092 = CandlestickChart god-component refactor (2026-07-13, paired; extracts a plain-TS `ChartController` imperative core so the React component becomes a thin adapter, halting the post-0072 regrowth; behavior-preserving, renderer-only, five `ui-builder` phases + human smoke; hard chart-file contention with 0092/0096/0097 — recommended to run last over the settled surface).** **Plan 0099 / ADRs 0093 + 0094 = DeFi LP out-of-range monitor + notification + advisory rebalance hook (2026-07-13, paired; from a user report of an out-of-range CL LP on Base): ADR-0093 a dedicated dwell-triggered in-sidecar position monitor (sibling to ADR-0055's `WatchScheduler`, shipped path untouched) emitting `defi.position_alert v1`, ADR-0094 an OS-native notification transport (renderer→main `notification:show` IPC — the open-backlog item, extends ADR-0017's SSE, notify-while-fully-closed still deferred per ADR-0016); realizes ADR-0029 for the advisor rebalance recommendation; on-chain rebalancing explicitly out of scope (ADR-0025/0072 boundary). Adds one additive migration.** **Plans 0100–0103 / ADRs 0095–0098 = the upstream-parity batch (2026-07-13, from the inspiration-project gap analysis): Plan 0100 watchlist condition scanners (paired ADR-0095, the shared `_scan_symbols` fan-out harness — squeeze / gainers-losers / momentum over caller symbols on cached bars), Plan 0101 composite quality rank (paired ADR-0096 — a screening rank, not a call; stays on the ADR-0029 conditions side), Plan 0102 crypto sector rotation (paired ADR-0097 — a self-defined versioned taxonomy + equal-weight baskets), Plan 0103 Reddit keyless crowd sentiment (paired ADR-0098 — a 4th sentiment surface beside news/StockTwits/F&G). 0101 + 0102 build on 0100's harness; 0103 is independent; all four are `dev`+`human`, migration-free, no chart contention.** **Plan 0104 / ADR-0099 = drawing read-back + position & range tools (2026-07-14, paired; ADR-0099 refines ADR-0091 — a read-only per-symbol mirror of user drawings crosses the wire for agent consumption, ownership never moves — reuses the ADR-0021 buffer for a `ui.drawing_changed` event (gating amended out by ADR-0101), and extends ADR-0029 to geometry: agent-placed long/short position drawings require rationale+basis at the `annotate_chart` boundary; hard prerequisite Plan 0097, serializes 0096 → 0105 → 0106 → 0097 → 0104 → 0098).** **Plan 0105 / ADR-0100 = chart legibility (2026-07-14, paired; parallel-session sibling — pane labels, fib/pivot/structure label+tooltip fidelity, glossary completeness, zoom-clip, OBV pane reclaim; renderer-only, joins the chart chain adjacent to 0096).** **Plan 0106 / ADR-0101 = remove agent mode (2026-07-14, paired; ADR-0101 amends ADR-0021's gate — the buffer/tool/resource transport stands — and ADR-0099's push-gate clause; always-on gesture forwarding, select-range mode + single-instance stay; runs before 0097 in the chart chain).** **Plans 0107 + 0108 / ADRs 0102 + 0103 = the DeFi-fundamentals + X/social-sentiment tracks (2026-07-15, from the AERO analysis): Plan 0107 DeFi token/protocol fundamentals (paired ADR-0102 — keyless DefiLlama primary + best-effort Aerodrome-native emissions/veAERO deep tier, closing the fundamentals blind spot for on-chain-native assets), Plan 0108 X/social sentiment (paired ADR-0103 — a 5th `SentimentSource` behind the seam, LunarCrush-recommended, keyed with honest-empty-without-key; "design now, decide spend later", deliberately breaks keyless-first). Both `dev`+`human`, migration-free, no chart contention, independent of the chart chain and of each other.** **Plan 0109 / ADR-0104 = MCP tool-surface consolidation (2026-07-15, paired; from a tool-count investigation): ADR-0104 adopts a "one tool per verb; a new *mode* of an existing verb extends that tool via a discriminator, only a new verb earns a new top-level tool" granularity rule with `EXPECTED_FULL_TOOLSET` as the enforced budget ledger, and Plan 0109 executes it by folding five same-verb clusters (watchlist scanners, forecast kinds, sentiment sources, price-structure reads, volume reads) into discriminated tools — surface 62 → 50, behavior-preserving. Reroutes queued 0103/0108 (sentiment) + 0042 (collapse plural risk tools). Migration-free, no chart contention.** **Plan 0110 = watch management UX (2026-07-15, no ADR — extends ADR-0055's "agent creates, viewer manages" grain: condition + created_at rendering, viewer delete with cascade, nullable `note` column agent-set/viewer-edited, history echo via render-time join; migration-bearing).** **0111 / ADR 0105** are the real next-free for any unrelated work.
- **One plan per file.** No `0004a`/`0004b` splits — if work grows, write a new numbered plan and reference the parent.
- **Plans aren't ADRs.** A plan says *what we're building and how* (it expires); an ADR says *why we chose this over the alternatives* (it doesn't). If a plan's decision warrants permanent capture, write an ADR too.
- **Plans don't move until the close ceremony.** Implementers commit per phase but never `git mv` to `done/`. The architect reviews the whole plan, then flips status + moves the file in one commit.
- **In-progress plans are append-only on substance.** The only mid-flight edits are the `Status:` line and minor honesty fixes. Structural amendments happen via a new followup plan.
- **Cross-references stay link-shaped** so they survive renumbering and the move to `done/`.
